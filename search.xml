<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Characterizing, Modeling, and Benchmarking RocksDB Key-Value Workloads at Facebook 论文笔记</title>
    <url>/2020/07/16/FAST20-RocksDBBenchmark/</url>
    <content><![CDATA[<h1 id="摘要"><a href="#摘要" class="headerlink" title="摘要"></a>摘要</h1><p>在当今的IT基础设施中，持久性KV存储被广泛用作构建块，用于管理和存储大量数据。然而，由于缺乏跟踪/分析工具以及在操作环境中收集跟踪的困难，表征KV存储的真实工作负载的研究受到了限制。在本文中，我们首先对Facebook上三个典型的RocksDB生产用例的工作负载进行了详细表征:UDB(用于社交图数据的MySQL存储层)、ZippyDB(分布式KV存储)和UP2X(用于AI/ML服务的分布式KV存储)。这些特征揭示了几个有趣的发现:首先，键和值大小的分布与用例/应用程序高度相关;二是KV对的访问具有良好的局部性，并遵循一定的特殊模式;第三，收集的性能指标在UDB中显示强烈的昼夜变化模式，而其他两个没有。</p>
<p>我们进一步发现，尽管广泛使用的KV基准测试YCSB提供了各种工作负载配置和KV对访问分布模型，但是由于忽视键的空间局部性，YCSB触发的基础存储系统工作负载仍不足以与我们收集的工作负载接近。为了解决这个问题，我们提出了一种基于键范围的建模，并开发了一个可以更好地模拟实际KV存储的工作负载的基准测试。这个基准测试可以综合生成更精确的KV查询，表示对底层存储系统的KV存储的读写。</p>
<h1 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h1><p>在当前的IT基础架构中，持久KV存储广泛用作支持各种上层应用程序的存储引擎。KV存储的高性能，灵活性和易用性吸引了更多的用户和开发人员。许多现有的系统和应用程序，例如文件系统，基于对象的存储系统，SQL数据库，甚至AI/ML系统，都使用KV存储作为后端存储来实现高性能和高空间效率。</p>
<p>但是，调整和提高KV存储的性能仍然具有挑战性。首先，对KV存储的实际工作负载表征和分析的研究非常有限，并且KV存储的性能与应用程序生成的工作负载高度相关。其次，表征KV存储工作负载的分析方法不同于现有的块存储或文件系统工作负载表征研究。KV存储有简单但非常不同的接口和行为。一套好的工作负载收集、分析和表征工具可以在优化性能和开发新功能方面使KV存储的开发人员和用户受益。第三，在评估KV存储的底层存储系统时，不知道KV存储基准测试生成的工作负载是否代表真实的KV存储工作负载。</p>
<p>为了解决这些问题，在本文中，我们在Facebook上对RocksDB（高性能持久性KV存储）的工作负载进行了表征，建模和基准测试。据我们所知，这是表征持久性KV存储工作负载的首次研究。首先，我们介绍了一套可在生产中使用的工具，用于收集KV级别的查询跟踪，重放这些跟踪以及分析跟踪。这些工具在RocksDB版本中开源，并在Facebook内用于调试和对KV存储的性能调整。其次，为了更好地理解KV工作负载及其与应用程序的相关性，我们选择了Facebook上的三个RocksDB用例进行研究:1)UDB、2)ZippyDB和3)UP2X。这三个用例是如何使用KV存储的典型例子:1)作为SQL数据库的存储引擎，2)作为分布式KV存储的存储引擎，3)作为人工智能/机器学习(AI/ML)服务的持久存储。</p>
<p>UDB是Facebook上社交图数据的MySQL存储层，而RocksDB被用作其后端存储引擎。社交图数据保存在MySQL表中，表行作为KV对存储在RocksDB中。从MySQL表到RocksDB KV对的转换是通过MyRocks实现的。ZippyDB是一个分布式KV存储，使用RocksDB作为存储节点来实现数据持久性和可靠性。ZippyDB通常将诸如照片元数据和对象元数据之类的数据存储在存储中。本文中，ZippyDB的工作负载是从存储了Facebook对象存储系统（论文中称为ObjStorage）元数据的分片中收集的。键通常包含ObjStorage文件或数据块的元数据，值存储了相应的对象地址。UP2X是基于RocksDB的特殊分布式KV存储。UP2X存储用于在Facebook上预测和推断多个AI/ML服务的配置文件数据（例如，计数器和统计信息）。因此，UP2X中的KV对经常更新。</p>
<p>基于一系列收集的工作负载，我们进一步探讨了KV存储的特定特征。根据我们的分析，我们发现1）读取在UDB和ZippyDB中占主导地位，而读-修改-写（Merge）是UP2X中的主要查询类型； 2）由于上层应用程序的键构成设计，键的大小通常很小且分布较窄，并且仅在某些特殊情况下才会出现较大的值； 3）大多数KV对是冷的（访问较少），只有很少一部分KV对是经常访问的； 4）Get，Put和Iterator具有很强的键空间局部性（例如，频繁访问的KV对位于键空间中相对较近的位置内），并且与上层应用程序的请求位置密切相关的某些键范围非常热（经常访问）； 5）与ZippyDB和UP2X中的访问方式不同，UDB中的访问方式明确地表现为昼夜变化模式。 </p>
<p>基准测试被广泛地用于评估KV存储的性能和测试存储系统。通过对真实世界的跟踪，我们将研究现有的KV基准测试是否可以综合生成具有类似特性的存储I/O的真实世界工作负载。YCSB是最广泛使用的KV基准测试之一，并已成为KV存储基准测试的黄金标准。它提供了不同的工作负载模型、各种查询类型、灵活的配置，并支持大多数广泛使用的KV存储。YCSB可以帮助用户以一种方便的方式模拟真实的工作负载。然而，我们发现，尽管YCSB可以生成具有类似ZippyDB工作负载中所示的KV查询统计信息的工作负载，但RocksDB存储I/O可能非常不同。这个问题主要是由YCSB生成的工作负载忽略了键的空间局部性造成的。在YCSB中，热KV对要么在整个键空间中随机分配，要么聚集在一起。这导致存储中访问的数据块和与KV查询关联的数据块之间的I/O不匹配。如果不考虑键的空间局部性，基准测试产生的工作负载将导致RocksDB有更大的读放大和更小的写放大，相比于真实世界工作负载。</p>
<p>为了开发可以更精确地模拟KV存储工作负载的基准，我们提出了一种基于键范围的热度的工作负载建模方法。整个键空间被划分为较小的键范围，并且我们对这些较小的键范围的热度进行建模。在新的基准测试中，将根据键范围热度的分布将查询分配给键范围，并且在每个键范围中热键将被紧密地分配。在我们的评估中，在相同配置下，与实际工作负载相比，YCSB导致读取字节至少增加500％，并且在RocksDB中仅提供17％的缓存命中。 由我们提出的新基准测试产生的工作负载仅增加了43％的读取字节，并实现了RocksDB中约77％的缓存命中，因此与实际工作负载更加接近。 此外，我们以UDB为例，说明新基准测试生成的综合工作负载在键/值大小，KV对访问和迭代器扫描长度方面具有良好的分布。</p>
<p>本文组织如下。首先，我们将在第2节中介绍RocksDB以及三个RocksDB用例的系统背景。我们将在第3部分中描述我们的方法和工具。这三个用例的详细工作负载特征(包括通用查询统计信息、键和值大小以及KV对访问分布)分别在4、5和6中给出。在第7节中，我们展示了YCSB存储统计数据的调查结果，并描述了提出的新的建模和基准测试方法。我们也比较了YCSB和新基准的结果。相关工作描述在第8节中，我们在第9节中总结了论文。</p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>在本节中，我们首先简要介绍KV存储和RocksDB。然后，我们提供了Facebook、UDB、ZippyDB和UP2X上的三个RocksDB用例的背景知识，以促进对它们工作负载的理解。</p>
<h2 id="KV存储和RocksDB"><a href="#KV存储和RocksDB" class="headerlink" title="KV存储和RocksDB"></a>KV存储和RocksDB</h2><p>KV存储是一种基于{key，value}对存储和访问数据的数据存储。键唯一地标识KV对，并且值保存数据。KV存储被公司广泛用作分布式哈希表（例如Amazon Dynamo），内存数据库（例如Redis）和持久性存储（例如Google的BigTable和Facebook的RocksDB）。</p>
<p>RocksDB是Facebook源自LevelDB的高性能嵌入式持久KV存储，并针对诸如SSD之类的快速存储设备进行了优化。许多大型网站也使用RocksDB，例如阿里巴巴，雅虎和LinkedIn。 在Facebook上，RocksDB用作多种数据存储服务的存储引擎，例如MySQL，Laser，Cassandra，ZippyDB和AI/ML平台。</p>
<p>RocksDB支持KV接口，例如Get，Put，Delete，Iterator，SingleDelete，DeleteRange和Merge。Get，Put和Delete分别用于读取，写入和删除具有特定键的KV对。Iterator用于从开始键开始扫描一组连续的KV对。扫描方向可以是向前（调用Nexts）或向后（调用Prevs）。SingleDelete仅可用于删除尚未被覆盖的KV对。DeleteRange用于删除[start, end)之间的键范围（删除中不包含结束键）。RocksDB将读取-修改-写入的语义封装到称为Merge的简单抽象接口中，这避免了在每次Put之前随机获取Get的性能开销。Merge将写入delta的增量存储到RocksDB中，并且可以堆叠或合并这些增量。这会导致较高的读取开销，因为“获取一个键”需要查找并合并所有以前存储的增量与Merge插入的相同键。Merge功能由用户定义为RocksDB插件。</p>
<p>RocksDB采取一个LSM-Tree来维护持久存储的KV对。RocksDB的基础架构如Figure 1所示。一个RocksDB维持至少一个逻辑分区，即列族，它有自己的内存写缓冲区（Memtable）。当一个Memtable满了，它被刷到文件系统并存储为Sorted Sequence Table文件（SST）。SST文件以排序的方式持久存储KV对，并按从Level-0开始的一系列levels进行组织。当一个级别达到其极限时，将选择一个SST文件与下一个级别具有重叠键范围（称为Compaction）的SST文件合并。 </p>
<p><img src="/images/RocksDB1.png" alt="img"></p>
<h2 id="三个RocksDB用例的背景"><a href="#三个RocksDB用例的背景" class="headerlink" title="三个RocksDB用例的背景"></a>三个RocksDB用例的背景</h2><p>我们讨论了RocksDB在Facebook的三个重要的大规模生产用例:1)UDB 2)ZippyDB 3)UP2X。在所有三个用例中都使用了切分来实现负载平衡。因此，所有切片的工作负载非常相似，我们从每个用例中随机选择三个RocksDB实例来收集跟踪。</p>
<h3 id="UDB"><a href="#UDB" class="headerlink" title="UDB"></a>UDB</h3><p>Facebook上的社交图数据永久存储在分片的MySQL数据库层中。缓存读取未命中以及对社交图数据的所有写入均由UDB服务器处理。UDB依靠MySQL实例来处理所有查询，并且这些查询通过MyRocks转换为RocksDB查询。许多社交图数据都以对象和关联的形式表示，并根据模型保存在不同的MySQL表中。RocksDB使用不同的列族来存储对象和关联相关的数据。</p>
<p>在UDB中有6个主要的列族：Object，Assoc，Assoc_count，Object_2ry，Assoc_2ry和Non_SG。Object存储社交图对象数据，而Assoc存储社交图关系数据，关系定义了两个对象的连接。Assoc_count存储每个对象的关系数量。关系计数器总是被更新成新的值并且没有任何删除。Object_2ry和Assoc_2ry是维护对象和关系各自的二级索引的列族。他们还用于ETL。Non_SG存储来自其他非社交图相关服务的数据。</p>
<p>由于UDB工作负载是一个从SQL查询转换而来的KV查询的示例，因此存在一些特殊的模式。我们收集了14天的踪迹。由于三个UDB服务器的工作负载特征非常相似，所以我们只给出其中一个。此服务器中的跟踪文件总大小约为1.1TB。对于某些特性，日常数据更为重要。因此，我们还分别分析了14天时间段(24小时跟踪)中最后一天的工作负载。</p>
<h3 id="ZippyDB"><a href="#ZippyDB" class="headerlink" title="ZippyDB"></a>ZippyDB</h3><p>在RocksDB的基础上开发了高性能分布式KV存储，并依靠Paxos实现数据的一致性和可靠性。KV对被划分为切片，每个切片由一个RocksDB实例支持。选择一个副本作为切片，其他副本作为次要切片。主切片处理对某个切片的所有写操作。如果读取需要强一致性，则读取请求(如Get和Scan)仅由主切片处理。一个ZippyDB查询被转换为一组RocksDB查询(一个或多个)。</p>
<p>与UDB用例相比，ZippyDB中的上层查询直接映射到RocksDB查询，因此ZippyDB的工作负载特征非常不同。我们随机选取了ZippyDB的三个主切片，收集了24小时的踪迹。像UDB一样，我们只展示其中的一个。这个切片存储ObjStorage的元数据，ObjStorage是Facebook的一个对象存储系统。在这个分片中，一个KV对通常包含一个ObjStorage文件或一个数据块及其地址信息的元数据信息。</p>
<h3 id="UP2X"><a href="#UP2X" class="headerlink" title="UP2X"></a>UP2X</h3><p>Facebook使用各种AI/ML服务来支持社交网络，并且大量动态变化的数据集（例如用户活动的统计计数器）用于AI/ML预测和推理。UP2X是一种分布式KV存储，专门用于将这种类型的数据存储为KV对。当用户使用Facebook服务时，UP2X中的KV对会经常更新，例如计数器增加时。如果UP2X在每个Put之前调用Get来实现读-修改-写操作，由于随机Get的速度相对较慢，它将产生很高的开销。UP2X利用RocksDB Merge接口避免在更新过程中获取Gets。</p>
<p>UP2X中的KV对被划分为RocksDB实例支持的分片。我们从UP2X中随机选择了三个RocksDB实例，然后收集并分析了24小时跟踪。 请注意，在Compaction期间，通过Compaction过滤器清除了由Merge插入的KV对。Compaction过滤器在Compaction期间使用自定义逻辑在后台删除或修改KV对。 因此，即使不使用删除操作（例如Delete，DeleteRange和SingleDelete），也会从UP2X中删除大量KV对。</p>
<h1 id="方法和工具集"><a href="#方法和工具集" class="headerlink" title="方法和工具集"></a>方法和工具集</h1><p>为了从不同用例分析和描述RocksDB工作负载，并生成合成工作负载，我们提出并开发了一组KV存储的跟踪、回放、分析、建模和基准测试工具。这些工具已经在RocksDB发行版中开源了。在本节中，我们将介绍这些工具，并讨论如何使用它们来描述和生成KV存储的工作负载。</p>
<h2 id="跟踪"><a href="#跟踪" class="headerlink" title="跟踪"></a>跟踪</h2><p>跟踪工具在RocksDB公共KV接口收集查询信息，并作为记录写入跟踪文件。它在每个跟踪记录中存储以下五种类型的信息:1)查询类型，2)列族ID, 3)键，4)特定查询数据，5)时间戳。对于Put和Merge，我们将值信息存储在特定查询数据中。对于像Seek和SeekForPrev这样的迭代器查询，扫描长度(在Seek或SeekForPrev之后调用的Next或Prev的数量)存储在特定查询数据中。时间戳是在以微秒级的精度调用RocksDB公共接口时收集的。为了在跟踪文件中记录每个查询的跟踪记录，使用了一个锁来序列化所有查询，这可能会导致一些性能开销。但是，根据正常生产工作负载下的生产性能监视统计数据，我们没有观察到跟踪工具会导致主要吞吐量下降或延迟增加。</p>
<h2 id="跟踪重放"><a href="#跟踪重放" class="headerlink" title="跟踪重放"></a>跟踪重放</h2><p>收集到的跟踪文件可以通过在db_bench中实现的Replayer工具进行重放。重播工具根据跟踪记录信息向RocksDB发出查询，查询之间的时间间隔遵循跟踪中的时间戳。通过设置不同的快进和多线程参数，RocksDB可以对不同强度的工作负载进行基准测试。但是，多线程不能保证查询顺序。Replayer生成的工作负载可以看作是真实世界的工作负载。</p>
<h2 id="跟踪分析"><a href="#跟踪分析" class="headerlink" title="跟踪分析"></a>跟踪分析</h2><p>使用收集的跟踪进行回放有其局限性。由于工作负载跟踪的潜在性能开销，很难跟踪大规模和持久的工作负载。此外，跟踪文件的内容对用户/所有者来说是敏感和保密的，因此RocksDB用户很难与其他RocksDB开发人员或来自第三方公司的开发人员(例如，上层应用程序开发人员或存储供应商)共享跟踪，以进行基准测试和性能调优。为了解决这些限制，我们提出了一种分析RocksDB工作负载的方法，该方法根据跟踪中的信息对工作负载进行概要分析。</p>
<p>跟踪分析工具读取跟踪文件并提供以下特征:1)每个CF中的KV对、查询编号和查询类型的详细统计摘要;2)键大小和值大小统计;3)KV对的流行程度;4)键空间局部性，将访问的键与数据库中现有的键按排序顺序进行组合;和5)查询每秒(QPS)统计。</p>
<h2 id="建模和基准测试"><a href="#建模和基准测试" class="headerlink" title="建模和基准测试"></a>建模和基准测试</h2><p>我们首先计算任意两个选定变量之间的皮尔逊相关系数，以确保这些变量具有非常低的相关性。这样，可以分别对每个变量建模。然后，我们将收集到的工作负载拟合到不同的统计模型中，找出哪个拟合误差最低，这比总是将不同的工作负载拟合到同一模型更准确。然后，提出的基准可以基于这些概率模型生成KV查询。细节将在第7节中讨论。</p>
<h1 id="工作负载的一般统计"><a href="#工作负载的一般统计" class="headerlink" title="工作负载的一般统计"></a>工作负载的一般统计</h1><p>本节中，我们介绍每个用例的一般工作负载统计信息，包括每个CF中的查询组成，KV对热度分布和每秒查询。</p>
<h2 id="查询组成"><a href="#查询组成" class="headerlink" title="查询组成"></a>查询组成</h2><p>通过分析查询组成，我们可以计算出查询强度、不同用例中查询类型的比率以及查询的流行度。我们发现:1)Get在UDB和ZippyDB中是使用最频繁的查询类型，而Merge在UP2X中占主导地位;2)查询组成在不同的CFs中可能有很大的不同。</p>
<h3 id="UDB-1"><a href="#UDB-1" class="headerlink" title="UDB"></a>UDB</h3><p>如下图Figure 2所示，存储在相同RocksDB的不同列族，他们的工作负载是很不一样的。如果没有分离不同的列族，很难对这种混合工作负载进行分析和建模。</p>
<p><img src="/images/RocksDB2.png" alt="img"></p>
<h3 id="ZippyDB-1"><a href="#ZippyDB-1" class="headerlink" title="ZippyDB"></a>ZippyDB</h3><p>只有一个列族被用在ZippyDB上。Get是ZippyDB上的主要查询类型，它与ObjStorage的读密集型工作负载保持一致。</p>
<h3 id="UP2X-1"><a href="#UP2X-1" class="headerlink" title="UP2X"></a>UP2X</h3><p>UP2X的查询组成与读主导的UDB/ZippyDB完全不同。读-修改-写（Merge）是UP2X的主要工作负载模式。</p>
<h2 id="KV对热度分布"><a href="#KV对热度分布" class="headerlink" title="KV对热度分布"></a>KV对热度分布</h2><p>为了了解每个案例中KV对的热度，我们计算了在24小时跟踪期内每个KV对被访问了多少次，并以累积分布函数（CDF）图显示了它们。 X轴是访问计数，Y轴是0和1之间的累加比。我们发现，在UDB和ZippyDB中，大多数KV对都是冷的。</p>
<h3 id="UDB-2"><a href="#UDB-2" class="headerlink" title="UDB"></a>UDB</h3><p>Get和Put的KV对访问计数CDF如下图Figure 3所示。从图3（a）中可以看出，在Assoc中，超过70%的KV对是至少发生2次的Get请求，而其他的列族都不超过40%，这说明Assoc的读miss比其他列族更加频繁。从图3（b）可以看出所有列族的写miss都很小，这是因为大多数KV对都很少更新。</p>
<p><img src="/images/RocksDB3.png" alt="img"></p>
<p>24小时内start-keys of Iterators的访问计数CDF如下图Figure 4所示。大多数start-keys只被使用一次，显示了一个低的访问局部性。扫描长度非常大（大于10,000）非常罕见，但是我们仍然可以在Non_SG和Assoc中找到这种类型的一些示例。</p>
<p><img src="/images/RocksDB4.png" alt="img"></p>
<p>不同时间段内被访问的唯一键的数量如下表Table 1所示。在过去的24小时不到3%的键被访问。在过去的14天，所有列族的比例仍低于15%。所以在这个用例中，RocksDB的大部分键是冷的。一方面，大多数读取请求由上层缓存层响应，只有读miss会触发RocksDB的查询。另一方面，社交媒体数据具有很强的时间局部性。人们可能会访问Facebook上最近发布的内容。</p>
<p><img src="/images/RocksDB5.png" alt="img"></p>
<h3 id="ZippyDB-2"><a href="#ZippyDB-2" class="headerlink" title="ZippyDB"></a>ZippyDB</h3><p>访问计数分布如下图Figure 5所示。读取查询显示了非常好的局部性，而大多数KV对只在过去24小时内Put和Delete一次。对Iterator_seek的start-keys访问很特殊，这是由于ObjStorage中元数据扫描请求造成的。例如，如果一个KV对存储了文件的第一个块的元数据，那么当请求整个文件时，它总是被用作Iterator_seek的start_key。</p>
<p><img src="/images/RocksDB6.png" alt="img"></p>
<h3 id="UP2X-2"><a href="#UP2X-2" class="headerlink" title="UP2X"></a>UP2X</h3><p>KV对访问计数的CDF分配如下图Figure 6所示。Merge和Get有广泛的访问计数分布。如果我们定义KV对在24小时内访问10次以上为热的KV对，那么通过Get访问的大约50%的KV对和通过Merge访问的25%的KV对是热的。</p>
<p><img src="/images/RocksDB7.png" alt="img"></p>
<h2 id="QPS"><a href="#QPS" class="headerlink" title="QPS"></a>QPS</h2><p>QPS度量显示了工作负载随时间变化的强度。部分列族在UDB中QPS具有较强的昼夜变化规律，而ZippyDB和UP2X中QPS的昼夜变化较小。每日QPS的变化与社交网络行为有关。</p>
<h3 id="UDB-3"><a href="#UDB-3" class="headerlink" title="UDB"></a>UDB</h3><p><img src="/images/RocksDB8.png" alt="img"></p>
<p>由于世界各地Facebook用户的行为，某些列族（例如Assoc和Non_SG）和某些查询类型（例如Get和Put）具有很强的昼夜模式。如图7（a）所示，Get或Put的QPS通常从大约8:00 PST开始增加，并在大约17:00 PST达到峰值。然后，QPS迅速下降并在太平洋标准时间23:00左右达到最低点。Delete，SingleDelete和Iterator的QPS会显示变化，但很难观察到任何昼夜模式，这些查询是由Facebook内部服务触发的，与用户行为的相关性较低，六个列族的QPS如图7所示（b）。Assoc和Non_SG的昼夜变化很大，但Non_SG的QPS更加尖锐。由于ETL请求不是由Facebook用户触发的，因此Object_2ry的QPS很尖刻，我们无法找到任何清晰的模式。</p>
<h3 id="ZippyDB-3"><a href="#ZippyDB-3" class="headerlink" title="ZippyDB"></a>ZippyDB</h3><p>ZippyDB的QPS与UDB不同。ZippyDB的QPS在24小时内变化，但是我们没有发现昼夜变化模式，尤其是对于Put，Delete和Iterator_Seek。由于ObjStorage是存储在Facebook上的对象，因此对象读取与社交网络行为有关。因此，Get的QPS在晚上相对较低，而在白天则相对较高（基于特定的标准时间）。由于范围查询（Iterator_Seek）通常不是由Facebook用户触发的，因此该查询类型的QPS是稳定的，并且大多数时候在100到120之间。</p>
<h3 id="UP2X-3"><a href="#UP2X-3" class="headerlink" title="UP2X"></a>UP2X</h3><p>UP2X中的Get或Put的QPS没有很强的昼夜变化模式。但是，Merge的使用与Facebook用户的行为密切相关，例如查看帖子，喜欢和其他操作。因此，Merge的QPS在晚上相对较低（约1000），在白天较高（约1500）。</p>
<h1 id="键和值的大小"><a href="#键和值的大小" class="headerlink" title="键和值的大小"></a>键和值的大小</h1><p>键大小和值大小是了解KV存储工作负载的重要因素。它们与性能和存储空间效率密切相关。表2中显示了键和值大小的平均值（AVG）和标准偏差（SD），图8显示了键和值大小的CDF。总体来说键大小通常很小，并且分布较窄，而值大小与数据类型密切相关。键大小的标准偏差相对较小，而值大小的标准偏差较大。UDB的平均值大小大于其他两个值。</p>
<p><img src="/images/RocksDB9.png" alt="img"></p>
<p><img src="/images/RocksDB10.png" alt="img"></p>
<h1 id="键的空间和时间模式"><a href="#键的空间和时间模式" class="headerlink" title="键的空间和时间模式"></a>键的空间和时间模式</h1><p>RocksDB中的KV对被排序并存储在SST文件中。为了理解和可视化键空间位置，我们按照存储在RocksDB中的顺序对所有现有键进行排序，并绘制出每个KV对的访问计数，这称为整个键空间的热图。每个现有的键被分配一个唯一的整数作为其键id，基于其排序顺序并从0开始。我们将这些键id称为键序列。</p>
<p>KV对访问显示了一些特殊的时间模式。例如，一些KV对在短时间内被密集访问。为了理解时间模式和键空间局部性之间的关系，我们使用时间序列顺序来可视化这些模式。我们按升序对键排序，并按照前面讨论的键id为它们分配键，这个键序列用作x轴。y轴显示调用查询的时间。为了简化y轴值，我们将每个查询的时间戳相对于跟踪的开始时间移动。时间序列图中的每个点表示当时对某个键的请求。在UDB用例中，根据MyRocks的键组成，键的前4字节是MySQL表索引号。我们用红色竖线将键空间分隔为属于不同表的不同键范围。</p>
<p>三个用例的热图显示了很强的键空间局部性。热KV对紧密地位于键空间中。UDB的Delete和Single Delete，UP2X的Merge的时间序列图显示了很强的时间局部性。对于某些查询类型，某些键范围中的KV对会在短时间内被大量访问。</p>
<h2 id="UDB-4"><a href="#UDB-4" class="headerlink" title="UDB"></a>UDB</h2><p>我们使用Object和Assoc_count中Get操作在24小时内的热图作为示例来显示键空间局部性，如下图Figure 9所示。热KV对(具有高访问计数)通常位于一个小的键范围内，彼此接近。也就是说，它们显示了一个很强的键空间局部性(由稠密的绿色区域表示)。一些MySQL表(红色竖线之间的键范围)非常热(例如，Object中的绿色稠密区域)，而其他表没有KV对访问。</p>
<p><img src="/images/RocksDB11.png" alt="img"></p>
<p>大多数KV对仅删除一次，因此不太可能重新插入。因此，在Delete和SingleDelete查询中没有热的KV对。但是，它们显示了一些特殊的模式。例如，附近的一些KV对在短时间内被删除，如图10所示。</p>
<p><img src="/images/RocksDB12.png" alt="img"></p>
<p>通常，不会在整个键空间中随机访问KV对。大多数KV对不被访问或访问次数很少。 KV对中只有一小部分非常热。这些模式出现在整个键空间中，并且出现在不同的键范围内。属于同一MySQL表的KV对物理上存储在一起。不同级别的一些SST文件或同一SST文件中的数据块非常热。因此，Compaction和缓存机制可以相应地进行优化。</p>
<h2 id="ZippyDB-4"><a href="#ZippyDB-4" class="headerlink" title="ZippyDB"></a>ZippyDB</h2><p>ZippyDB中的Get热图显示了很好的键空间局部性，如下图Figure 11所示。Get访问的KV对具有较高的访问计数，并且集中在几个关键范围内（例如1×106和5×106之间）。热KV对不是随机分布的，相反，这些KV对集中在几个小的关键范围内。通常，ZippyDB工作负载是读取密集型的，并且具有非常好的键空间局部性。</p>
<p><img src="/images/RocksDB13.png" alt="img"></p>
<h2 id="UP2X-4"><a href="#UP2X-4" class="headerlink" title="UP2X"></a>UP2X</h2><p>下图Figure 12中所示的Getas访问的所有KV对的热图，我们可以在热和冷KV对之间找到清晰的边界。这种特殊的局部性可能是由AI/ML服务及其数据更新模式的独特行为引起的。</p>
<p><img src="/images/RocksDB14.png" alt="img"></p>
<p>UP2X用例在Merge中显示了非常强大的键空间局部性和时间局部性。但是，Merge插入的KV对中约有90％实际上是在Compaction过程中清理的。由于键空间热图未显示通过Compaction清除的KV对的存在，因此我们绘制了Merge的时间序列顺序，这可以指示所有KV对的Merge访问，如下图Figture 13所示。绿色块表示一小部分KV对在半小时内被Merge集中调用。在此之后，在接下来的半个小时内，Merge将密集地访问一组新的KV对(具有增量组成的键)。这些KV对在Compaction时被清楚。</p>
<p><img src="/images/RocksDB15.png" alt="img"></p>
<h1 id="建模和基准测试-1"><a href="#建模和基准测试-1" class="headerlink" title="建模和基准测试"></a>建模和基准测试</h1><p>在理解了一些现实工作负载的特征之后，我们进一步研究是否可以使用现有的基准测试来建模和生成接近这些现实工作负载的KV工作负载。我们不在当前模型中的考虑删除。</p>
<h2 id="现有的基准测试有多好？"><a href="#现有的基准测试有多好？" class="headerlink" title="现有的基准测试有多好？"></a>现有的基准测试有多好？</h2><p>一些研究使用YCSB/db_bench + LevelDB/RocksDB来基准测试KV存储的存储性能。研究人员通常认为YCSB产生的工作量接近于实际工作量。对于实际的工作负载，YCSB可以针对给定的查询类型比率，KV对热度分布和值大小分布生成具有相似统计信息的查询。但是，尚不清楚它们在实际工作负载中生成的工作负载是否与基础存储系统的I/O相匹配。</p>
<p>为了对此进行调查，我们集中于存储I/O统计信息，例如由RocksDB中的perf_stat和io_stat收集的块读取，块缓存命中，读取字节和写入字节。为了排除可能影响存储I/O的其他因素，我们重放跟踪并在干净的服务器中收集统计信息。基准测试也在同一服务器中进行评估，以确保设置相同。为确保重放期间生成的RocksDB存储I/O与生产环境中的I/O相同，我们在收集跟踪的同一RocksDB的快照中重放跟踪。快照是在我们开始跟踪时创建的。YCSB是NoSQL应用程序的基准测试，而ZippyDB是典型的分布式KV存储。因此，预期YCSB生成的工作量接近ZippyDB的工作量，我们以ZippyDB为例进行调查。由于特殊的插件要求以及UDB和UP2X的工作量复杂性，我们没有分析这两个用例的存储统计信息。</p>
<p>分析结果表明用YCSB的基准测试结果来指导生产可能会产生一些误导的结果。例如，RocksDB在生产工作负载下的读取性能将高于我们使用YCSB测试的性能。由于其极高的读取放大，其工作负载很容易达到存储带宽的限制。而且，从他们的基准测试结果估计的写放大低于实际生产。写性能可能被高估，还可能导致不正确的SSD生存期估计。</p>
<p>通过详细的分析，我们发现导致这种严重的读放大和较少的存储写入的主要因素是忽视了键空间局部性。当RocksDB遇到高速缓存未命中时，它会从存储到内存读取数据块（例如16 KB）而不是KV对。在YCSB中，即使整个KV对的热度遵循实际工作负载的分布，实际上，这些热KV对实际上是随机分布在整个键空间中。对这些热KV对的查询使大量数据块变热。由于缓存空间的限制，包含请求的KV对的大量热数据块将不会被缓存，这会触发大量的块读取。相比之下，在ZippyDB中，热KV对仅出现在一些键范围，因此热数据块的数量要少得多。类似地，热KV对的随机分布会导致更多更新的KV对在Compaction期间被垃圾回收到较新的级别。因此，正在更新的冷KV对的旧版本在较新的级别中被更早地删除，这导致在压缩较旧的级别时写入较少。相反，如果只有一些关键字范围频繁更新，则冷KV对的旧版本将不断压缩到较旧的级别，直到它们在Compaction期间与其更新。这会导致在Compaction期间写入更多数据。</p>
<h2 id="基于键范围的建模"><a href="#基于键范围的建模" class="headerlink" title="基于键范围的建模"></a>基于键范围的建模</h2><p>与YCSB生成的工作负载不同，现实世界的工作负载根据第6节中介绍的工作负载特征表现出很强的键空间局部性，热KV对通常集中在几个键范围内。因此，为了更好地模拟真实的工作负载，我们提出了一种基于键范围的模型。整个键空间被划分成几个较小的键范围。我们不再仅仅基于整个键空间统计数据对KV对访问进行建模，而是关注这些键范围的热度。</p>
<p>如何确定键范围的大小（键范围内KV对的数量）是基于键范围建模的主要挑战。如果键范围非常大，则热KV对仍会分散在很大的范围内。对这些KV对的访问仍可能触发大量数据块读取。如果键范围非常小，则热KV对实际上位于不同的键范围内，这会倒退成与不考虑键范围的模型有相同的限制。根据我们的调查，当键范围大小接近SST文件中KV对的平均数目时，它可以保留数据块级别和SST级别的局部性。因此，我们使用每个SST文件的平均KV对数作为键范围大小。</p>
<p>我们首先将键大小，值大小和QPS的分布适配到不同的数学模型（例如，幂，指数，多项式，Webull，Pareto和正弦），然后选择拟合标准误差（均方根误差）最小的模型。对于ZippyDB收集的工作负载，键大小固定为48或90字节，值大小遵循广义Pareto分布，QPS可以在24小时内以较小的振幅更好地拟合余弦或正弦。</p>
<p>然后，根据KV对访问计数及其在整个键空间中的顺序，计算每个键范围的每个KV对的平均访问，并拟合到分布模型（例如，幂分布）。这样，当生成一个查询时，我们可以计算每个键范围对此查询作出响应的概率。在每个键范围内，我们让KV对访问计数分布遵循整个键空间的分布。这样可以确保整个KV对访问计数的分布满足实际工作负载。另外，我们确保将热KV对紧密分配在一起。热键和冷键范围可以随机分配给整个键空间，因为键范围的位置对工作负载局部性的影响很小。</p>
<p>基于这些模型，我们进一步使用db_bench开发了一个新的基准测试。运行基准测试时，QPS模型控制两个连续查询之间的时间间隔。发出查询时，查询类型由从收集的工作负载中计算出的每种查询类型的概率确定。然后，键大小和值大小由拟合模型中的概率函数确定。接下来，根据每个键范围的访问概率，我们选择一个键范围来响应此查询。最后，根据KV对访问次数的分布，选择此键范围内的一个KV对，并使用其键来构成查询。这样，KV查询将由基准测试生成并遵循预期的统计模型。同时，它更好地保留了键空间的局部性。</p>
<h2 id="比较基准测试结果"><a href="#比较基准测试结果" class="headerlink" title="比较基准测试结果"></a>比较基准测试结果</h2><p>我们将ZippyDB工作负载调整为建议的模型（不包括删除），并建立一个称为Prefix_dist的新基准测试。为了评估基于键范围的建模的有效性，我们还实施了三个具有不同KV对分配的基准测试：1）Prefix_random对键范围的热度进行建模，但在每个键范围内随机分配热和冷KV对； 2）与YCSB相似，All_random遵循KV对访问计数的分布，但在整个键空间中随机分布KV对； 3）All_dist将热键放在整个键空间中，而不是使用随机分布。所有这四个基准测试达到了与ZippyDB相似的压缩率。</p>
<p>与第7.1节中描述的过程类似，我们将YCSB工作负载a和工作负载b配置为尽可能紧密地拟合ZippyDB工作负载。我们使用以下4种不同的请求分布来运行YCSB：1）均匀（YCSB_uniform），2）压缩（YCSB_zipfian），3）热点（YCSB_hotspot）和4）指数（YCSB_exp）。对于8个基准测试，我们使用相同的预加载数据库（具有5000万个随机插入的KV对，其平均键和值大小与真实工作负载的平均键和值大小相同）。RocksDB缓存大小与生产设置具有相同的值。</p>
<p>下图Figure 14比较了这8个基准测试的I/O统计信息。YCSB_zipfian的工作负载的块读取总数和读取字节数至少比原始重放结果高500％。更糟糕的是，其他三个YCSB基准测试结果的块读取数和读取字节数甚至更高，与重放结果相比，达到1000％或更高。相反，Prefix_dist的读取字节数仅高40％，并且最接近原始重放结果。如果我们比较实现的4个基准测试，可以得出结论，通过考虑键空间局部性，Prefix_dist可以更好地模拟存储读取的数量。All_dist和Prefix_random通过以不同的粒度（整个键空间级别与键范围级别）收集热KV对来减少额外的读取次数。请注意，如果YCSB达到类似的压缩率，则RocksDB存储I/O可以降低约35-40％。但是，这仍然比All_dist，Prefix_random和Prefix_dist的存储I/O差得多。</p>
<p>如果应用相同的压缩率，则YCSB的实际写入字节数应小于原始重放的50％。Prefix_dist可实现原始重放的大约60％的写入字节。实际上，与原始重放结果相比，键/值大小和KV对热度之间的不匹配导致写入字节更少。通常，可以通过以下方式进一步改进YCSB：1）添加基于键范围的分布模型作为生成键的选项； 2）提供吞吐量控制以模拟QPS变化； 3）提供键和值大小分布模型；以及4）增加模拟不同值压缩比的能力。</p>
<p><img src="/images/RocksDB16.png" alt="img"></p>
<h2 id="基准测试统计数据的检验"><a href="#基准测试统计数据的检验" class="headerlink" title="基准测试统计数据的检验"></a>基准测试统计数据的检验</h2><p>我们选择UDB中的Assoc工作负载作为另一个示例，以验证我们的基准测试是否可以实现与实际工作负载非常相似的KV查询统计信息。由于在Assoc中90％的键是28个字节，而10％的键是32个字节，我们可以使用这两个固定的键大小。我们发现，广义Pareto分布最适合值的大小和Iterator的扫描长度。在二项幂模型中，键范围的平均KV对访问次数可以更好地拟合，并且KV对访问次数的分布遵循可以拟合为简单幂模型。正如我们在第4.3节中讨论的那样，由于QPS变化具有很强的昼夜模式，因此它更适合于24小时周期的正弦模型。</p>
<p>为了将通过基准测试获得的工作负载统计数据与实际工作负载的统计数据进行比较，我们以不同的工作负载规模运行新的基准测试。我们在基准测试期间收集跟踪并分析跟踪。下图Figure 15显示了UDB Assoc工作负载和生成的工作负载之间的值大小，KV对访问计数以及Iterator扫描长度的QPS变化和PDF（概率密度函数）比较。尽管从我们的基准测试中产生的工作量规模与UDB Assoc的规模不同，但PDF文件显示它们的分布几乎相同。这证明，就这些统计数据而言，生成的综合工作负载与UDB Assoc工作负载非常接近。</p>
<p><img src="/images/RocksDB17.png" alt="img"></p>
<h1 id="总结和未来工作"><a href="#总结和未来工作" class="headerlink" title="总结和未来工作"></a>总结和未来工作</h1><p>在本文中，我们介绍了Facebook上持久性KV存储工作负载的研究。我们首先介绍跟踪，重放，分析和基准测试的方法和工具。键/值大小分布，访问模式，键范围局部性和工作负载变化的发现提供了有助于优化KV存储性能的见解。通过比较以YCSB为基准的RocksDB的存储I/O和跟踪重放的存储I/O，我们发现以YCSB为基准产生的读取次数更多，写入次数更少。为了解决此问题，我们提出了一种基于键范围的模型，以更好地保留键空间的局部性。新的基准测试不仅可以在查询级别上很好地模拟工作负载，而且还可以实现比YCSB更精确的RocksDB存储I/O。</p>
<p>我们已经在最新的RocksDB版本中开源了跟踪，重放，分析和新的基准测试。新的基准测试是db_bench基准测试工具的一部分。我们目前还没有发布跟踪。将来，我们将通过键范围分布进一步改善YCSB工作负载的生成。此外，我们还将收集，分析和建模其他维度的工作负载，例如查询之间的相关性，KV对热度和KV对大小之间的相关性，以及其他统计信息，例如查询延迟和缓存状态。</p>
]]></content>
      <categories>
        <category>Paper</category>
      </categories>
      <tags>
        <tag>存储</tag>
        <tag>RocksDB</tag>
      </tags>
  </entry>
  <entry>
    <title>PriorityMeister - Tail Latency QoS for Shared Networked Storage 论文笔记</title>
    <url>/2020/05/28/SOCC14-PriorityMeister/</url>
    <content><![CDATA[<h1 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h1><p>在云计算环境下，在共享网络存储系统中提供端到端的尾延迟QoS是非常重要的问题。通常情况下，人们会在90%或95%观察尾延迟。但是，越来越多研究机构和公司开始关心99%甚至99.99%的长延迟。本文讨论99.9%和99.99%的尾延迟。</p>
<p>在生产环境中的突发性工作负载下，满足尾延迟SLO是有挑战性的，主要因为下面两个原因：第一，尾延迟很大程度上受排队的影响，而突发性工作负载导致共享潜在基础设施的工作负载的排队；第二，端到端的尾延迟受到请求中所有阶段的影响（比如访问存储，通过网络发送数据），因此队列可能在不同时间、不同阶段构建起来。</p>
<p>之前大部分存储调度的工作都局限在更简单的问题——共享存储带宽；比起延迟QoS，共享带宽是一段时间的平均水平，不会被暂时的排队影响。有一些之前的工作研究延迟QoS，但是主要关注平均延迟；只看平均延迟会掩盖一些会引起掉队的最糟情况。</p>
<p>关于最近的工作，Cake已经考虑了99%比例下的尾延迟QoS，使用基于反馈控制的技术。但是，像Cake这种通过回应的方法不适用于突发的工作负载，因为突发可能会在Cake对其做出反应之前导致大量SLO违规，如下图所示。在处理多个阶段时，满足延迟SLO的困难更加复杂，因为端到端延迟是由所有阶段延迟的总和组成的。</p>
<p><img src="/images/socc14-PM-1.png" alt="pic"></p>
<p>文章介绍了PriorityMeister（PM），这是一种主动式QoS系统，它通过优先级和令牌桶速率限制的组合，在多个阶段实现了端到端尾延迟SLO。 PM通过分析每个阶段的每个工作负载的突发性和负载来工作。反过来，这用于计算每个工作负载的令牌桶速率限制，该限制限制了一个工作负载对共享系统的其他工作负载的影响。 PM中的一个关键思想是在每个阶段为每个工作负载同时使用多个速率限制器。同时使用多个速率限制器可以使我们更好地限制工作负载的突发性。在很好地限制了每个工作负载的突发性之后，我们构建了一个模型来估计最坏情况下的每个工作负载延迟。我们的模型基于网络演算，这是用于最坏情况排队估计的分析框架。</p>
<p>仅速率限制是不够的，因为工作负载具有不同的延迟要求和不同的工作负载突发性。因此，需要对工作负载进行不同的处理，以满足其延迟SLO，并限制对其他工作负载的影响。 PM使用优先级作为区分工作负载之间的延迟的关键机制。请注意，优先级用于避免对来自具有严格延迟要求的工作负载请求的延迟，而不是根据外部重要性概念对工作负载进行优先级排序。手动设置优先级是很典型的，但是很费力并且容易出错。确实，很难同时捕获每个工作负载的突发性对优先级较低的工作负载的影响。我们的延迟分析模型使PM可以在每个阶段快速搜索较大范围的优先顺序，以自动设置优先级以满足SLO。</p>
<p>PM在每个阶段还支持不同的每个工作负载优先级和速率限制（与整个过程中的单个优先级相对）。 在第一个工作负载满足其SLO而第二个工作负载不满足的情况下，我们不必让一个工作负载始终具有最高的优先级，而第二个工作负载则具有较低的优先级，我们可以在某些阶段将两个工作负载都设置为最高优先级， 别人的优先级较低。 由于工作负载可能不需要在所有地方都满足其SLO的最高优先级，因此这种混合优先级方案可能会允许更多工作负载满足其SLO。</p>
<p>PM做出以下主要贡献。首先，我们开发了一种算法，该算法可以自动确定每个阶段每个工作负载的优先级和速率限制，以满足端到端延迟SLO。 PM通过将网络演算与针对给定工作负载同时使用多个速率限制器的思想相结合来实现这些目标。其次，我们构建了一个由网络和存储组成的真实QoS系统，在这里我们证明PM优于Cake的最新方法。第三，我们显示PM可以抵抗错误的存储性能估计，不同程度的工作负载突发性和工作负载不当行为。第四，我们提出了一种简单的方法作为PM的起点，我们将其称为bySLO，我们发现它的表现出奇的好，也胜过Cake。</p>
<h1 id="Previous-Work"><a href="#Previous-Work" class="headerlink" title="Previous Work"></a>Previous Work</h1><p>PM与先前的工作在两个主要方面不同。首先，它是专门为满足多租户存储环境中的尾延迟SLO而设计的。其次，PM可以推广到包括网络和存储在内的多种资源。下表比较了现有的调度程序和PM。</p>
<p><img src="/images/socc14-PM-2.png" alt="pic"></p>
<h2 id="Tail-latency"><a href="#Tail-latency" class="headerlink" title="Tail latency"></a>Tail latency</h2><p>先前大多数存储调度的工作关注更简单的问题——共享存储的带宽。而关注延迟的工作大多数是以平均延迟为目标。我们仅知道两个存储调度程序 Cake 和 Avatar，它们研究尾延迟行为。</p>
<p>Cake是一种反应式反馈控制调度程序，可调整比例份额以满足99百分位延迟SLO。我们的目标相似，但是我们采用了不同的方法并克服了Cake的某些局限性。Cake仅处理一种对延迟敏感的工作负载，而一种处理面向吞吐量的工作负载。PM可以处理多个延迟和吞吐量SLO，并且可以自动调整它所有系统参数。此外，PM可以处理在生产级存储跟踪中发现的突发性，并且可以满足更高的百分位数延迟SLO（例如99.9％），这两种都无法使用反应式方法来实现。</p>
<p>Cake虽然为HBase（CPU）和HDFS（存储）分配了多种资源，但它需要一种机制来动态调整网络不容易获得的比例份额。 相反，PM使用优先级，这是一种更为简单的机制，并且在许多网络交换机中都得到了支持。 我们尝试扩展Cake以将网络速率限制用作比例份额的代理，但事实证明，这样做带来的伤害大于帮助。</p>
<p>Avatar 是最早截止日期优先（EDF）调度程序，具有速率限制支持。 尽管Avatar显示了尾延迟性能，但仅评估了第95个百分位（模拟）。我们的工作重点是较高的尾延迟（例如99.9％），并且我们会在实际硬件上进行评估。 Avatar发现速率限制对于提供性能隔离很重要，但是它没有解决如何设置速率限制，并且其速率限制模型无法针对突发性变化的工作负载进行配置。 PM分析工作负载跟踪以自动配置速率限制，并且可以处理突发性不同的工作负载。 最后，Avatar的重点仅在于存储，并且该解决方案不能推广到网络，因为EDF依赖于拥有一个可以对请求进行时间戳和排序的实体。</p>
<h2 id="Multi-resource"><a href="#Multi-resource" class="headerlink" title="Multi-resource"></a>Multi-resource</h2><p>最近的一些论文已经开始研究多资源调度的挑战。跨多个资源提供QoS与端到端延迟SLO非常相关，因为延迟是在所有资源阶段（例如存储，CPU，网络等）累积的。可以想象使用两种不同的QoS系统进行存储和网络访问，但是如何根据给定的总端到端SLO确定每个阶段的SLO并不明显。 PM是一个单QoS系统，它既了解存储又了解网络，并且可以自动配置系统以满足端到端延迟SLO。我们的多资源QoS体系结构与IOFlow最相似。 IOFlow为存储和网络QoS引入了一种新的软件定义的存储体系结构，但是没有解决如何配置系统以满足延迟SLO的问题。我们的工作是对IOFlow的补充，可以被认为是可以建立在IOFlow架构之上的政策。</p>
<h2 id="Other-related-work"><a href="#Other-related-work" class="headerlink" title="Other related work"></a>Other related work</h2><p>Bobtail 的论文还研究了云中尾延迟的问题，并且管理员发现了不良的CPU协同调度的根本原因。 我们的工作是对他们的补充，我们的工作有可能在将来合并CPU QoS。 HULL 通过速率限制和将队列转移到终端主机来解决长网络交换队列产生的延迟问题。 Xu 也解决了这个问题，但是使用网络优先级来解决。 这两篇论文都允许低带宽工作负载快速通过网络交换机，但是没有涉及如何使用不同的端到端延迟SLO处理更高带宽的工作负载。 PM 利用网络演算领域来对工作负载进行建模，并使用诸如到达曲线和服务曲线等概念。 我们的延迟分析类似于Bouillard等人的最新理论论文。</p>
<h1 id="Architecture"><a href="#Architecture" class="headerlink" title="Architecture"></a>Architecture</h1><p>PM基于每个工作负载提供QoS。每个负载运行在一台client VM上，访问在server VM上的存储。多个client VM的同一应用程序可以表示为具有相同SLO的多个工作负载。</p>
<p>工作负载由从client到server再返回的请求流组成，其中每个请求都以到达时间，请求大小和请求偏移量为特征。一个请求包含三个阶段：从client到server的网络请求、server访问存储、从server到client的网络应答。对于每个网络阶段，在每台机器和网络交换机出口端口都有队列。对于每个存储阶段每个存储设备都有一个队列。每个阶段都有独立的优先级和速率限制，它们由PM决定。</p>
<h2 id="PriorityMeister-system-design"><a href="#PriorityMeister-system-design" class="headerlink" title="PriorityMeister system design"></a>PriorityMeister system design</h2><p><img src="/images/socc14-PM-3.png" alt="pic"></p>
<p>上图展示了PM系统的QoS组件，PM在每个客户端和服务器上都有本地QoS实施模块，以控制对存储和网络资源的访问。 每个实施模块都是独立的，只需要对从我们的全局控制器接收到的QoS参数采取行动即可。 这使PM可以利用全局信息。 我们的系统架构与IOFlow中的架构相似，已证明可以扩展并容忍控制器故障。</p>
<p>PM自动为每个实施模块配置的两个主要QoS参数，优先级和速率限制。优先级是在工作负载之间提供延迟区分的关键机制。文章使用严格的优先级来为需要低延迟的工作负载提供良好的延迟。为了防止饿死，我们使用速率限制，并且只在工作负载的速率限制内执行优先级。PM的不同寻常之处在于，它在每个阶段为每个工作负载都部署了速率限制器。</p>
<p>速率限制器建立在泄漏令牌桶模型上，该模型以速率和令牌桶大小为参数。当请求到达时，会根据请求将令牌添加到令牌桶。 对于网络，添加的令牌数与请求的传输字节数相对应。 对于存储，添加的令牌数量对应于存储设备处理请求的估计时间（该估计由存储估计器执行）。如果存储桶中有足够的空间添加令牌，则允许请求继续。 否则，请求将使请求排队，直到令牌桶有足够的空间。随着令牌以配置的速率不断从存储桶中泄漏，空间变得可用。</p>
<h2 id="PriorityMeister-controller-design"><a href="#PriorityMeister-controller-design" class="headerlink" title="PriorityMeister controller design"></a>PriorityMeister controller design</h2><p><img src="/images/socc14-PM-4.png" alt="pic"></p>
<p>上图展示了PM控制器的设置。用户提供他们的目标，可以是延迟SLO或者吞吐量SLO，应用程序访问模式的代表性跟踪（trace可以在应用运行时被自动捕获，或者高级用户可以使用特定于应用程序的知识来选择一组更具代表性的访问模式），还有client和数据的位置（IP地址）。延迟SLO指定了请求的最大可接受延迟的值；吞吐量SLO是指定了给定追踪的的请求应该完成的总时间。</p>
<p><img src="/images/socc14-PM-5.png" alt="pic"></p>
<p>关于PM设置速率限制，我们从假想实验开始。假设优先级比较高的工作负载W，图(a) 的速率限制对（速率和令牌桶大小）都能保证W满足SLO，但是不同的选择会影响优先级较低的工作负载，图(b) 表示使用更大的令牌桶的size(例如，绿色的X)会在W出现突发性请求时导致中等优先级的工作更高的尾部延迟。图(C) 表示使用更高的速率(例如，红色的X)会在W出现突发性请求时导致低优先级工作负载饥饿。</p>
<p>在W上使用较小的存储桶大小和较低的速率将有助于降低优先级的工作负载，但是图(a)中的W不存在这样的（r，b）对。PM中的关键思想是在图(a) 的阴影区域中将W限制为所有（r，b）对，这给我们带来了小桶尺寸和低速率的好处。 具体来说，我们沿边界挑选了多个（r，b）对，例如所示的叉叉X，并同时为W部署了这些速率限制器；即在令牌被添加到其所有令牌桶中之前，请求都会排队等待。</p>
<p>PM从工作负载分析器开始，它通过生成工作负载的（r，b）对区域（例如，上图(a) ）来表征每个工作负载。接下来，优先级算法会在优先级排序的空间中进行搜索，以确定最能满足工作负载SLO的工作负载优先级。我们的算法建立在延迟分析模型的基础上，该模型以优先级顺序和速率限制（r，b）对的集合作为输入，并基于网络演算为每个工作负载输出延迟的最坏情况界限。优先级算法不会搜索所有优先级顺序，取而代之的是，开发了一种简单的贪心算法来挑选“好的”顺序，这替代了系统管理员手动设置优先级的方式。</p>
<p>最后，将优先级和速率限制发送到执行模块。由于我们主动分析工作负载突发，因此在用户需求发生变化（例如，更改应用程序访问模式或SLO）或者新的工作负载到来之前，无需再次运行优先级算法。发生更改时，我们将重新运行优先级算法，并可能重新调整优先级和速率限制。</p>
<h1 id="Implementation"><a href="#Implementation" class="headerlink" title="Implementation"></a>Implementation</h1><h2 id="Workload-analysis"><a href="#Workload-analysis" class="headerlink" title="Workload analysis"></a>Workload analysis</h2><p>工作负载分析的作用是基于该工作负载访问模式的代表性追踪为每个工作负载生成速率限制，保证一个工作负载不会因速率限制而被阻碍。也就是说，我们沿着上图(a) 的蓝色边界计算（r，b）对的集合。要计算（r，b）对，我们选择一个速率r，并使用具有该速率的无限制令牌桶来重播工作负载追踪。我们将存储桶大小b设置为最大令牌存储桶使用量，该值对应于使工作负载不受阻碍的最小令牌存储桶大小。我们在一系列速率上执行此操作，以获得整个（r，b）对的集合。</p>
<h2 id="Estimator"><a href="#Estimator" class="headerlink" title="Estimator"></a>Estimator</h2><p>评估器用作工作负载分析的一部分，并在存储执行器中决定一个请求对应的token数量。它们提供了一种将各种请求类型（例如，读，写）和请求大小抽象成一个单个公共量度的方法。PM目前支持两种类型的评估器：存储和网络。对于存储，我们使用“工作”为通用指标，以时间为单位进行度量。“工作”意味着在没有排队影响下请求所花费的时间。对于网络，我们使用被传输的字节作为通用量度，该量度在不同的链路带宽下不会改变。</p>
<h3 id="Storage-estimator"><a href="#Storage-estimator" class="headerlink" title="Storage estimator"></a>Storage estimator</h3><p>我们的存储估计器类似于基于表的方法。 我们对存储设备进行了先验配置，以便为（i）带宽和（ii）服务请求的基本时间构建表。 我们的表格通过请求类型（例如，读取，写入），请求偏移量以及后续请求偏移量之间的距离（即，偏移量-先前的偏移量）进行参数化。 此外，我们保留捕获连续访问请求的历史记录，并假设连续访问的基准时间为0。然后我们评估服务请求的存储时间为 request size / bandwidth + base time.</p>
<h3 id="Network-estimator"><a href="#Network-estimator" class="headerlink" title="Network estimator"></a>Network estimator</h3><p>网络估计器负责估计请求发送的字节数。 我们根据请求类型和请求大小使用一个简单的估算器，发现它足够了。 对于读取请求，有一个小的请求发送到服务器，而有一个很大的响应从服务器返回。 对于写请求，有一个很大的请求发送到服务器，而有一个很小的响应从服务器返回。</p>
<h2 id="Prioritizer-algorithm"><a href="#Prioritizer-algorithm" class="headerlink" title="Prioritizer algorithm"></a>Prioritizer algorithm</h2><p>优先级排序算法负责查找可以满足工作负载SLO的优先级排序。也就是说，我们要确定每个工作负载的每个阶段的优先级，以使通过延迟分析模型计算出的工作负载的最坏情况下的延迟小于工作负载的SLO。虽然搜索空间的大小在工作负载数量上是组合显示的，但我们有一个关键的见解可以使搜索变成多项式时间：如果工作负载可以满足给定的低优先级的SLO，则高优先级工作负载的特定顺序就无关紧要。仅优先级较高的工作负载的累积影响很重要。因此，我们的算法尝试为每个工作负载分配最低的优先级，并且可以将满足最低要求的SLO的任何工作负载分配给该优先级，并将其从搜索中删除。然后，我们的算法以下一个最低优先级迭代其余工作负载。如果到了剩下的工作负载都无法满足最低优先级的SLO的地步，那么我们将利用在每个阶段为工作负载分配不同的优先级的优势。</p>
<h2 id="Storage-enforcer"><a href="#Storage-enforcer" class="headerlink" title="Storage enforcer"></a>Storage enforcer</h2><p>存储的执行会为每个存储设备调度请求。PM当前的实现是以NFS挂载的方式公开存储，而存储执行器则作为NFS之上的插入层而构建。由于NFS基于SunRPC，因此PM能够在RPC层挂接到NFS，而无需对内核进行修改。 存储执行器为每个工作负载创建队列，并根据PM全局控制器分配的优先级和速率限制在不同工作负载之间执行仲裁。</p>
<h2 id="Network-enforcer"><a href="#Network-enforcer" class="headerlink" title="Network enforcer"></a>Network enforcer</h2><p>网络的执行对来自每个工作负载的网络流量进行优先级排序和速率限制。网络执行器构建在现有的Linux Traffic Control（TC）基础结构之上。TC基础结构允许用户构建用于网络的任意QoS排队结构。 由于分层令牌桶（HTB）队列只能支持两个速率限制器，因此我们将多个HTB队列链接在一起，以表示每个工作负载的多个速率限制器。 然后，我们使用DSMARK标记数据包为DSCP标志，这些标志指示网络交换机如何确定数据包的优先级。 我们的网络交换机为每个端口提供7级优先级，使用这些优先级仅需要在交换机上启用DSCP。 为了在主机和交换机上获得优先级，我们最后添加了PRIO队列。 为了通过正确的TC队列识别和路由数据包，我们使用基于源和目标地址的过滤，每个虚拟机都不同。</p>
]]></content>
      <categories>
        <category>Paper</category>
      </categories>
      <tags>
        <tag>网络</tag>
        <tag>存储</tag>
        <tag>调度</tag>
      </tags>
  </entry>
  <entry>
    <title>GIFT - A Coupon Based Throttle-and-Reward Mechanism for Fair and Efficient I/O Bandwidth Management on Parallel Storage Systems 论文笔记</title>
    <url>/2020/04/24/FAST20-GIFT/</url>
    <content><![CDATA[<h1 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h1><p>在并行高性能计算应用中，多个应用带来IO竞争，导致应用之间带宽的不公平分配。GIFT是一个原则上的动态方案，为竞争的应用获得公平性，改善效率。</p>
<p>已经有很多研究从IO吞吐量和公平性角度去缓解IO竞争。但是确保公平性和最大化吞吐量是相互冲突的，在IO竞争下达到平衡很有挑战性。</p>
<p>在并行高性能计算应用中，IO竞争的副作用被同步IO进度增强了。高性能计算应用本身与同步密不可分；在IO阶段，并行计算的MPI进程会等待所有的进程完成IO时，才能继续执行计算任务，这就是同步IO进度。</p>
<p>一个高性能计算应用的MPI进程执行并行IO访问多个后端存储设备。后端存储设备被并发运行程序共享，并且随着时间的推移具有不同程度的共享，因此竞争水平也有所不同。不同级别的IO竞争使不同的MPI进程以不同的速率推进，因此导致非同步的IO进度。而非同步IO进度是共享并行存储系统中效率低下的关键原因，（1）计算节点上的计算周期浪费，以及（2）有效系统IO带宽（即，有助于同步IO进程的带宽）的减少。</p>
<p>由于现有方法无法系统地实现同步IO进度以平衡竞争目标——提高有效的系统IO带宽并提高公平性。所以文章介绍GIFT，这是一种基于优惠券的带宽分配方法，可确保HPC应用程序的同步IO进度，同时最大程度地提高IO带宽利用率并确保并行存储系统上并行应用程序之间的公平性。</p>
<h2 id="GIFT方法总结"><a href="#GIFT方法总结" class="headerlink" title="GIFT方法总结"></a>GIFT方法总结</h2><p>（1）放宽公平性窗口：GIFT不使用传统的对每个IO请求的实时公平性，而是确保在多个IO阶段和应用程序运行中保持公平。这是由于观察到高性能计算应用程序在运行过程中具有多个IO阶段并且具有高度重复性，并且在整个运行过程中通常表现出相似的行为。</p>
<p>（2）IO带宽分配的节流和补偿方法：GIFT会时不时地节流某些应用程序的IO带宽，使对IO同步进度无贡献的IO带宽浪费最小化，以尝试改善整体有效的系统IO带宽。此外，放宽公平性窗口使GIFT可以在以后补偿“受限制”的应用程序，以确保公平性。</p>
<p>首先，GIFT以公平的方式为所有竞争的应用程序分配IO带宽，并确保始终在同一应用程序的所有进程之间实现同步IO进度，这一基本设计原理消除了并行存储系统效率低下的关键根源（第3.1节）。然后，GIFT通过识别和限制某些应用程序的IO带宽份额并扩展其他应用程序的IO带宽份额来减少带宽浪费（第3.2节）。为了最大程度地减少IO带宽浪费，GIFT使用基于约束的线性规划来为应用程序最优地分配带宽（第3.4节）。GIFT向受限制的应用程序发放“优惠券”，这些优惠券的价值与节流程度成正比。稍后，GIFT将“以前发行的优惠券”“赎回”到受限制的应用程序中以确保公平（第3.3节）。</p>
]]></content>
      <categories>
        <category>Paper</category>
      </categories>
      <tags>
        <tag>存储</tag>
        <tag>调度</tag>
        <tag>并行</tag>
      </tags>
  </entry>
  <entry>
    <title>ReFlex - Remote Flash ≈ Local Flash 论文笔记</title>
    <url>/2020/04/21/ASPLOS17-ReFlex/</url>
    <content><![CDATA[<h1 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h1><p>对NVMe Flash的远程访问实现了数据中心内Flash容量以及IOPS的灵活扩展和高利用率。但是，现有的用于远程Flash访问的系统会带来巨大的性能开销，或者无法隔离共享每个Flash设备的多个远程clients。</p>
<p>在实现对Flash的远程访问方面存在重大挑战。要实现低延迟，需要在server和client的网络和存储层上将处理开销降至最低。除了低延迟之外，每台服务器还必须以最低成本实现高吞吐量，从而使一个或多个NVMe Flash设备且具有少量CPU cores的机器达到饱和。此外，要管理共享一个Flash设备的多个租户之间的干扰以及Flash设备的不均匀读写行为，需要一种隔离机制，以保证所有租户的可预测性能。最后，在共享程度，部署规模和用于远程连接的网络协议方面需要具有灵活性。现有的仅软件用于远程Flash访问的选项（例如iSCSI或基于事件的服务器）无法达到性能预期。最近提出的硬件加速选件，例如基于RDMA架构的NVMe，缺乏性能隔离，并且部署灵活性有限。</p>
<p>我们提出了ReFlex，一个用于远程Flash访问的软件系统，其性能与访问本地Flash几乎相同。ReFlex使用数据平面内核紧密集成网络和存储处理，以在低资源需求下实现低延迟和高吞吐量。数据平面设计避免了中断和数据拷贝的开销，针对局部性进行了优化，并在高吞吐量（IOPS）和低尾部延迟之间取得了平衡。ReFlex包括一个QoS调度程序，该调度程序实现优先级和速率限制，以便为共享设备的多个租户实施针对延迟和吞吐量的服务水平目标（SLO）。ReFlex提供了用户态库和远程块设备驱动程序来支持client应用程序。</p>
<p>ReFlex服务器可以支持数千个远程租户。它的QoS调度程序可以通过SLO强制执行租户的尾部延迟和吞吐量要求，同时允许尽力而为的租户消耗NVMe设备的所有剩余吞吐量。</p>
<p>ReFlex是开源软件。该代码位于 <a href="https://github.com/stanford-mast/reflex。" target="_blank" rel="noopener">https://github.com/stanford-mast/reflex。</a></p>
<h1 id="Background-and-Motivation"><a href="#Background-and-Motivation" class="headerlink" title="Background and Motivation"></a>Background and Motivation</h1><p>远程访问提供了使用Flash的灵活性，而不管其在数据中心中的物理位置如何，从而提高了利用率并降低了总拥有成本。</p>
<p>远程访问硬盘在数据中心中已经很普遍，因为它们的高延迟和低吞吐量很容易掩盖网络开销（网络开销不是很明显）。各种软件系统可以使远程磁盘可用作块设备（例如iSCSI），网络文件系统（例如NFS），分布式文件系统（例如Google File System）或分布式数据存储（例如Bigtable）。还提出了使用远程直接存储器访问（RDMA）（例如NVMe over Fabrics）或PCIe互连进行硬件加速访问远程Flash的建议。现有的远程Flash访问方法面临两个主要挑战：以低成本实现高性能，以及在存在干扰的情况下提供可预测的性能。</p>
<h2 id="Interference-Management"><a href="#Interference-Management" class="headerlink" title="Interference Management"></a>Interference Management</h2><p><img src="/images/asplos17-ReFlex-1.png" alt="pic"></p>
<p>如果远程Flash即使多个租户共享一个设备也能提供可预测的性能，则很有用。由于读/写干扰的影响，可预测的性能对于NVMe Flash设备是一个挑战。图1绘制了Flash上的尾部读取延迟（第95个百分位数）与各种读写比率的工作负载的吞吐量（IOPS）的关系。尾部读取延迟取决于吞吐量（负载）和读写比率。对于我们测试过的所有NVMe Flash设备，此行为都是典型的，因为写入操作速度较慢，并且触发磨损平衡和垃圾回收活动，这些活动无法始终被隐藏。当单个应用程序使用本地Flash设备时，可以管理读/写干扰，但是对于远程Flash和共享同一设备但彼此不知道的多个租户而言，这成为一个巨大的挑战。</p>
<h1 id="ReFlex-Design"><a href="#ReFlex-Design" class="headerlink" title="ReFlex Design"></a>ReFlex Design</h1><p>ReFlex使用紧密集成了网络和存储层的数据平面架构，提供了对远程Flash的低延迟和高吞吐量访问。它通过TCP和UDP等通用网络协议为任意大小的逻辑块提供远程读/写请求。ReFlex主要是软件系统，它利用NIC和NVMe Flash设备中的硬件虚拟化功能直接在硬件队列上运行，并有效地在NIC和Flash设备之间转发请求和数据，而无需拷贝。它的基于轮询的执行模型允许在不中断的情况下处理请求，从而提高了本地性并减少了不可预测性。ReFlex使用新颖的IO调度程序来保证具有不同读写请求比率的租户的延迟和吞吐量SLO。ReFlex可以为成千上万个租户和网络连接提供服务，使用所需的尽可能多的cores来使Flash设备IOPS饱和。</p>
<h2 id="Dataplane-Execution-Model"><a href="#Dataplane-Execution-Model" class="headerlink" title="Dataplane Execution Model"></a>Dataplane Execution Model</h2><p><img src="/images/asplos17-ReFlex-2.png" alt="pic"></p>
<p>每个ReFlex服务器线程使用专用core，可以直接和排它地访问网络队列对以进行数据包的接收/发送，并使用NVMe队列对进行Flash命令的提交/完成。</p>
<p>图2展示了ReFlex服务器线程的执行模型，该线程处理传入的Flash读/写请求。首先，NIC接收网络数据包，然后通过DMA将其传送到网络栈提供的预分配的内存缓冲区（1）。ReFlex线程轮询接收描述符环，并通过以太网驱动程序和网络栈（例如TCP/IP）处理数据包，从而生成事件条件，指示新消息的可用性（2）。同一线程使用libix（类似于Linux libevent的库）来处理事件。这涉及切换到服务器代码，以解析消息，提取IO请求，执行访问控制检查以及提交Flash read/write系统调用之前所需的任何其他存储协议的处理（3）。然后，线程切换到系统调用处理并执行IO调度，以在共享ReFlex服务器的所有租户之间实施SLO。调度之后，请求将通过NVMe提交队列提交给Flash设备（4）。Flash设备执行读/写IO，并通过DMA将数据传送到预分配的用户空间缓冲区（或从预分配的用户空间缓冲区获取数据）（7）。线程轮询完成队列（5），并提供完成事件（6）。事件回调通过libix执行并发出send系统调用（7）。最后，线程处理send系统调用，以通过网络栈将请求的数据传递回发起方（8）。执行模型支持每条网络消息多个IO请求以及跨多个网络消息的大型IO。</p>
<h2 id="QoS-Scheduling-and-Isolation"><a href="#QoS-Scheduling-and-Isolation" class="headerlink" title="QoS Scheduling and Isolation"></a>QoS Scheduling and Isolation</h2><p>QoS调度程序允许ReFlex为共享服务器中Flash设备的租户提供性能保证。租户是一种逻辑抽象，用于说明和执行服务级别目标（SLO）。SLO在特定吞吐量和读/写比率下指定尾部读取延迟的限制。例如，租户可以以80％的读取比率注册具有200us读取尾部延迟（95％百分数）的50K IOPS的SLO。除了此类延迟关键（LC）租户，这些租户在尾部延迟和吞吐量方面保证了分配，ReFlex还为尽力而为（BE）租户提供服务，这些租户可以机会使用任何未分配或未使用的Flash带宽并容忍较高的延迟。租户定义可以由成千上万的网络连接共享，这些连接来自运行任何应用程序的不同客户端计算机。应用程序可以使用多个租户为不同的数据流请求单独的SLO。</p>
<p>如图1所示，在Flash设备访问上强制执行SLO有两个因素。首先，设备可以支持的最大带宽（IOPS）取决于它在所有租户中看到的请求的总体读写比率。其次，读取请求的尾部等待时间取决于总体读取/写入比率和当前带宽负载。因此，QoS调度程序需要全局可见性和对Flash上的总负载以及未完成的IO操作类型的控制。我们使用请求成本模型来说明每个Flash IO对读取尾部延迟的影响，并使用一种新颖的调度算法来保证所有租户和所有数据平面线程之间的SLO。</p>
<h3 id="Request-Cost-Model"><a href="#Request-Cost-Model" class="headerlink" title="Request Cost Model"></a>Request Cost Model</h3><p>该模型估计读取尾部延迟是加权IOPS的函数，其中请求的成本（权重）取决于I/O size，I/O type（读取与写入）以及当前设备上的读/写请求比率r：</p>
<p><img src="/images/asplos17-ReFlex-3.png" alt="pic"></p>
<p>成本是r的函数，因为相对于读负载为99％或更低的情况，某些闪存设备对只读负载（r = 100％）提供了更高的IOPS，如图1所示。因此，该模型调整了设备负载为只读时的读取请求。成本以token的倍数表示，其中一个token代表4KB随机读取请求的成本。在我们使用的所有Flash设备中，对于大于4KB的大小，成本都会随着请求大小线性增长（例如，一个32KB的请求的成本高达8个连续4KB的请求）。对于4KB及以下的请求，成本是恒定的，因为这些Flash设备似乎以4KB的粒度运行。</p>
<p>我们针对ReFlex服务器中部署的每种类型的Flash设备校准成本模型。首先，对于具有各种读写比率和请求大小的工作负载，我们使用本地Flash测量了尾部等待时间与吞吐量的关系（请参见图1中的4KB示例）。由于写入请求的成本取决于垃圾回收和页面擦除事件的频率，因此我们保守地使用随机写入模式来触发最坏的情况。接下来，我们使用曲线拟合来得出C（I / O type，r），尽管非线性曲线拟合模型可以实现更好的拟合，但是为了简单起见，调度程序采用线性模型，因为线性模型的精度足够了。部署后可以重新校准模型，以解决由于闪存磨损而导致的性能下降。</p>
<p>根据不同的Flash设备，写操作往往比读操作多出10-20倍的成本，即一次写操作需要10-20个token，C(write,  r &lt; 100%) = 10 tokens ~ 20 tokens。如图1所示，相比于99%或者更低读写比的负载，某些Flash设备对只读负载 (r = 100％) 提供了更高的IOPS。对于只读负载，模型需要调整读请求的成本，一次读请求只需要其他负载下一半的成本，C(read,  r = 100%) = 1/2 token。</p>
<h3 id="Scheduling-Mechanism"><a href="#Scheduling-Mechanism" class="headerlink" title="Scheduling Mechanism"></a>Scheduling Mechanism</h3><p>QoS调度器构建在成本模型之上，保持延迟关键租户的尾端延迟和吞吐量的SLO，同时允许尽力交付型租户以公平的方式利用剩余的吞吐量。</p>
<h4 id="token管理"><a href="#token管理" class="headerlink" title="token管理"></a>token管理</h4><p>ReFlex调度以等于Flash设备在给定尾端延迟SLO上可以支持的最大加权IOPS（上述的成本模型）的速率生成token。ReFlex在所有共享一个Flash设备的延迟关键租户中执行最严格的延迟SLO。在它们的SLO指示的读写比加权情况下，延迟关键租户被提供能够满足它们IOPS SLO的token供应。由调度程序生成但未分配给延迟关键的token将在尽力交付型租户之间公平分配。当调度程序将租户的请求提交到Flash设备时，它会根据每个请求的成本来花费租户的token。</p>
<h4 id="调度算法"><a href="#调度算法" class="headerlink" title="调度算法"></a>调度算法</h4><p><img src="/images/asplos17-ReFlex-4.png" alt="pic"></p>
<p>每个ReFlex线程将Flash请求排入每个租户的软件队列中。当线程到达数据平面执行模型中的QoS调度步骤时，线程使用算法1计算排队请求的加权成本，并将所有允许的请求提交给Flash设备，从而逐渐花费每个租户的token。根据线程负载和批处理因子，执行模型每0.5us至100us进入一次调度回合。通过对控制平面和批处理大小限制确保调度程序调用之间的时间不超过最严格SLO的5%。必须进行频繁的调度，以避免过多的排队延迟并保持NVMe设备的高利用率。</p>
<p>其次，ReFlex采用自适应批处理请求，以分摊开销并提高预取和指令缓存效率。在低负载下，将立即处理传入数据包或已完成的NVMe命令。随着负载的增加，NIC接收和NVMe完成队列将填满，并为批量处理多个传入数据包或多个完成的访问提供了机会。批大小随负载增加而增加，但上限为64，以避免过多的延迟。不同于传统的批处理，后者需要在带宽和延迟之间进行权衡，自适应批处理在高吞吐量和低延迟之间实现了良好的平衡。</p>
<h4 id="延迟关键（LC）租户"><a href="#延迟关键（LC）租户" class="headerlink" title="延迟关键（LC）租户"></a>延迟关键（LC）租户</h4><p>调度算法首先服务延迟关键租户。首先，调度程序根据每个LC租户的IOPS SLO和自上次调度程序调用以来的经过时间来为每个LC租户生成token。由于控制平面已确定每个LC租户的加权IOPS预留是可接受的，因此调度程序通常可以将所有LC租户的排队请求提交到NVMe设备。但是由于流量很少统一，并且租户发出的IOPS可能比其SLO中保留的平均IOPS少或多，因此调度程序会跟踪每个租户的token使用情况。算法允许LC租户暂时突破其token分配，以避免短期排队，但是一旦租户达到token不足限制（NEG_LIMIT），算法通过限制LC租户的速率来限制突破的大小。NEG_LIMIT参数通常设置为-50个令牌，以限制突破中昂贵的写请求的数量。当达到此限制时，我们还会通知控制平面，以检测具有错误SLO租户，以重新协商。</p>
<p>消耗少于它们被提供的tokens的LC租户可以为以后的突破积累tokens，积累被参数POS_LIMIT限制。到达参数限制时，调度程序将大部分累积的tokens（经验上为90％）捐赠给全局令牌存储区，以供BE租户使用。根据经验，POS LIMIT设置为LC租户在最近三轮调度中收到的令牌数量，以适应短暂的突发事件而不会出现赤字。</p>
<h4 id="尽力交付（BE）用户"><a href="#尽力交付（BE）用户" class="headerlink" title="尽力交付（BE）用户"></a>尽力交付（BE）用户</h4><p>调度程序通过为每个BE租户分配设备上未分配吞吐量的公平份额来生成BE租户的token。未分配的设备吞吐量与设备在执行最严格的LC延迟SLO减去LC租户令牌率之和（基于LC租户IOPS SLO）时可支持的令牌率相对应。假设有N个BE租户，每一轮调度回合，每个BE租户都将收到未分配token率的1/N乘以自上一个调度周期以来所经过的时间。如果BE租户没有足够的token来提交其所有入队的请求，则租户可以从全局token桶中索取tokens，这些tokens有由带有闲置tokens的LC租户提供。BE租户在各个调度回合中以循环顺序进行调度，以提供对全局token存储区的公平访问。仅当租户具有足够的token用于请求时，调度程序才会提交BE请求。</p>
<p>限速BE流量对于实现LC SLO至关重要。由于调度回合以高频率发生，因此典型的回合可能仅生成令牌的一小部分。当BE租户的请求队列不为空时，它们会在多个调度回合中累积令牌。当BE租户的软件队列为空时，我们禁止令牌累积来防止在空闲时间后突发。调度程序的这一方面是受赤字循环调度调度的启发。BE租户未使用的令牌将捐赠给全局令牌存储区，以供其他BE租户使用。为了避免大量积累，使BE租户能够发出不受控制的突发事件，我们会定期重置存储桶。</p>
]]></content>
      <categories>
        <category>Paper</category>
      </categories>
      <tags>
        <tag>存储</tag>
        <tag>调度</tag>
      </tags>
  </entry>
  <entry>
    <title>C++多线程并发系列（四）</title>
    <url>/2020/04/21/Cpp-thread-concurrency-4th/</url>
    <content><![CDATA[<h1 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h1><p>C++ 是一种系统编程语言，标准委员会的目标之一是，将不需要比 C++ 更底层的语言。C++ 应该为程序员提供足够的灵活性，使他们能够在没有语言妨碍的情况下执行所需的任何操作，从而在需要时允许他们“靠近机器”。<code>原子类型和操作</code>允许这样做，从而为低级同步操作提供了便利，这些操作通常会减少为一到两个CPU指令。</p>
<p>在本章中，我将首先介绍内存模型的基础知识，然后继续介绍原子类型和操作，最后介绍原子类型操作可使用的各种同步类型。这非常复杂：除非您打算编写使用原子操作进行同步的代码（例如无锁数据结构），否则您无需了解这些详细信息。</p>
<h1 id="内存模型"><a href="#内存模型" class="headerlink" title="内存模型"></a>内存模型</h1><p>内存模型有两个方面：基本结构方面（与事物在内存中的布局方式有关）和并发方面。结构方面对于并发非常重要，尤其是在您研究低级原子操作时，因此我将从这些方面开始。在 C++ 中，所有内容都与对象和内存位置有关。</p>
<h2 id="对象与内存位置"><a href="#对象与内存位置" class="headerlink" title="对象与内存位置"></a>对象与内存位置</h2><p>C++ 程序中的所有数据均由对象组成，C++ 标准将对象定义为“存储区域”，尽管它继续为这些对象分配属性，例如它们的类型和生命周期。</p>
<p>这些对象中的一些是基本类型的简单值，例如int或float，而另一些是用户定义类的实例。有些对象（例如数组，派生类的实例以及具有非静态数据成员的类的实例）具有子对象，而其他对象则没有。无论其类型如何，一个对象都存储在一个或多个存储位置中。</p>
<ul>
<li><p>每个变量都是一个对象，包括那些属于其他对象的成员。</p>
</li>
<li><p>每个对象至少占用一个内存位置。</p>
</li>
<li><p>基本类型的变量（例如int或char）无论它们大小如何，都恰好占据一个内存位置，即使它们是相邻的或数组的一部分。</p>
</li>
<li><p>相邻的 bit fields 是同一内存位置的一部分。</p>
</li>
</ul>
<h2 id="对象，内存位置与并发"><a href="#对象，内存位置与并发" class="headerlink" title="对象，内存位置与并发"></a>对象，内存位置与并发</h2><p>C++ 中的多线程应用程序至关重要的部分：一切都取决于这些内存位置。如果有两个线程访问单独的内存位置，那就没问题：一切正常。另一方面，如果两个线程访问相同的内存位置，则必须小心。如果两个线程都没有更新内存位置，那很好。只读数据不需要保护或同步。如果任何一个线程正在修改数据，则存在 race condition（争用情况）的可能性。</p>
<p>为了避免争用情况，线程就要规定执行顺序。一种方式是使用 mutex，后一线程必须等待前一线程解锁。第二种方式是使用原子操作来避免竞争访问同一内存位置而导致的未定义行为。</p>
<p>每个对象从初始化开始都有一个修改顺序，这个顺序由来自所有线程对该对象的写操作组成。通常这个顺序在运行时会变动，但在任何给定的程序执行中，系统中所有线程都必须遵循此顺序。</p>
<p>如果对象不是原子类型，你有责任确保有足够的同步来确保线程同意每个变量的修改顺序。如果不同的线程看到单个变量的值的不同序列，则说明存在数据争用和未定义的行为。使用原子操作就可以把同步的责任抛给编译器。</p>
<h1 id="C-的原子类型与操作"><a href="#C-的原子类型与操作" class="headerlink" title="C++ 的原子类型与操作"></a>C++ 的原子类型与操作</h1><h2 id="标准原子类型"><a href="#标准原子类型" class="headerlink" title="标准原子类型"></a>标准原子类型</h2><p><a href="https://downdemo.gitbook.io/cpp-concurrency-in-action-2ed/4.-c++-nei-cun-mo-xing-he-ji-yu-yuan-zi-lei-xing-de-cao-zuo-the-c++-memory-model-and-operations-on-a/yuan-zi-cao-zuo-he-yuan-zi-lei-xing/biao-zhun-yuan-zi-lei-xing" target="_blank" rel="noopener">标准原子类型</a></p>
<h2 id="std-atomic-flag"><a href="#std-atomic-flag" class="headerlink" title="std::atomic_flag"></a>std::atomic_flag</h2><p><a href="https://downdemo.gitbook.io/cpp-concurrency-in-action-2ed/4.-c++-nei-cun-mo-xing-he-ji-yu-yuan-zi-lei-xing-de-cao-zuo-the-c++-memory-model-and-operations-on-a/yuan-zi-cao-zuo-he-yuan-zi-lei-xing/std-atomic_flag" target="_blank" rel="noopener">std::atomic_flag</a></p>
<h2 id="其他原子类型"><a href="#其他原子类型" class="headerlink" title="其他原子类型"></a>其他原子类型</h2><p><a href="https://downdemo.gitbook.io/cpp-concurrency-in-action-2ed/4.-c++-nei-cun-mo-xing-he-ji-yu-yuan-zi-lei-xing-de-cao-zuo-the-c++-memory-model-and-operations-on-a/yuan-zi-cao-zuo-he-yuan-zi-lei-xing/qi-ta-yuan-zi-lei-xing" target="_blank" rel="noopener">其他原子类型</a></p>
<h1 id="同步操作和强制排序"><a href="#同步操作和强制排序" class="headerlink" title="同步操作和强制排序"></a>同步操作和强制排序</h1><h2 id="synchronizes-with-relationship"><a href="#synchronizes-with-relationship" class="headerlink" title="synchronizes-with relationship"></a>synchronizes-with relationship</h2><p><a href="https://downdemo.gitbook.io/cpp-concurrency-in-action-2ed/4.-c++-nei-cun-mo-xing-he-ji-yu-yuan-zi-lei-xing-de-cao-zuo-the-c++-memory-model-and-operations-on-a/tong-bu-cao-zuo-he-qiang-zhi-pai-xu-enforced-ordering/synchronizes-with" target="_blank" rel="noopener">synchronizes-with</a></p>
<h2 id="happens-before-relationship"><a href="#happens-before-relationship" class="headerlink" title="happens-before relationship"></a>happens-before relationship</h2><p><a href="https://downdemo.gitbook.io/cpp-concurrency-in-action-2ed/4.-c++-nei-cun-mo-xing-he-ji-yu-yuan-zi-lei-xing-de-cao-zuo-the-c++-memory-model-and-operations-on-a/tong-bu-cao-zuo-he-qiang-zhi-pai-xu-enforced-ordering/happens-before" target="_blank" rel="noopener">happens-before</a></p>
<p><a href="https://downdemo.gitbook.io/cpp-concurrency-in-action-2ed/4.-c++-nei-cun-mo-xing-he-ji-yu-yuan-zi-lei-xing-de-cao-zuo-the-c++-memory-model-and-operations-on-a/tong-bu-cao-zuo-he-qiang-zhi-pai-xu-enforced-ordering/inter-thread-happens-before" target="_blank" rel="noopener">inter-thread happens-before</a></p>
<p><a href="https://downdemo.gitbook.io/cpp-concurrency-in-action-2ed/4.-c++-nei-cun-mo-xing-he-ji-yu-yuan-zi-lei-xing-de-cao-zuo-the-c++-memory-model-and-operations-on-a/tong-bu-cao-zuo-he-qiang-zhi-pai-xu-enforced-ordering/strongly-happens-before" target="_blank" rel="noopener">strongly-happens-before</a></p>
<h2 id="Memory-ordering-for-atomic-operations"><a href="#Memory-ordering-for-atomic-operations" class="headerlink" title="Memory ordering for atomic operations"></a>Memory ordering for atomic operations</h2><p><a href="https://downdemo.gitbook.io/cpp-concurrency-in-action-2ed/4.-c++-nei-cun-mo-xing-he-ji-yu-yuan-zi-lei-xing-de-cao-zuo-the-c++-memory-model-and-operations-on-a/tong-bu-cao-zuo-he-qiang-zhi-pai-xu-enforced-ordering/std-memory_order" target="_blank" rel="noopener">std::memory_order</a></p>
<h3 id="Relaxed-ordering"><a href="#Relaxed-ordering" class="headerlink" title="Relaxed ordering"></a>Relaxed ordering</h3><p><a href="https://downdemo.gitbook.io/cpp-concurrency-in-action-2ed/4.-c++-nei-cun-mo-xing-he-ji-yu-yuan-zi-lei-xing-de-cao-zuo-the-c++-memory-model-and-operations-on-a/tong-bu-cao-zuo-he-qiang-zhi-pai-xu-enforced-ordering/std-memory_order/relaxed-ordering" target="_blank" rel="noopener">Relaxed ordering</a></p>
<h3 id="Release-Consume-ordering"><a href="#Release-Consume-ordering" class="headerlink" title="Release-Consume ordering"></a>Release-Consume ordering</h3><p><a href="https://downdemo.gitbook.io/cpp-concurrency-in-action-2ed/4.-c++-nei-cun-mo-xing-he-ji-yu-yuan-zi-lei-xing-de-cao-zuo-the-c++-memory-model-and-operations-on-a/tong-bu-cao-zuo-he-qiang-zhi-pai-xu-enforced-ordering/std-memory_order/release-consume-ordering" target="_blank" rel="noopener">Release-Consume ordering</a></p>
<h3 id="Release-Acquire-ordering"><a href="#Release-Acquire-ordering" class="headerlink" title="Release-Acquire ordering"></a>Release-Acquire ordering</h3><p><a href="https://downdemo.gitbook.io/cpp-concurrency-in-action-2ed/4.-c++-nei-cun-mo-xing-he-ji-yu-yuan-zi-lei-xing-de-cao-zuo-the-c++-memory-model-and-operations-on-a/tong-bu-cao-zuo-he-qiang-zhi-pai-xu-enforced-ordering/std-memory_order/release-acquire-ordering" target="_blank" rel="noopener">Release-Acquire ordering</a></p>
<h3 id="Sequentially-consistent-ordering"><a href="#Sequentially-consistent-ordering" class="headerlink" title="Sequentially-consistent ordering"></a>Sequentially-consistent ordering</h3><p><a href="https://downdemo.gitbook.io/cpp-concurrency-in-action-2ed/4.-c++-nei-cun-mo-xing-he-ji-yu-yuan-zi-lei-xing-de-cao-zuo-the-c++-memory-model-and-operations-on-a/tong-bu-cao-zuo-he-qiang-zhi-pai-xu-enforced-ordering/std-memory_order/sequentially-consistent-ordering" target="_blank" rel="noopener">Sequentially-consistent ordering</a></p>
<h2 id="Fences"><a href="#Fences" class="headerlink" title="Fences"></a>Fences</h2><p><a href="https://downdemo.gitbook.io/cpp-concurrency-in-action-2ed/4.-c++-nei-cun-mo-xing-he-ji-yu-yuan-zi-lei-xing-de-cao-zuo-the-c++-memory-model-and-operations-on-a/tong-bu-cao-zuo-he-qiang-zhi-pai-xu-enforced-ordering/std-atomic_thread_fence" target="_blank" rel="noopener">std::atomic_thread_fence</a></p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>并发控制</tag>
      </tags>
  </entry>
  <entry>
    <title>Beej&#39;s Guide to Network Programming 读书笔记（下）</title>
    <url>/2020/04/18/Beejs-Guide-to-Network-Programming-2nd/</url>
    <content><![CDATA[<h1 id="Client-Server-基础"><a href="#Client-Server-基础" class="headerlink" title="Client-Server 基础"></a>Client-Server 基础</h1><h2 id="一个简单的-Stream-Server"><a href="#一个简单的-Stream-Server" class="headerlink" title="一个简单的 Stream Server"></a>一个简单的 Stream Server</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">** server.c － 展示一个 stream socket server</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;errno.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netdb.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;signal.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PORT <span class="meta-string">" 3490"</span> <span class="comment">// 提供给用戶连接的 port</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BACKLOG 10 <span class="comment">// 有多少个特定的连接队列（pending connections queue）</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">sigchld_handler</span><span class="params">(<span class="keyword">int</span> s)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">while</span>(waitpid(<span class="number">-1</span>, <span class="literal">NULL</span>, WNOHANG) &gt; <span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 取得 sockaddr， IPv4 或 IPv6：</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">get_in_addr</span><span class="params">(struct sockaddr *sa)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (sa-&gt;sa_family == AF_INET) &#123;</span><br><span class="line">        <span class="keyword">return</span> &amp;(((struct sockaddr_in*)sa)-&gt;sin_addr);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> &amp;(((struct sockaddr_in6*)sa)-&gt;sin6_addr);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> sockfd, new_fd; <span class="comment">// 在 sock_fd 进行 listen， new_fd 是新的连接</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> <span class="title">hints</span>, *<span class="title">servinfo</span>, *<span class="title">p</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_storage</span> <span class="title">their_addr</span>;</span> <span class="comment">// 连接者的地址资料</span></span><br><span class="line">    <span class="keyword">socklen_t</span> sin_size;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sigaction</span> <span class="title">sa</span>;</span></span><br><span class="line">    <span class="keyword">int</span> yes=<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">char</span> s[INET6_ADDRSTRLEN];</span><br><span class="line">    <span class="keyword">int</span> rv;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">memset</span>(&amp;hints, <span class="number">0</span>, <span class="keyword">sizeof</span> hints);</span><br><span class="line">    hints.ai_family = AF_UNSPEC;</span><br><span class="line">    hints.ai_socktype = SOCK_STREAM;</span><br><span class="line">    hints.ai_flags = AI_PASSIVE; <span class="comment">// 使用我的 IP</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((rv = getaddrinfo(<span class="literal">NULL</span>, PORT, &amp;hints, &amp;servinfo)) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">" getaddrinfo: %s\n"</span> , gai_strerror(rv));</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 以循环找出全部的结果， 并绑定（bind）到第一个能用的结果</span></span><br><span class="line">    <span class="keyword">for</span>(p = servinfo; p != <span class="literal">NULL</span>; p = p-&gt;ai_next) &#123;</span><br><span class="line">        <span class="keyword">if</span> ((sockfd = socket(p-&gt;ai_family, p-&gt;ai_socktype, p-&gt;ai_protocol)) == <span class="number">-1</span>) &#123;</span><br><span class="line">            perror(<span class="string">" server: socket"</span> );</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (setsockopt(sockfd, SOL_SOCKET, SO_REUSEADDR, &amp;yes, <span class="keyword">sizeof</span>(<span class="keyword">int</span>)) == <span class="number">-1</span>) &#123;</span><br><span class="line">            perror(<span class="string">" setsockopt"</span> );</span><br><span class="line">            <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (bind(sockfd, p-&gt;ai_addr, p-&gt;ai_addrlen) == <span class="number">-1</span>) &#123;</span><br><span class="line">            <span class="built_in">close</span>(sockfd);</span><br><span class="line">            perror(<span class="string">" server: bind"</span> );</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (p == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">" server: failed to bind\n"</span> );</span><br><span class="line">        <span class="keyword">return</span> <span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    freeaddrinfo(servinfo); <span class="comment">// 全部都用这个 structure</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">listen</span>(sockfd, BACKLOG) == <span class="number">-1</span>) &#123;</span><br><span class="line">        perror(<span class="string">" listen"</span> );</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    sa.sa_handler = sigchld_handler; <span class="comment">// 收拾全部死掉的 processes</span></span><br><span class="line">    sigemptyset(&amp;sa.sa_mask);</span><br><span class="line">    sa.sa_flags = SA_RESTART;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (sigaction(SIGCHLD, &amp;sa, <span class="literal">NULL</span>) == <span class="number">-1</span>) &#123;</span><br><span class="line">        perror(<span class="string">" sigaction"</span> );</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">" server: waiting for connections...\n"</span> );</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>) &#123; <span class="comment">// 主要的 accept() 循环</span></span><br><span class="line">        sin_size = <span class="keyword">sizeof</span> their_addr;</span><br><span class="line">        new_fd = accept(sockfd, (struct sockaddr *)&amp;their_addr, &amp;sin_size);</span><br><span class="line">        <span class="keyword">if</span> (new_fd == <span class="number">-1</span>) &#123;</span><br><span class="line">            perror(<span class="string">" accept"</span> );</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        inet_ntop(their_addr.ss_family, get_in_addr((struct sockaddr *)&amp;their_addr), s, <span class="keyword">sizeof</span> s);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">" server: got connection from %s\n"</span> , s);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!fork()) &#123; <span class="comment">// 这个是 child process</span></span><br><span class="line">            <span class="built_in">close</span>(sockfd); <span class="comment">// child 不需要 listener</span></span><br><span class="line">            <span class="keyword">if</span> (send(new_fd, <span class="string">" Hello, world!"</span> , <span class="number">13</span>, <span class="number">0</span>) == <span class="number">-1</span>)</span><br><span class="line">                perror(<span class="string">" send"</span> );</span><br><span class="line">            <span class="built_in">close</span>(new_fd);</span><br><span class="line">            <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">close</span>(new_fd); <span class="comment">// parent 不需要这个</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>sigaction()</code>这个代码是用来清理 zombie processes（僵尸进程），当 parent process 所<code>fork()</code>出来的 child process 结束时，且 parent process 没有取得 child process 的离开状态时，就会出现 zombie process。如果你产生了许多 zombies，但却无法清除他们时， 你的系统管理员就会开始焦虑不安了。</p>
<h2 id="一个简单的-Stream-Client"><a href="#一个简单的-Stream-Client" class="headerlink" title="一个简单的 Stream Client"></a>一个简单的 Stream Client</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">** client.c -- 一个 stream socket client 的 demo</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;errno.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netdb.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PORT <span class="meta-string">" 3490"</span> <span class="comment">// Client 所要连接的 port</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAXDATASIZE 100 <span class="comment">// 我们一次可以收到的最大字节数量（number of bytes）</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 取得 IPv4 或 IPv6 的 sockaddr：</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">get_in_addr</span><span class="params">(struct sockaddr *sa)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (sa-&gt;sa_family == AF_INET) &#123;</span><br><span class="line">        <span class="keyword">return</span> &amp;(((struct sockaddr_in*)sa)-&gt;sin_addr);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> &amp;(((struct sockaddr_in6*)sa)-&gt;sin6_addr);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> sockfd, numbytes;</span><br><span class="line">    <span class="keyword">char</span> buf[MAXDATASIZE];</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> <span class="title">hints</span>, *<span class="title">servinfo</span>, *<span class="title">p</span>;</span></span><br><span class="line">    <span class="keyword">int</span> rv;</span><br><span class="line">    <span class="keyword">char</span> s[INET6_ADDRSTRLEN];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>,<span class="string">" usage: client hostname\n"</span> );</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">memset</span>(&amp;hints, <span class="number">0</span>, <span class="keyword">sizeof</span> hints);</span><br><span class="line">    hints.ai_family = AF_UNSPEC;</span><br><span class="line">    hints.ai_socktype = SOCK_STREAM;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((rv = getaddrinfo(argv[<span class="number">1</span>], PORT, &amp;hints, &amp;servinfo)) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">" getaddrinfo: %s\n"</span> , gai_strerror(rv));</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 用循环取得全部的结果， 并先连接到能成功连接的</span></span><br><span class="line">    <span class="keyword">for</span>(p = servinfo; p != <span class="literal">NULL</span>; p = p-&gt;ai_next) &#123;</span><br><span class="line">        <span class="keyword">if</span> ((sockfd = socket(p-&gt;ai_family, p-&gt;ai_socktype,</span><br><span class="line">            p-&gt;ai_protocol)) == <span class="number">-1</span>) &#123;</span><br><span class="line">            perror(<span class="string">" client: socket"</span> );</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">connect</span>(sockfd, p-&gt;ai_addr, p-&gt;ai_addrlen) == <span class="number">-1</span>) &#123;</span><br><span class="line">            <span class="built_in">close</span>(sockfd);</span><br><span class="line">            perror(<span class="string">" client: connect"</span> );</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (p == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">" client: failed to connect\n"</span> );</span><br><span class="line">        <span class="keyword">return</span> <span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    inet_ntop(p-&gt;ai_family, get_in_addr((struct sockaddr *)p-&gt;ai_addr), s, <span class="keyword">sizeof</span> s);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">" client: connecting to %s\n"</span> , s);</span><br><span class="line"></span><br><span class="line">    freeaddrinfo(servinfo); <span class="comment">// 全部皆以这个 structure 完成</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((numbytes = recv(sockfd, buf, MAXDATASIZE<span class="number">-1</span>, <span class="number">0</span>)) == <span class="number">-1</span>) &#123;</span><br><span class="line">        perror(<span class="string">" recv"</span> );</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    buf[numbytes] = <span class="string">'\0'</span>;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">" client: received '%s'\n"</span> ,buf);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">close</span>(sockfd);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Datagram-Sockets"><a href="#Datagram-Sockets" class="headerlink" title="Datagram Sockets"></a>Datagram Sockets</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">** listener.c -- 一个 datagram sockets " server" 的 demo</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;errno.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netdb.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MYPORT <span class="meta-string">" 4950"</span> <span class="comment">// 用戶所要连线的 port</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAXBUFLEN 100</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// get sockaddr, IPv4 or IPv6:</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">get_in_addr</span><span class="params">(struct sockaddr *sa)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (sa-&gt;sa_family == AF_INET) &#123;</span><br><span class="line">        <span class="keyword">return</span> &amp;(((struct sockaddr_in*)sa)-&gt;sin_addr);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> &amp;(((struct sockaddr_in6*)sa)-&gt;sin6_addr);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> sockfd;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> <span class="title">hints</span>, *<span class="title">servinfo</span>, *<span class="title">p</span>;</span></span><br><span class="line">    <span class="keyword">int</span> rv;</span><br><span class="line">    <span class="keyword">int</span> numbytes;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_storage</span> <span class="title">their_addr</span>;</span></span><br><span class="line">    <span class="keyword">char</span> buf[MAXBUFLEN];</span><br><span class="line">    <span class="keyword">socklen_t</span> addr_len;</span><br><span class="line">    <span class="keyword">char</span> s[INET6_ADDRSTRLEN];</span><br><span class="line"></span><br><span class="line">    <span class="built_in">memset</span>(&amp;hints, <span class="number">0</span>, <span class="keyword">sizeof</span> hints);</span><br><span class="line">    hints.ai_family = AF_UNSPEC; <span class="comment">// 设定 AF_INET 以强制使用 IPv4</span></span><br><span class="line">    hints.ai_socktype = SOCK_DGRAM;</span><br><span class="line">    hints.ai_flags = AI_PASSIVE; <span class="comment">// 使用我的 IP</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((rv = getaddrinfo(<span class="literal">NULL</span>, MYPORT, &amp;hints, &amp;servinfo)) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">" getaddrinfo: %s\n"</span> , gai_strerror(rv));</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 用循环找出全部的结果， 并 bind 到首先找到能 bind 的</span></span><br><span class="line">    <span class="keyword">for</span>(p = servinfo; p != <span class="literal">NULL</span>; p = p-&gt;ai_next) &#123;</span><br><span class="line">        <span class="keyword">if</span> ((sockfd = socket(p-&gt;ai_family, p-&gt;ai_socktype, p-&gt;ai_protocol)) == <span class="number">-1</span>) &#123;</span><br><span class="line">        perror(<span class="string">" listener: socket"</span> );</span><br><span class="line">        <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (bind(sockfd, p-&gt;ai_addr, p-&gt;ai_addrlen) == <span class="number">-1</span>) &#123;</span><br><span class="line">            <span class="built_in">close</span>(sockfd);</span><br><span class="line">            perror(<span class="string">" listener: bind"</span> );</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (p == <span class="literal">NULL</span>) &#123;</span><br><span class="line">    <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">" listener: failed to bind socket\n"</span> );</span><br><span class="line">    <span class="keyword">return</span> <span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    freeaddrinfo(servinfo);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">" listener: waiting to recvfrom...\n"</span> );</span><br><span class="line"></span><br><span class="line">    addr_len = <span class="keyword">sizeof</span> their_addr;</span><br><span class="line">    <span class="keyword">if</span> ((numbytes = recvfrom(sockfd, buf, MAXBUFLEN<span class="number">-1</span> , <span class="number">0</span>, (struct sockaddr *)&amp;their_addr, &amp;addr_len)) == <span class="number">-1</span>) &#123;</span><br><span class="line">        perror(<span class="string">" recvfrom"</span> );</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">" listener: got packet from %s\n"</span>, inet_ntop(their_addr.ss_family, get_in_addr((struct sockaddr *)&amp;their_addr), s, <span class="keyword">sizeof</span> s));</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">" listener: packet is %d bytes long\n"</span> , numbytes);</span><br><span class="line"></span><br><span class="line">    buf[numbytes] = <span class="string">'\0'</span>;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">" listener: packet contains \" %s\" \n"</span> , buf);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">close</span>(sockfd);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">** talker.c -- 一个 datagram " client" 的 demo</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;errno.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netdb.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SERVERPORT <span class="meta-string">" 4950"</span> <span class="comment">// 用户所要连接的 port</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> sockfd;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> <span class="title">hints</span>, *<span class="title">servinfo</span>, *<span class="title">p</span>;</span></span><br><span class="line">    <span class="keyword">int</span> rv;</span><br><span class="line">    <span class="keyword">int</span> numbytes;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">3</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>,<span class="string">" usage: talker hostname message\n"</span> );</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">memset</span>(&amp;hints, <span class="number">0</span>, <span class="keyword">sizeof</span> hints);</span><br><span class="line">    hints.ai_family = AF_UNSPEC;</span><br><span class="line">    hints.ai_socktype = SOCK_DGRAM;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((rv = getaddrinfo(argv[<span class="number">1</span>], SERVERPORT, &amp;hints, &amp;servinfo)) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">" getaddrinfo: %s\n"</span> , gai_strerror(rv));</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 用循环找出全部的结果， 并产生一个 socket</span></span><br><span class="line">    <span class="keyword">for</span>(p = servinfo; p != <span class="literal">NULL</span>; p = p-&gt;ai_next) &#123;</span><br><span class="line">        <span class="keyword">if</span> ((sockfd = socket(p-&gt;ai_family, p-&gt;ai_socktype, p-&gt;ai_protocol)) == <span class="number">-1</span>) &#123;</span><br><span class="line">            perror(<span class="string">" talker: socket"</span> );</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (p == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">" talker: failed to bind socket\n"</span> );</span><br><span class="line">        <span class="keyword">return</span> <span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((numbytes = sendto(sockfd, argv[<span class="number">2</span>], <span class="built_in">strlen</span>(argv[<span class="number">2</span>]), <span class="number">0</span>, p-&gt;ai_addr, p-&gt;ai_addrlen)) == <span class="number">-1</span>) &#123;</span><br><span class="line">        perror(<span class="string">" talker: sendto"</span> );</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    freeaddrinfo(servinfo);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">" talker: sent %d bytes to %s\n"</span> , numbytes, argv[<span class="number">1</span>]);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">close</span>(sockfd);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>要记得：使用 UDP datagram socket 传送的数据是不会使命必达的！</p>
<p>如果 talker 调用<code>connect()</code>并指定 listener 的地址。从这开始，talker 就只能从<code>connect()</code>所指定的地址进行传送与接收。因此，你不用使用<code>sendto()</code>与<code>recvfrom()</code>，可以单纯使用<code>send()</code>与<code>recv()</code>就好。</p>
<h1 id="高等技术"><a href="#高等技术" class="headerlink" title="高等技术"></a>高等技术</h1><h2 id="Blocking（阻塞）"><a href="#Blocking（阻塞）" class="headerlink" title="Blocking（阻塞）"></a>Blocking（阻塞）</h2><p>很多函数都会 block，<code>accept()</code>会 block，全部的<code>recv()</code>函数都会 block。原因是它们有权这么做。当你先用<code>socket()</code>建立  socket descriptor 时，kernel 会将它设置为 blocking。若你不想要 blocking socket，你必须调用<code>fcntl()</code>：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;fcntl.h&gt;</span></span></span><br><span class="line"></span><br><span class="line">sockfd = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">fcntl(sockfd, F_SETFL, O_NONBLOCK);</span><br></pre></td></tr></table></figure>

<p>将 socket 设置为 non-blocking（非阻塞），你就能 poll（轮询） socket 以取得数据。如果你试着读取 non-blocking  socket，而 socket 没有数据时，函数就不会发生 block，而是返回 -1，并将 errno 设置为<code>EWOULDBLOCK</code>，你可以过一段时间再检查socket是否有数据可以读了。</p>
<p>然而，一般来说，这样 polling 是不好的想法。如果你让程序一直忙着查 socket 上是否有数据，则会浪费 CPU 的时间，这样是不合适的。比较漂亮的解法是利用下一节的<code>select()</code>来检查 socket 是否有数据需要读取。</p>
<h2 id="select-－-同步-IO-多路复用"><a href="#select-－-同步-IO-多路复用" class="headerlink" title="select() － 同步 IO 多路复用"></a>select() － 同步 IO 多路复用</h2><p><code>select()</code>授予你同时监视多个 sockets 的权力，它会告诉你哪些 sockets 已经有数据可以读取、哪些 sockets 已经可以写入，如果你真的想知道， 还会告诉你哪些 sockets 触发了异常。</p>
<p>即使<code>select()</code>有很好的可移植性，不过却是监视 sockets 最慢的方法。一个比较可行的替代方案是<code>libevent</code>（an event notification library）或者其它类似的方法， 封装全部的系统相依要素，用以取得 socket 的通知。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 函数原型</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// numfds 参数应该要设置为 file descriptor 的最高值加 1。</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">select</span><span class="params">(<span class="keyword">int</span> numfds, fd_set *readfds, fd_set *writefds, fd_set *exceptfds, struct timeval *timeout)</span></span>;</span><br></pre></td></tr></table></figure>

<p>每个 sets 的型别都是 fd_set，下列是用来控制这个型别的 macro：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">FD_SET(<span class="keyword">int</span> fd, fd_set *<span class="built_in">set</span>); <span class="comment">// 将 fd 新增到 set。</span></span><br><span class="line">FD_CLR(<span class="keyword">int</span> fd, fd_set *<span class="built_in">set</span>); <span class="comment">// 从 set 移除 fd。</span></span><br><span class="line">FD_ISSET(<span class="keyword">int</span> fd, fd_set *<span class="built_in">set</span>); <span class="comment">// 若 fd 在 set 中，返回 true。</span></span><br><span class="line">FD_ZERO(fd_set *<span class="built_in">set</span>); <span class="comment">// 将 set 整个清为零。</span></span><br></pre></td></tr></table></figure>

<p><code>struct timeval</code>让你可以设置 timeout 的周期。如果时间超过了，而<code>select()</code>还没有找到任何就绪的 file descriptor 时，它会回传，让你可以继续做其它事情。还有，当函数回传时，会更新 timeout，用以表示还剩下多少时间，这个行为取决于你所使用的 Unix。不过无论你将<code>struct timeval</code>设置的多小，你可能还要等待一小段 standard Unix timeslice（标准 Unix 时间片段）。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">timeval</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> tv_sec; <span class="comment">// 秒（second）</span></span><br><span class="line">    <span class="keyword">int</span> tv_usec; <span class="comment">// 微秒（microseconds）</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">** select.c -- a select() demo</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> STDIN 0 <span class="comment">// standard input 的 file descriptor</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">timeval</span> <span class="title">tv</span>;</span></span><br><span class="line">    fd_set readfds;</span><br><span class="line"></span><br><span class="line">    tv.tv_sec = <span class="number">2</span>;</span><br><span class="line">    tv.tv_usec = <span class="number">500000</span>;</span><br><span class="line"></span><br><span class="line">    FD_ZERO(&amp;readfds);</span><br><span class="line">    FD_SET(STDIN, &amp;readfds);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 不用管 writefds 与 exceptfds：</span></span><br><span class="line">    select(STDIN+<span class="number">1</span>, &amp;readfds, <span class="literal">NULL</span>, <span class="literal">NULL</span>, &amp;tv);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (FD_ISSET(STDIN, &amp;readfds))</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">" A key was pressed!\n"</span> );</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">" Timed out.\n"</span> );</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个方法用在需要等待数据的 datagram socket 上很好。</p>
<p>有些系统会用这个方式来使用<code>select()</code>，而有些不行，如果你想要用它，你应该要参考你系统上的 man 使用手册说明看是否会有问题。有些系统会更新<code>struct timeval</code>的时间，用来反映<code>select()</code>原本还剩下多少时间 timeout；不过有些却不会。如果你想要程序是可移植的，那就不要倚赖这个特性。（如果你需要追踪剩下的时间， 可以使用<code>gettimeofday()</code>）</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">** selectserver.c -- 一个 cheezy 的多人聊天室 server</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netdb.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PORT <span class="meta-string">" 9034"</span> <span class="comment">// 我们正在 listen 的 port</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 取得 sockaddr， IPv4 或 IPv6：</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">get_in_addr</span><span class="params">(struct sockaddr *sa)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (sa-&gt;sa_family == AF_INET) &#123;</span><br><span class="line">        <span class="keyword">return</span> &amp;(((struct sockaddr_in*)sa)-&gt;sin_addr);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> &amp;(((struct sockaddr_in6*)sa)-&gt;sin6_addr);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    fd_set master; <span class="comment">// master file descriptor 表</span></span><br><span class="line">    fd_set read_fds; <span class="comment">// 给 select() 用的暂时 file descriptor 表</span></span><br><span class="line">    <span class="keyword">int</span> fdmax; <span class="comment">// 最大的 file descriptor 数目</span></span><br><span class="line">    <span class="keyword">int</span> listener; <span class="comment">// listening socket descriptor</span></span><br><span class="line">    <span class="keyword">int</span> newfd; <span class="comment">// 新接受的 accept() socket descriptor</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_storage</span> <span class="title">remoteaddr</span>;</span> <span class="comment">// client address</span></span><br><span class="line">    <span class="keyword">socklen_t</span> addrlen;</span><br><span class="line">    <span class="keyword">char</span> buf[<span class="number">256</span>]; <span class="comment">// 储存 client 数据的缓冲区</span></span><br><span class="line">    <span class="keyword">int</span> nbytes;</span><br><span class="line">    <span class="keyword">char</span> <span class="built_in">remoteIP</span>[INET6_ADDRSTRLEN];</span><br><span class="line">    <span class="keyword">int</span> yes=<span class="number">1</span>; <span class="comment">// 供底下的 setsockopt() 设置 SO_REUSEADDR</span></span><br><span class="line">    <span class="keyword">int</span> i, j, rv;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> <span class="title">hints</span>, *<span class="title">ai</span>, *<span class="title">p</span>;</span></span><br><span class="line"></span><br><span class="line">    FD_ZERO(&amp;master); <span class="comment">// 清除 master 与 temp sets</span></span><br><span class="line">    FD_ZERO(&amp;read_fds);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 给我们一个 socket， 并且 bind 它</span></span><br><span class="line">    <span class="built_in">memset</span>(&amp;hints, <span class="number">0</span>, <span class="keyword">sizeof</span> hints);</span><br><span class="line">    hints.ai_family = AF_UNSPEC;</span><br><span class="line">    hints.ai_socktype = SOCK_STREAM;</span><br><span class="line">    hints.ai_flags = AI_PASSIVE;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((rv = getaddrinfo(<span class="literal">NULL</span>, PORT, &amp;hints, &amp;ai)) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">" selectserver: %s\n"</span> , gai_strerror(rv));</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span>(p = ai; p != <span class="literal">NULL</span>; p = p-&gt;ai_next) &#123;</span><br><span class="line">        listener = socket(p-&gt;ai_family, p-&gt;ai_socktype, p-&gt;ai_protocol);</span><br><span class="line">        <span class="keyword">if</span> (listener &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 避开这个错误信息：" address already in use"</span></span><br><span class="line">        setsockopt(listener, SOL_SOCKET, SO_REUSEADDR, &amp;yes, <span class="keyword">sizeof</span>(<span class="keyword">int</span>));</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (bind(listener, p-&gt;ai_addr, p-&gt;ai_addrlen) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="built_in">close</span>(listener);</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 若我们进入这个判断式， 则表示我们 bind() 失败</span></span><br><span class="line">    <span class="keyword">if</span> (p == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">" selectserver: failed to bind\n"</span> );</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">2</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    freeaddrinfo(ai); <span class="comment">// all done with this</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// listen</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">listen</span>(listener, <span class="number">10</span>) == <span class="number">-1</span>) &#123;</span><br><span class="line">        perror(<span class="string">" listen"</span> );</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">3</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 将 listener 新增到 master set</span></span><br><span class="line">    FD_SET(listener, &amp;master);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 持续追踪最大的 file descriptor</span></span><br><span class="line">    fdmax = listener; <span class="comment">// 到此为止， 就是它了</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 主要循环</span></span><br><span class="line">    <span class="keyword">for</span>( ; ; ) &#123;</span><br><span class="line">        read_fds = master; <span class="comment">// 复制 master</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (select(fdmax+<span class="number">1</span>, &amp;read_fds, <span class="literal">NULL</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>) == <span class="number">-1</span>) &#123;</span><br><span class="line">            perror(<span class="string">" select"</span> );</span><br><span class="line">            <span class="built_in">exit</span>(<span class="number">4</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 在现存的连接中寻找需要读取的数据</span></span><br><span class="line">        <span class="keyword">for</span>(i = <span class="number">0</span>; i &lt;= fdmax; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (FD_ISSET(i, &amp;read_fds)) &#123; <span class="comment">// 我们找到一个！！</span></span><br><span class="line">                <span class="keyword">if</span> (i == listener) &#123;</span><br><span class="line">                    <span class="comment">// handle new connections</span></span><br><span class="line">                    addrlen = <span class="keyword">sizeof</span> remoteaddr;</span><br><span class="line">                    newfd = accept(listener, (struct sockaddr *)&amp;remoteaddr, &amp;addrlen);</span><br><span class="line"></span><br><span class="line">                    <span class="keyword">if</span> (newfd == <span class="number">-1</span>) &#123;</span><br><span class="line">                        perror(<span class="string">" accept"</span> );</span><br><span class="line">                    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                        FD_SET(newfd, &amp;master); <span class="comment">// 新增到 master set</span></span><br><span class="line">                        <span class="keyword">if</span> (newfd &gt; fdmax) &#123; <span class="comment">// 持续追踪最大的 fd</span></span><br><span class="line">                            fdmax = newfd;</span><br><span class="line">                        &#125;</span><br><span class="line"></span><br><span class="line">                        <span class="built_in">printf</span>(<span class="string">" selectserver: new connection from %s on socket %d\n"</span> , inet_ntop(remoteaddr.ss_family, get_in_addr((struct sockaddr*)&amp;remoteaddr), <span class="built_in">remoteIP</span>, INET6_ADDRSTRLEN), newfd);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 处理来自 client 的数据</span></span><br><span class="line">                    <span class="keyword">if</span> ((nbytes = recv(i, buf, <span class="keyword">sizeof</span> buf, <span class="number">0</span>)) &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">                        <span class="comment">// got error or connection closed by client</span></span><br><span class="line">                        <span class="keyword">if</span> (nbytes == <span class="number">0</span>) &#123;</span><br><span class="line">                            <span class="comment">// 关闭连接</span></span><br><span class="line">                            <span class="built_in">printf</span>(<span class="string">" selectserver: socket %d hung up\n"</span> , i);</span><br><span class="line">                        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                            perror(<span class="string">" recv"</span> );</span><br><span class="line">                        &#125;</span><br><span class="line">                        <span class="built_in">close</span>(i); <span class="comment">// bye!</span></span><br><span class="line">                        FD_CLR(i, &amp;master); <span class="comment">// 从 master set 中移除</span></span><br><span class="line">                    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="comment">// 我们从 client 收到一些数据</span></span><br><span class="line">                        <span class="keyword">for</span>(j = <span class="number">0</span>; j &lt;&gt;= fdmax; j++) &#123;</span><br><span class="line">                            <span class="comment">// 发送给聊天室的其他人！</span></span><br><span class="line">                            <span class="keyword">if</span> (FD_ISSET(j, &amp;master)) &#123;</span><br><span class="line">                                <span class="comment">// 不用送给 listener 跟我们自己</span></span><br><span class="line">                                <span class="keyword">if</span> (j != listener &amp;&amp; j != i) &#123;</span><br><span class="line">                                    <span class="keyword">if</span> (send(j, buf, nbytes, <span class="number">0</span>) == <span class="number">-1</span>) &#123;</span><br><span class="line">                                        perror(<span class="string">" send"</span> );</span><br><span class="line">                                    &#125;</span><br><span class="line">                                &#125;</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="comment">// END handle data from client</span></span><br><span class="line">            &#125; <span class="comment">// END got new incoming connection</span></span><br><span class="line">        &#125; <span class="comment">// END looping through file descriptors</span></span><br><span class="line">    &#125; <span class="comment">// END for( ; ; )--and you thought it would never end!</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line">```       </span><br><span class="line"></span><br><span class="line">还有一个名为`poll()`的函数， 它的行为与`select()`很像， 但是在管理file descriptor sets 时用不一样的结构。</span><br><span class="line"></span><br><span class="line">## 不完整的传送</span><br><span class="line"></span><br><span class="line">`sendall()`会尽力将数据送出， 不过如果有错误发生时， 它就会立刻回传给你。函数在错误时返回 <span class="number">-1</span>，而 errno 仍然从调用`send()`中设置。</span><br><span class="line"></span><br><span class="line">``` C</span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sendall</span><span class="params">(<span class="keyword">int</span> s, <span class="keyword">char</span> *buf, <span class="keyword">int</span> *len)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> total = <span class="number">0</span>; <span class="comment">// 我们已经送出多少 bytes 的数据</span></span><br><span class="line">    <span class="keyword">int</span> bytesleft = *len; <span class="comment">// 我们还有多少数据要送</span></span><br><span class="line">    <span class="keyword">int</span> n;</span><br><span class="line">    <span class="keyword">while</span>(total &lt; *len) &#123;</span><br><span class="line">        n = send(s, buf+total, bytesleft, <span class="number">0</span>);</span><br><span class="line">        <span class="keyword">if</span> (n == <span class="number">-1</span>) &#123; <span class="keyword">break</span>; &#125;</span><br><span class="line">        total += n;</span><br><span class="line">        bytesleft -= n;</span><br><span class="line">    &#125;</span><br><span class="line">    *len = total; <span class="comment">// 返回实际上送出的数据量</span></span><br><span class="line">    <span class="keyword">return</span> n==<span class="number">-1</span>? <span class="number">-1</span>:<span class="number">0</span>; <span class="comment">// 失败时返回 -1 丶成功时返回 0</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 调用函数的示例</span></span><br><span class="line"><span class="keyword">char</span> buf[<span class="number">10</span>] = <span class="string">" Beej!"</span> ;</span><br><span class="line"><span class="keyword">int</span> len;</span><br><span class="line"></span><br><span class="line">len = <span class="built_in">strlen</span>(buf);</span><br><span class="line"><span class="keyword">if</span> (sendall(s, buf, &amp;len) == <span class="number">-1</span>) &#123;</span><br><span class="line">    perror(<span class="string">" sendall"</span> );</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">" We only sent %d bytes because of the error!\n"</span> , len);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果数据包的长度是会变动的（variable），接收端要如何知道另一端的数据包何时开始与结束呢？是的， 你或许必须封装（encapsulate）</p>
<h2 id="Serialization-如何封装数据"><a href="#Serialization-如何封装数据" class="headerlink" title="Serialization - 如何封装数据"></a>Serialization - 如何封装数据</h2><p>我们把变量从内存中变成可存储或传输的过程称为 Serialization（序列化）。序列化之后，就可以把序列化后的内容写入磁盘，或者通过网络传输到别的机器上。反过来，把变量内容从序列化的对象重新读到内存里称之为反序列化。</p>
<h3 id="文本数据的封装"><a href="#文本数据的封装" class="headerlink" title="文本数据的封装"></a>文本数据的封装</h3><p>封装数据，以最简单的例子而言，需要 header（标头），用来代表识别的资料或数据长度，或者都有。</p>
<p>举例来说，使用 SOCK_STREAM 的多用户聊天程序。当某个用户输入［＂says＂］某些字，会有两笔资料要传送给 server：＂谁＂以及＂说了什么＂。这样的问题是信息的长度是会变动的。一个叫做＂tom＂的人可能会说＂Hi＂， 而另一个叫做＂Benjamin＂的人可能说：＂Hey guys what is up？）＂所以你在收到全部的数据之後， 将它全部<code>send()</code>给 clients。 你输出的 data stream 类似这样：</p>
<p><code>tomHiBenjaminHeyguyswhatisup?</code></p>
<p>那 client 要如何知道信息何时开始与结束呢？可以让全部的讯息都一样长，并调用<code>sendall()</code>就行了。但是这样会浪费带宽！</p>
<p>所以我们以小巧的 header 与数据包结构封装（encapsulate）数据。Client 与 server 都知道如何封装（pack）与解封装（unpack）这笔数据。我们会开始定义一个协议（protocol），用来描述 client 与 server 是如何沟通的！</p>
<p>在这个例子中， 假设用户的名称是固定 8 个字元， 并用 ‘\0’ 结尾。 然后接着让我们假设数据的长度是变动的，最多高达 128 个字元。我们看个可能在这个情况会用到的数据包结构范例。</p>
<ol>
<li><p>len［1 个 byte，unsigned］：数据包的总长度，计算 8 个 bytes 的用户名称，以及聊天数据</p>
</li>
<li><p>name［8 个 bytes］：用户名称</p>
</li>
<li><p>chatdata［n 个 bytes］：数据本身，最多 128 bytes。 数据包的长度应该要以这个数据长度加 8 ［上面的 name 栏位长度］来计算</p>
</li>
</ol>
<p>当你传送数据时，你应该要谨慎点，使用类似前面的<code>sendall()</code>指令，因而你可以知道全部的数据都有送出。</p>
<p>同样地，当你接收这笔数据时，你需要额外做些处理。如果要保险一点，你应该假设你可能只会收到部分的数据包内容，但是我们这次调用<code>recv()</code>全部就只收到这些数据。我们需要一次又一次的调用<code>recv()</code>，直到完整地收到数据包内容。</p>
<p>我们可以知道所要接收的数据包它全部的 byte 数量，因为这个数量会记载在数据包前面。我们也知道最大的数据包大小是 1 + 8 + 128，或者 137 bytes，因为这是之前我们定义的。</p>
<p>我们可以采用两种recv的策略：</p>
<p>第一种：</p>
<p>你知道每个数据包是以长度做开头，所以你可以调用 <code>recv()</code>只取得数据包长度。接着，你知道长度以後你就可以再次调用<code>recv()</code>，这时候你就可以正确地指定剩下的数据包长度，直到你收到完整的数据包内容为止。</p>
<p>这个方法的优点是你只需有一个足以存放一个数据包的缓冲区，而缺点是你为了要接收全部的数据， 至少调用两次的<code>recv()</code>。</p>
<p>第二种：</p>
<p>直接调用<code>recv()</code>，并且指定你所要接收的数据包之最大数据量。这样的话，无论你收到多少，都将它写入缓冲区，并最后检查数据包是否完整。当然，你可能会收到下一个数据包的内容，所以你需要有足够的空间。你所能做的是声明一个足以容纳两个数据包的数组，这是你在数据包到达时， 你可以重新建构数据包的地方。</p>
<p>当你处理完第一个数据包后，你可以将第一个数据包的数据从工作缓冲区中清掉，并将第二个数据包的部分内容移到缓冲区的前面，准备进行下一次的<code>recv()</code>。程序可以写成利用<code>环状缓冲区</code>（circular buffer），消除第二个数据包移动到开头的开销。</p>
<h3 id="二进制数据的封装"><a href="#二进制数据的封装" class="headerlink" title="二进制数据的封装"></a>二进制数据的封装</h3><p>要将文字数据透过网路传送很简单，你已经知道了，不过如果你想要送一些＂二进制＂的数据，如 int 或 float，会发生什麽事情呢？这里有一些选择。</p>
<ol>
<li><p>将数字转换为文字，使用如<code>sprintf()</code>的函数，接着传送文字。接收者会使用如<code>strtol()</code>函数解析文字，并转换为数字。</p>
</li>
<li><p>直接以原始数据传送，将指向数据的指针传递给<code>send()</code>。</p>
</li>
<li><p>将数字编码（encode）为可移植的二进制格式，接收者会将它译码（decode）。</p>
</li>
</ol>
<p>实际上，上面全部的方法都有它们的缺点与优点，但是通常偏好第三个方法</p>
<p>第一个方法：在传送以前先将数字编码为文字，优点是你可以很容易打印出及读取来自网路的数据。有时，人类易读的协定比较适用于频带不敏感（non-bandwidth-intensive）的情况，例如：Internet Relay Chat（IRC）。然而，缺点是转换耗时，且总是需要比原本的数字使用更多的空间。</p>
<p>第二个方法：传送原始数据（raw data），这个方法相当简单，但是危险！只要将数据指针提供给<code>send()</code>。事实证明不是全部的架构都能表示<code>double</code>或<code>int</code>。如果你不需要可移植性，在这样的情况下这个方法很好，而且快速。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 发送者</span></span><br><span class="line"><span class="keyword">double</span> d = <span class="number">3490.15926535</span>;</span><br><span class="line"></span><br><span class="line">send(s, &amp;d, <span class="keyword">sizeof</span> d, <span class="number">0</span>); <span class="comment">/* 危险， 不具可移植性！ */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 接收者</span></span><br><span class="line"><span class="keyword">double</span> d;</span><br><span class="line"></span><br><span class="line">recv(s, &amp;d, <span class="keyword">sizeof</span> d, <span class="number">0</span>); <span class="comment">/* 危险， 不具可移植性！ */</span></span><br></pre></td></tr></table></figure>

<p>第三个方法：我们可以将数据封装为接收者已知的二进制格式， 让接收着可以在远端解码。我们已经看过了<code>htons()</code>范例了，它将数字从 host 格式编码为 Network Byte Order 格式；如果要解码这个数字，接收端会调用<code>ntohs()</code>。</p>
<p>但是没有这样的函数可供非整数型别使用，这是因为C语言并没有规范标准的方式来做。要做的事情是将数据封装到已知的格式， 并透过网路送出。例如：封装<code>float</code>，这里的东西有很大的改善空间。这部分的内容就不详细展开，具体的可以参考这个教材。</p>
<h2 id="Broadcast-Packet（广播数据包）"><a href="#Broadcast-Packet（广播数据包）" class="headerlink" title="Broadcast Packet（广播数据包）"></a>Broadcast Packet（广播数据包）</h2><p>到了这里，本文已经谈了如何将数据从一台主机传送到另一台主机。现在考虑同时将数据送给多个主机！</p>
<p>用 UDP（TCP 不行）与标准的 IPv4，可以透过一种叫作广播（broadcasting）的机制达成。IPv6 不支援广播，所以你必须要采用比较高级的技术－群播（multicasting），本文不讨论这个。</p>
<p>你必须在将广播数据包送到网路之前，先设置<code>SO_BROADCAST</code> socket 选项。</p>
<p>指定广播信息的目地地址有两种选择：</p>
<ol>
<li><p>将数据送给子网路（subnet）的广播地址，就是将 subnet’s network（子网路网段）的主机号那部分全部填 1。如果网路是 192.168.1.0，而netmask是 255.255.255.0，广播地址就是 192.168.1.255。</p>
</li>
<li><p>将数据送给 global（全局的）广播地址，255.255.255.255，又称为 INADDR_BROADCAST。Routers 不会将这类的广<br>播数据包转发出你的区域网络</p>
</li>
</ol>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">** broadcaster.c -- 一个类似 talker.c 的 datagram " client" ，</span></span><br><span class="line"><span class="comment">** 差异在於这个可以广播</span></span><br><span class="line"><span class="comment">**</span></span><br><span class="line"><span class="comment">** 这里使用的都是IPv4的旧方法</span></span><br><span class="line"><span class="comment">** gethostbyname， 手动填struct sockaddr_in（inet_addr），inet_ntoa</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;errno.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netdb.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SERVERPORT 4950 <span class="comment">// 所要连接的 port</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> sockfd;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">their_addr</span>;</span> <span class="comment">// 连接者的地址资料</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">hostent</span> *<span class="title">he</span>;</span></span><br><span class="line">    <span class="keyword">int</span> numbytes;</span><br><span class="line">    <span class="keyword">int</span> broadcast = <span class="number">1</span>;</span><br><span class="line">    <span class="comment">//char broadcast = '1'; // 如果上面这行不能用的话， 改用这行</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">3</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>,<span class="string">" usage: broadcaster hostname message\n"</span> );</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((he=gethostbyname(argv[<span class="number">1</span>])) == <span class="literal">NULL</span>) &#123; <span class="comment">// 取得 host 资料</span></span><br><span class="line">        perror(<span class="string">" gethostbyname"</span> );</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((sockfd = socket(AF_INET, SOCK_DGRAM, <span class="number">0</span>)) == <span class="number">-1</span>) &#123;</span><br><span class="line">        perror(<span class="string">" socket"</span> );</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 这个 call 就是要让 sockfd 可以送广播数据包</span></span><br><span class="line">    <span class="keyword">if</span> (setsockopt(sockfd, SOL_SOCKET, SO_BROADCAST, &amp;broadcast, <span class="keyword">sizeof</span> broadcast) == <span class="number">-1</span>) &#123;</span><br><span class="line">        perror(<span class="string">" setsockopt (SO_BROADCAST)"</span> );</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    their_addr.sin_family = AF_INET; <span class="comment">// host byte order</span></span><br><span class="line">    their_addr.sin_port = htons(SERVERPORT); <span class="comment">// short, network byte order</span></span><br><span class="line">    their_addr.sin_addr = *((struct in_addr *)he-&gt;h_addr);</span><br><span class="line">    <span class="built_in">memset</span>(their_addr.sin_zero, <span class="string">'\0'</span>, <span class="keyword">sizeof</span> their_addr.sin_zero);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((numbytes=sendto(sockfd, argv[<span class="number">2</span>], <span class="built_in">strlen</span>(argv[<span class="number">2</span>]), <span class="number">0</span>, (struct sockaddr *)&amp;their_addr, <span class="keyword">sizeof</span> their_addr)) == <span class="number">-1</span>) &#123;</span><br><span class="line">        perror(<span class="string">" sendto"</span> ); </span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">" sent %d bytes to %s\n"</span>, numbytes,inet_ntoa(their_addr.sin_addr));</span><br><span class="line"></span><br><span class="line">    <span class="built_in">close</span>(sockfd);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个跟一般的 UDP client/server 没什么不同，除了 client 可以送出广播数据包。如果 listener 没有回应，可能是因为它绑到 IPv6 地址了，试着将 listener.c 中的<code>AF_UNSPEC</code>改成<code>AF_INET</code>，强制使用 IPv4。</p>
<p>如果 listener 收到你直接送给它的数据， 但不是在广播地址没有数据， 可能是因为你本地端上有防火墙（firewall）封锁了这些数据包。</p>
<p>防火墙，是指一种将内部网络和公有网络（如Internet）分开的方法。它实际上是一种隔离技术，在两个网络通信时执行的一种访问控制尺度，它能允许你”同意”的人和数据进入你的网络，同时将你“不同意”的人和数据拒之门外，最大限度地阻止网络中的黑客来访问你的网络。 </p>
<h1 id="常见的问题"><a href="#常见的问题" class="headerlink" title="常见的问题"></a>常见的问题</h1><h2 id="实现调用-connect-的-timeout"><a href="#实现调用-connect-的-timeout" class="headerlink" title="实现调用 connect() 的 timeout"></a>实现调用 connect() 的 timeout</h2><p>你要用<code>socket()</code>建立一个 socket descriptor，将它设置为 non-blocking，然后调用<code>connect()</code>，而如果一切顺利，<code>connect()</code> 会立即返回 -1，并将 errno 设置为<code>EINPROGRESS</code>。接着你要调用<code>select()</code>并设置你想要的 timeout 时间，传递读写组（read and write sets）的 socket descriptor。如果<code>select()</code>没有发生 timeout，这表示<code>connect()</code>调用已经完成。此时，你必须使用<code>getsockopt()</code>设置<code>SO_ERROR</code>选择项以取得<code>connect()</code>的返回值，在没有错误时，这个值应该是零。最后，在你开始透过 socket 传输数据以前，你可能想要再将它设置回 blocking。</p>
<p>要注意的是，这么做的好处是让你的程序在连接（connecting）期间也可以另外做点事情。比如：你可以将 timeout 时间设定为类似 500 毫秒，并在每次 timeout 发生时更新显示器画面，然後再次调用<code>select()</code>。当你已经调用了<code>select()</code>时，并且 timeout 了，像这样重复了 20 次，你就会知道应该放弃这个连接了。</p>
<h2 id="实现调用-recv-的-timeout"><a href="#实现调用-recv-的-timeout" class="headerlink" title="实现调用 recv() 的 timeout"></a>实现调用 recv() 的 timeout</h2><p>还是使用<code>select()</code>，请注意，recvtimeout() 在 timeout 的例子中会返回 -2。这是因为在呼叫<code>recv()</code>返回 0 值时所代表的意思是对方已经关闭了连接，而 -1 表示＂错误＂，所以我选择 -2 做为我的 timeout 表示。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">recvtimeout</span><span class="params">(<span class="keyword">int</span> s, <span class="keyword">char</span> *buf, <span class="keyword">int</span> len, <span class="keyword">int</span> timeout)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    fd_set fds;</span><br><span class="line">    <span class="keyword">int</span> n;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">timeval</span> <span class="title">tv</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 设置 file descriptor set</span></span><br><span class="line">    FD_ZERO(&amp;fds);</span><br><span class="line">    FD_SET(s, &amp;fds);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 设置 timeout 的数据结构 struct timeval</span></span><br><span class="line">    tv.tv_sec = timeout;</span><br><span class="line">    tv.tv_usec = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 一直等到 timeout 或收到数据</span></span><br><span class="line">    n = select(s+<span class="number">1</span>, &amp;fds, <span class="literal">NULL</span>, <span class="literal">NULL</span>, &amp;tv);</span><br><span class="line">    <span class="keyword">if</span> (n == <span class="number">0</span>) <span class="keyword">return</span> <span class="number">-2</span>; <span class="comment">// timeout!</span></span><br><span class="line">    <span class="keyword">if</span> (n == <span class="number">-1</span>) <span class="keyword">return</span> <span class="number">-1</span>; <span class="comment">// error</span></span><br><span class="line">    <span class="comment">// 数据一定有在这里， 所以调用一般的 recv()</span></span><br><span class="line">    <span class="keyword">return</span> recv(s, buf, len, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 调用 recvtimeout() 的示例：</span></span><br><span class="line">n = recvtimeout(s, buf, <span class="keyword">sizeof</span> buf, <span class="number">10</span>); <span class="comment">// 10 second timeout</span></span><br><span class="line"><span class="keyword">if</span> (n == <span class="number">-1</span>) &#123;</span><br><span class="line">    <span class="comment">// 发生错误</span></span><br><span class="line">    perror(<span class="string">" recvtimeout"</span> );</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (n == <span class="number">-2</span>) &#123;</span><br><span class="line">    <span class="comment">// 发生 timeout</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// 从 buf 收到一些数据</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>网络编程</category>
      </categories>
      <tags>
        <tag>网络编程</tag>
      </tags>
  </entry>
  <entry>
    <title>Beej&#39;s Guide to Network Programming 读书笔记（上）</title>
    <url>/2020/04/18/Beejs-Guide-to-Network-Programming-1st/</url>
    <content><![CDATA[<h1 id="何谓-Socket"><a href="#何谓-Socket" class="headerlink" title="何谓 Socket"></a>何谓 Socket</h1><p>Socket 是利用标准 UNIX file descriptors（文件描述符）与其它程序沟通的一种方式。可以用一般的<code>read()</code>与<code>write()</code>调用通过socket进行通讯，但是<code>send()</code>与<code>recv()</code>让你能对数据传输有更多的控制权。</p>
<p>一般的 socket 只能读取传输层以上（不含）的数据，raw socket 一般用在设计 network sniffer，可以让应用程序取得网路数据包底层的数据（如 TCP 层、IP 层、数据链路层），并用以分析数据包。</p>
<p>主要讨论两种 Internet sockets，一个是 Stream Sockets；而另一个是 Datagram Sockets，分别以<code>SOCK_STREAM</code>与<code>SOCK_DGRAM</code>来表示。不讨论 raw socket，以<code>SOCK_RAW</code>来表示。</p>
<p>为什麽你要用一个不可靠的底层协议（UDP）？有两个理由：第一个理由是速度，第二个理由还是速度。</p>
<h1 id="结构与数据转换"><a href="#结构与数据转换" class="headerlink" title="结构与数据转换"></a>结构与数据转换</h1><h2 id="字节顺序"><a href="#字节顺序" class="headerlink" title="字节顺序"></a>字节顺序</h2><p>网络序为大端序，即数字先存储比较大那一边（高位放在左边）</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Network Byte Order &lt;-&gt; Host Byte Order</span></span><br><span class="line">htons() <span class="comment">// host to network short</span></span><br><span class="line">htonl() <span class="comment">// host to network long</span></span><br><span class="line">ntohs() <span class="comment">// network to host short</span></span><br><span class="line">ntohl() <span class="comment">// network to host long</span></span><br></pre></td></tr></table></figure>

<h2 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h2><p><code>struct addrinfo</code>，这个数据结构是最近的发明，用来准备之后要用的 socket 地址数据结构，也用在主机名（host name）及服务名（service name）的查询。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> ai_flags; <span class="comment">// AI_PASSIVE, AI_CANONNAME等</span></span><br><span class="line">    <span class="keyword">int</span> ai_family; <span class="comment">// AF_INET, AF_INET6, AF_UNSPEC</span></span><br><span class="line">    <span class="keyword">int</span> ai_socktype; <span class="comment">// SOCK_STREAM, SOCK_DGRAM</span></span><br><span class="line">    <span class="keyword">int</span> ai_protocol; <span class="comment">// 用 0 当作 "any"</span></span><br><span class="line">    <span class="keyword">size_t</span> ai_addrlen; <span class="comment">// ai_addr 的大小， 单位是 byte</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span> *<span class="title">ai_addr</span>;</span> <span class="comment">// struct sockaddr_in 或 _in6</span></span><br><span class="line">    <span class="keyword">char</span> *ai_canonname; <span class="comment">// 典型的 hostname</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> *<span class="title">ai_next</span>;</span> <span class="comment">// 链表的下个节点</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>要注意的是，这是个链表，<code>ai_next</code>是指向下一个成员（element）， 可能会有多个结果让你选择。</p>
<p><code>struct sockaddr</code>记录了很多 sockets 类型的 socket 的地址资料。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span> &#123;</span></span><br><span class="line">    <span class="keyword">unsigned</span> short sa_family; <span class="comment">// address family, AF_xxx</span></span><br><span class="line">    <span class="keyword">char</span> sa_data[<span class="number">14</span>]; <span class="comment">// 14 bytes of protocol address</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><code>sa_data</code>包含一个 socket 的目地地址与端口号。这样很不方便，因为你不会想要手动的将地址封装到<code>sa_data</code>里。</p>
<p>为了处理<code>struct sockaddr</code>，程序设计师建立了对等平行的数据结构：<code>struct sockaddr_in</code>（in是代表internet）用在 IPv4。指向<code>struct sockaddr_in</code>的指针可以转型为指向<code>struct sockaddr</code>的指针，反之亦然。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// (IPv4 only--see struct sockaddr_in6 for IPv6)</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> &#123;</span></span><br><span class="line">    short <span class="keyword">int</span> sin_family; <span class="comment">// Address family, AF_INET</span></span><br><span class="line">    <span class="keyword">unsigned</span> short <span class="keyword">int</span> sin_port; <span class="comment">// Port number</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">in_addr</span> <span class="title">sin_addr</span>;</span> <span class="comment">// Internet address</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">char</span> sin_zero[<span class="number">8</span>]; <span class="comment">// 与 struct sockaddr 相同的大小</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// (仅限 IPv4 — Ipv6 请参考 struct in6_addr)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Internet address (a structure for historical reasons)</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">in_addr</span> &#123;</span></span><br><span class="line">    <span class="keyword">uint32_t</span> s_addr; <span class="comment">// that's a 32-bit int (4 bytes)</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>要注意的是<code>sin_zero</code>（这是用来将数据结构补足符合<code>struct sockaddr</code>的长度），应该要使用<code>memset()</code>函数将<code>sin_zero</code>整个清为零。还有<code>struct in_addr</code>和<code>sin_port</code>必须是 Network Byte Order（利用<code>htons()</code>）。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// (IPv6 only--see struct sockaddr_in and struct in_addr for IPv4)</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in6</span> &#123;</span></span><br><span class="line">    <span class="keyword">u_int16_t</span> sin6_family; <span class="comment">// address family, AF_INET6</span></span><br><span class="line">    <span class="keyword">u_int16_t</span> sin6_port; <span class="comment">// port number, Network Byte Order</span></span><br><span class="line">    <span class="keyword">u_int32_t</span> sin6_flowinfo; <span class="comment">// IPv6 flow information</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">in6_addr</span> <span class="title">sin6_addr</span>;</span> <span class="comment">// IPv6 address</span></span><br><span class="line">    <span class="keyword">u_int32_t</span> sin6_scope_id; <span class="comment">// Scope ID</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">in6_addr</span> &#123;</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">char</span> s6_addr[<span class="number">16</span>]; <span class="comment">// IPv6 address</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这个简单的<code>struct sockaddr_storage</code>是设计用来足够储存 IPv4 与 IPv6 结构的结构体。你可以在<code>ss_family</code>栏位看到地址家族，检查它是<code>AF_INET</code>或<code>AF_INET6</code>（是 IPv4 或 IPv6）。之后如果你愿意的话，你就可以将它转型为<code>struct sockaddr_in</code>或<code>struct sockaddr_in6</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_storage</span> &#123;</span></span><br><span class="line">    <span class="keyword">sa_family_t</span> ss_family; <span class="comment">// address family</span></span><br><span class="line">    <span class="comment">// all this is padding, implementation specific, ignore it:</span></span><br><span class="line">    <span class="keyword">char</span> __ss_pad1[_SS_PAD1SIZE];</span><br><span class="line">    <span class="keyword">int64_t</span> __ss_align;</span><br><span class="line">    <span class="keyword">char</span> __ss_pad2[_SS_PAD2SIZE];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="IP地址"><a href="#IP地址" class="headerlink" title="IP地址"></a>IP地址</h2><p>使用<code>inet_pton()</code>函数将 IP address 字符串格式转换成网络地址格式，并依照你指定的<code>AF_INET</code>或<code>AF_INET6</code>来决定要储存在<code>struct in_addr</code>或<code>struct in6_addr</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">sa</span>;</span> <span class="comment">// IPv4</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in6</span> <span class="title">sa6</span>;</span> <span class="comment">// IPv6</span></span><br><span class="line">inet_pton(AF_INET, <span class="string">" 192.0.2.1"</span> , &amp;(sa.sin_addr)); <span class="comment">// IPv4</span></span><br><span class="line">inet_pton(AF_INET6, <span class="string">" 2001:db8:63b3:1::3490"</span> , &amp;(sa6.sin6_addr)); <span class="comment">// IPv6</span></span><br></pre></td></tr></table></figure>

<p>目前上述的代码片段还不是很可靠，因为没有错误检查。<code>inet_pton()</code>在错误时会返回-1，而若地址被搞砸了，则会返回0。所以在使用之前要检查，并确认结果是大于0的。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 你有一个 struct in_addr 且你想要以数字与句号的字符串格式打印出来</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// IPv4:</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">char</span> ip4[INET_ADDRSTRLEN]; <span class="comment">// 储存 IPv4 字符串的空间</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">sa</span>;</span> <span class="comment">// pretend this is loaded with something</span></span><br><span class="line">inet_ntop(AF_INET, &amp;(sa.sin_addr), ip4, INET_ADDRSTRLEN);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">" The IPv4 address is: %s\n"</span> , ip4);</span><br><span class="line"></span><br><span class="line"><span class="comment">// IPv6:</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">char</span> ip6[INET6_ADDRSTRLEN]; <span class="comment">// 储存 IPv6 字符串的空间</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in6</span> <span class="title">sa6</span>;</span> <span class="comment">// pretend this is loaded with something</span></span><br><span class="line">inet_ntop(AF_INET6, &amp;(sa6.sin6_addr), ip6, INET6_ADDRSTRLEN);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">" The address is: %s\n"</span> , ip6);</span><br></pre></td></tr></table></figure>

<p>IPv4的老方法是用<code>inet_addr()</code>和<code>inet_ntoa()</code>实现字符串IP地址和IPv4地址结构in_addr值的转换</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// !!! 这 是 老 方 法 !!!</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 将字符串IP地址转换为IPv4地址结构in_addr值</span></span><br><span class="line"><span class="function"><span class="keyword">in_addr_t</span> <span class="title">inet_addr</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> * strptr)</span></span>; </span><br><span class="line"></span><br><span class="line"><span class="comment">// 将IPv4地址结构in_addr值转换为字符串IP地址</span></span><br><span class="line"><span class="function"><span class="keyword">char</span> * <span class="title">inet_ntoa</span><span class="params">(struct in_addr * addrptr)</span></span>;</span><br></pre></td></tr></table></figure>

<h2 id="私有网络"><a href="#私有网络" class="headerlink" title="私有网络"></a>私有网络</h2><p>防火墙会用所谓的网路地址转换（NAT，Network Address Translation）的方法，将内部的 IP 地址转换为外部的 IP address。</p>
<p>10.x.x.x 是其中一个少数保留的网路，只能用在完全无法连上 Internet 的网路，或是在防火墙的网路。一般而言，你较常见的是 10.x.x.x 及 192.168.x.x，这里的 x 是指 0-255。较少见的是 172.y.x.x，这里的 y 范围在 16 与 31 之间。</p>
<h1 id="System-Call"><a href="#System-Call" class="headerlink" title="System Call"></a>System Call</h1><h2 id="getaddrinfo-准备开始"><a href="#getaddrinfo-准备开始" class="headerlink" title="getaddrinfo() - 准备开始"></a>getaddrinfo() - 准备开始</h2><p>它前身是你用来做 DNS 查询的<code>gethostbyname()</code>，你需要手动将资料写入<code>struct sockaddr_in</code>，并在你的调用中使用。现在有<code>getaddrinfo()</code>函数，可以帮你做许多事情，包含 DNS 与 service name 查询，并填好你所需的structs。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 函数原型</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netdb.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">getaddrinfo</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *node, <span class="comment">// 例如： " www.example.com" 或 IP</span></span></span></span><br><span class="line"><span class="function"><span class="params"><span class="keyword">const</span> <span class="keyword">char</span> *service, <span class="comment">// 例如： " http" 或 port number</span></span></span></span><br><span class="line"><span class="function"><span class="params"><span class="keyword">const</span> struct addrinfo *hints,</span></span></span><br><span class="line"><span class="function"><span class="params">struct addrinfo **res)</span></span>;</span><br></pre></td></tr></table></figure>

<p><code>hints</code>参数指向一个你已经填好相关资料的<code>struct addrinfo</code>。下面是一个调用示例， 如果你是一个 server， 想要在你主机上的 IP address 及 port 3490 运行 listen。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> status;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> <span class="title">hints</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> *<span class="title">servinfo</span>;</span> <span class="comment">// 将指向结果</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">memset</span>(&amp;hints, <span class="number">0</span>, <span class="keyword">sizeof</span> hints); <span class="comment">// 确保 struct 为空</span></span><br><span class="line">hints.ai_family = AF_UNSPEC; <span class="comment">// 不用管是 IPv4 或 IPv6</span></span><br><span class="line">hints.ai_socktype = SOCK_STREAM; <span class="comment">// TCP stream sockets</span></span><br><span class="line">hints.ai_flags = AI_PASSIVE; <span class="comment">// 帮我填好我的 IP</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> ((status = getaddrinfo(<span class="literal">NULL</span>, <span class="string">" 3490"</span> , &amp;hints, &amp;servinfo)) != <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">" getaddrinfo error: %s\n"</span> , gai_strerror(status));</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// servinfo 目前指向一个或多个 struct addrinfos 的链表</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// ... 做每件事情， 一直到你不再需要 servinfo ....</span></span><br><span class="line"></span><br><span class="line">freeaddrinfo(servinfo); <span class="comment">// 释放这个链表</span></span><br></pre></td></tr></table></figure>

<p>在这里看到<code>AI_PASSIVE</code>flag；这个会告诉 <code>getaddrinfo()</code>要将我本地端的地址指定给 socket structure。 这样你就不用写固定的地址了，或者你可以将特定的地址放在<code>getaddrinfo()</code>的第一个参数中，现在写 NULL 的那个参数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">** showip.c -- 显示命令行中所给的主机 IP address</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netdb.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> <span class="title">hints</span>, *<span class="title">res</span>, *<span class="title">p</span>;</span></span><br><span class="line">    <span class="keyword">int</span> status;</span><br><span class="line">    <span class="keyword">char</span> ipstr[INET6_ADDRSTRLEN];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>,<span class="string">" usage: showip hostname\n"</span> );</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">memset</span>(&amp;hints, <span class="number">0</span>, <span class="keyword">sizeof</span> hints);</span><br><span class="line">    hints.ai_family = AF_UNSPEC; <span class="comment">// AF_INET 或 AF_INET6 可以指定版本</span></span><br><span class="line">    hints.ai_socktype = SOCK_STREAM;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((status = getaddrinfo(argv[<span class="number">1</span>], <span class="literal">NULL</span>, &amp;hints, &amp;res)) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">" getaddrinfo: %s\n"</span> , gai_strerror(status));</span><br><span class="line">        <span class="keyword">return</span> <span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">" IP addresses for %s:\n\n"</span> , argv[<span class="number">1</span>]);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span>(p = res;p != <span class="literal">NULL</span>; p = p-&gt;ai_next) &#123;</span><br><span class="line">        <span class="keyword">void</span> *addr;</span><br><span class="line">        <span class="keyword">char</span> *ipver;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 取得本身地址的指针，</span></span><br><span class="line">        <span class="comment">// 在 IPv4 与 IPv6 中的栏位不同：</span></span><br><span class="line">        <span class="keyword">if</span> (p-&gt;ai_family == AF_INET) &#123; <span class="comment">// IPv4</span></span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> *<span class="title">ipv4</span> = (<span class="title">struct</span> <span class="title">sockaddr_in</span> *)<span class="title">p</span>-&gt;<span class="title">ai_addr</span>;</span></span><br><span class="line">            addr = &amp;(ipv4-&gt;sin_addr);</span><br><span class="line">            ipver = <span class="string">" IPv4"</span> ;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123; <span class="comment">// IPv6</span></span><br><span class="line">            struct sockaddr_in6 *ipv6 = (struct sockaddr_in6 *)p-&gt;ai_addr;</span><br><span class="line">            addr = &amp;(ipv6-&gt;sin6_addr);</span><br><span class="line">            ipver = <span class="string">" IPv6"</span> ;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// convert the IP to a string and print it:</span></span><br><span class="line">        inet_ntop(p-&gt;ai_family, addr, ipstr, <span class="keyword">sizeof</span> ipstr);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">" %s: %s\n"</span> , ipver, ipstr);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    freeaddrinfo(res); <span class="comment">// 释放链表</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="socket-取得-File-Descriptor"><a href="#socket-取得-File-Descriptor" class="headerlink" title="socket() - 取得 File Descriptor"></a>socket() - 取得 File Descriptor</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 函数原型</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">socket</span><span class="params">(<span class="keyword">int</span> domain, <span class="keyword">int</span> type, <span class="keyword">int</span> protocol)</span></span>;</span><br></pre></td></tr></table></figure>

<p><code>domain</code>是<code>PF_INET</code>或<code>PF_INET6</code>（在<code>struct sockaddr_in</code>中使用<code>AF_INET</code>，而在调用<code>socket()</code>时使用<code>PF_INET</code>）；<code>type</code>是<code>SOCK_STREAM</code>或<code>SOCK_DGRAM</code>；而<code>protocol</code>可以设置为0，用来帮给予的<code>type</code>选择适当的协议。 或者你可以调用 getprotobyname() 来查询你想要的协议，＂tcp＂或＂udp＂。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> s;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> <span class="title">hints</span>, *<span class="title">res</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 运行查询，假装我们已经填好 " hints" struct</span></span><br><span class="line">getaddrinfo(<span class="string">" www.example.com"</span> , <span class="string">" http"</span> , &amp;hints, &amp;res);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 你应该要对 getaddrinfo() 进行错误检查, 并走到 " res" 链表查询能用的资料，而不是假设第一笔资料就是好的</span></span><br><span class="line">s = socket(res-&gt;ai_family, res-&gt;ai_socktype, res-&gt;ai_protocol);</span><br></pre></td></tr></table></figure>

<p><code>socket()</code>单纯返回一个之后的 system call 要用的 socket descriptor 给你，错误时会返回 -1，errno 全局变量会设置为该错误的值。</p>
<h2 id="bind-－-绑定端口"><a href="#bind-－-绑定端口" class="headerlink" title="bind() － 绑定端口"></a>bind() － 绑定端口</h2><p>port number 是用来让 kernel 可以比对出进入的数据包是属于哪个 process 的 socket descriptor。一个 process 可以有多个 socket fd，绑定多个 port number。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 函数原型</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">bind</span><span class="params">(<span class="keyword">int</span> sockfd, struct sockaddr *my_addr, <span class="keyword">int</span> addrlen)</span></span>;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> <span class="title">hints</span>, *<span class="title">res</span>;</span></span><br><span class="line"><span class="keyword">int</span> sockfd;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 首先， 用 getaddrinfo() 载入地址结构：</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">memset</span>(&amp;hints, <span class="number">0</span>, <span class="keyword">sizeof</span> hints);</span><br><span class="line">hints.ai_family = AF_UNSPEC; <span class="comment">// use IPv4 or IPv6, whichever</span></span><br><span class="line">hints.ai_socktype = SOCK_STREAM;</span><br><span class="line">hints.ai_flags = AI_PASSIVE; <span class="comment">// fill in my IP for me</span></span><br><span class="line"></span><br><span class="line">getaddrinfo(<span class="literal">NULL</span>, <span class="string">" 3490"</span> , &amp;hints, &amp;res);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 建立一个 socket：</span></span><br><span class="line"></span><br><span class="line">sockfd = socket(res-&gt;ai_family, res-&gt;ai_socktype, res-&gt;ai_protocol);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 将 socket bind 到我们传递给 getaddrinfo() 的 port：</span></span><br><span class="line"></span><br><span class="line">bind(sockfd, res-&gt;ai_addr, res-&gt;ai_addrlen);</span><br></pre></td></tr></table></figure>

<p>bind() 在错误时也会返回 -1， 并将 errno 设置为该错误的值。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// !!! 这 是 老 方 法 !!!</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 许多旧程序都在调用 bind() 以前手动封装 struct sockaddr_in</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> sockfd;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">my_addr</span>;</span></span><br><span class="line"></span><br><span class="line">sockfd = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">my_addr.sin_family = AF_INET;</span><br><span class="line">my_addr.sin_port = htons(MYPORT); <span class="comment">// short, network byte order</span></span><br><span class="line">my_addr.sin_addr.s_addr = inet_addr(<span class="string">" 10.12.110.57"</span> );</span><br><span class="line"><span class="built_in">memset</span>(my_addr.sin_zero, <span class="string">'\0'</span>, <span class="keyword">sizeof</span> my_addr.sin_zero);</span><br><span class="line"></span><br><span class="line">bind(sockfd, (struct sockaddr *)&amp;my_addr, <span class="keyword">sizeof</span> my_addr);</span><br></pre></td></tr></table></figure>

<p>在上列的代码中， 如果你想要 bind 到你本地端的 IP address（就像上面的<code>AI_PASSIVE</code> flag），你也可以将<code>INADDR_ANY</code>指定给<code>s_addr</code>栏位。<code>INADDR_ANY</code>的 IPv6 版本是一个<code>in6addr_any</code>全局变量，它会被指定给你的<code>struct sockaddr_in6</code>的<code>sin6_addr</code>栏位。</p>
<p><code>INADDR_ANY</code>转换过来就是 0.0.0.0，泛指本机的意思，也就是表示本机的所有 IP，因为有些机子不止一块网卡，多网卡的情况下，这个就表示所有网卡 IP 地址的意思。所以出现<code>INADDR_ANY</code>，你只需绑定<code>INADDR_ANY</code>，管理一个套接字就行，不管数据是从哪个网卡过来的，只要是绑定的端口号过来的数据，都可以接收到。</p>
<p>你可能有注意到，有时候你试着重新运行 server，而<code>bind()</code>却失败了，它声称＂Address already in use.＂。有些连接到 socket 的连接还悬在 kernel 里面，而它占据了这个 port。你可以等待它自行清除（一分钟左右），或者在你的程序中新增代码，让它重新使用这个 port，类似这样：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> yes=<span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 可以跳过 " Address already in use" 错误信息</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (setsockopt(listener,SOL_SOCKET,SO_REUSEADDR,&amp;yes,<span class="keyword">sizeof</span>(<span class="keyword">int</span>)) == <span class="number">-1</span>) &#123;</span><br><span class="line">    perror(<span class="string">" setsockopt"</span> );</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>若你正使用<code>connect()</code>连接到远端的机器，你可以不用管 local port 是多少，你可以单纯地调用<code>connect()</code>，它会检查 socket 是否尚未绑定， 并在有需要的时候自动将 socket <code>bind()</code>到一个尚未使用的 local port。</p>
<h2 id="connect-嘿！你好"><a href="#connect-嘿！你好" class="headerlink" title="connect() - 嘿！你好"></a>connect() - 嘿！你好</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 函数原型</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">connect</span><span class="params">(<span class="keyword">int</span> sockfd, struct sockaddr *serv_addr, <span class="keyword">int</span> addrlen)</span></span>;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> <span class="title">hints</span>, *<span class="title">res</span>;</span></span><br><span class="line"><span class="keyword">int</span> sockfd;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 首先， 用 getaddrinfo() 载入 address structs：</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">memset</span>(&amp;hints, <span class="number">0</span>, <span class="keyword">sizeof</span> hints);</span><br><span class="line">hints.ai_family = AF_UNSPEC;</span><br><span class="line">hints.ai_socktype = SOCK_STREAM;</span><br><span class="line"></span><br><span class="line">getaddrinfo(<span class="string">" www.example.com"</span> , <span class="string">" 3490"</span> , &amp;hints, &amp;res);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 建立一个 socket：</span></span><br><span class="line"></span><br><span class="line">sockfd = socket(res-&gt;ai_family, res-&gt;ai_socktype, res-&gt;ai_protocol);</span><br><span class="line"></span><br><span class="line"><span class="comment">// connect!</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">connect</span>(sockfd, res-&gt;ai_addr, res-&gt;ai_addrlen);</span><br></pre></td></tr></table></figure>

<p>要确定有检查<code>connect()</code>返回的值，它在错误时会返回 -1，并设定 errno 变量。</p>
<h2 id="listen-有人会调用我吗"><a href="#listen-有人会调用我吗" class="headerlink" title="listen() - 有人会调用我吗"></a>listen() - 有人会调用我吗</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 函数原型</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">listen</span><span class="params">(<span class="keyword">int</span> sockfd, <span class="keyword">int</span> backlog)</span></span>;</span><br></pre></td></tr></table></figure>

<p><code>backlog</code>是进入的队列（incoming queue）中所允许的连接数目。如同往常，<code>listen()</code>会返回 -1 并在错误时设置 errno。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">如果你正在 <span class="built_in">listen</span> 进入的连接， 你会运行的 system call 顺序如下</span><br><span class="line"></span><br><span class="line">getaddrinfo();</span><br><span class="line">socket();</span><br><span class="line">bind();</span><br><span class="line"><span class="built_in">listen</span>();</span><br><span class="line"><span class="comment">/* accept() 从这里开始 */</span></span><br></pre></td></tr></table></figure>

<h2 id="accept-谢谢你的调用"><a href="#accept-谢谢你的调用" class="headerlink" title="accept() - 谢谢你的调用"></a>accept() - 谢谢你的调用</h2><p>很远的人会试着<code>connect()</code>到你的电脑正在<code>listen()</code>的 port。他们的连接会排队等待被<code>accept()</code>。你调用<code>accept()</code>，并告诉它要取得搁置的（pending）连接。它会返回专属这个连接的一个新 socket file descriptor 给你！你突然有了两个 socket file descriptor！原本的 socket file descriptor 仍然正在 listen 之后的连线，而新建立的 socket file descriptor 则是在最後要准备给<code>send()</code>与<code>recv()</code>用的。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 函数原型</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">accept</span><span class="params">(<span class="keyword">int</span> sockfd, struct sockaddr *addr, <span class="keyword">socklen_t</span> *addrlen)</span></span>;</span><br></pre></td></tr></table></figure>

<p>sockfd 是正在进行<code>listen()</code>的 socket descriptor。很简单，<code>addr</code>通常是一个指向 local struct<br>sockaddr_storage 的指针，是关于进来的连接将往哪里去的资料，你可以用它来得知是哪一台主机从哪一个 port 调用你的。<code>addrlen</code>是一个 local 的整数变量，应该在将它的地址传递给<code>accept()</code>以前， 将它设置为<code>sizeof(struct sockaddr_storage)</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MYPORT <span class="meta-string">" 3490"</span> <span class="comment">// 使用者将连接的 port</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BACKLOG 10 <span class="comment">// 在队列中可以有多少个连接在等待</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_storage</span> <span class="title">their_addr</span>;</span></span><br><span class="line">    <span class="keyword">socklen_t</span> addr_size;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> <span class="title">hints</span>, *<span class="title">res</span>;</span></span><br><span class="line">    <span class="keyword">int</span> sockfd, new_fd;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// !! 不要忘了帮这些调用做错误检查 !!</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 首先， 使用 getaddrinfo() 载入 address struct：</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">memset</span>(&amp;hints, <span class="number">0</span>, <span class="keyword">sizeof</span> hints);</span><br><span class="line">    hints.ai_family = AF_UNSPEC; <span class="comment">// 使用 IPv4 或 IPv6， 都可以</span></span><br><span class="line">    hints.ai_socktype = SOCK_STREAM;</span><br><span class="line">    hints.ai_flags = AI_PASSIVE; <span class="comment">// 帮我填上我的 IP</span></span><br><span class="line"></span><br><span class="line">    getaddrinfo(<span class="literal">NULL</span>, MYPORT, &amp;hints, &amp;res);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 产生一个 socket， bind socket， 并 listen socket：</span></span><br><span class="line"></span><br><span class="line">    sockfd = socket(res-&gt;ai_family, res-&gt;ai_socktype, res-&gt;ai_protocol);</span><br><span class="line">    bind(sockfd, res-&gt;ai_addr, res-&gt;ai_addrlen);</span><br><span class="line">    <span class="built_in">listen</span>(sockfd, BACKLOG);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 现在接受一个进入的连接：</span></span><br><span class="line"></span><br><span class="line">    addr_size = <span class="keyword">sizeof</span> their_addr;</span><br><span class="line">    new_fd = accept(sockfd, (struct sockaddr *)&amp;their_addr, &amp;addr_size);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 准备好与 new_fd 这个 socket descriptor 进行沟通！</span></span><br><span class="line"></span><br><span class="line">    .</span><br><span class="line">    .</span><br><span class="line">    .</span><br></pre></td></tr></table></figure>

<p>若你只是要取得一个连接，你可以用<code>close()</code>关闭正在 listen 的 sockfd，以避免有更多的连接进入同一个 port， 若你有这个需要的话。</p>
<h2 id="send-与-recv-－-跟我说说话"><a href="#send-与-recv-－-跟我说说话" class="headerlink" title="send() 与 recv() － 跟我说说话"></a>send() 与 recv() － 跟我说说话</h2><p>这两个用来通讯的函数是透过 stream socket 或 connected datagram socket。若你想要使用常规的 unconnected datagram socket，你会需要参考底下的 <code>sendto()</code>及<code>recvfrom()</code>的章节。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 函数原型</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">send</span><span class="params">(<span class="keyword">int</span> sockfd, <span class="keyword">const</span> <span class="keyword">void</span> *msg, <span class="keyword">int</span> len, <span class="keyword">int</span> flags)</span></span>; <span class="comment">// flags设置0就好了</span></span><br></pre></td></tr></table></figure>

<p><code>send()</code>会返回实际有送出的 byte 数目，这可能会少于你所要传送的数目！有时候你告诉<code>send()</code>要送整笔的资料，而它就是无法处理这麽多资料。它只会尽量将资料送出，并认为你之后会再次送出剩下没送出的部分。要记住，如果<code>send()</code>返回的值与 len 的值不符合的话，你就需要再送出字串剩下的部分。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 函数原型</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">recv</span><span class="params">(<span class="keyword">int</span> sockfd, <span class="keyword">void</span> *buf, <span class="keyword">int</span> len, <span class="keyword">int</span> flags)</span></span>;</span><br></pre></td></tr></table></figure>

<p><code>recv()</code>返回实际读到并写入到缓冲区的 byte 数目，而错误时返回 -1。<code>recv()</code>会返回 0，这只能表示一件事情：远端那边已经关闭了你的连接！</p>
<h2 id="sendto-与-recvfrom-－-用-DGRAM-风格跟我说说话"><a href="#sendto-与-recvfrom-－-用-DGRAM-风格跟我说说话" class="headerlink" title="sendto() 与 recvfrom() － 用 DGRAM 风格跟我说说话"></a>sendto() 与 recvfrom() － 用 DGRAM 风格跟我说说话</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 函数原型</span></span><br><span class="line"></span><br><span class="line">sendto(<span class="keyword">int</span> sockfd, <span class="keyword">const</span> <span class="keyword">void</span> *msg, <span class="keyword">int</span> len, <span class="keyword">unsigned</span> <span class="keyword">int</span> flags, <span class="keyword">const</span> struct sockaddr *to, <span class="keyword">socklen_t</span> tolen);</span><br></pre></td></tr></table></figure>

<p><code>tolen</code>是一个 int，可以单纯地将它设置为<code>sizeof *to</code>或<code>sizeof(struct sockaddr_storage)</code>。（<code>sizeof</code>对于变量不用加括号，对于类型需要加括号）</p>
<p>为什麽我们要用<code>struct sockaddr_storage</code>做为 socket 的型别呢？为什麽不用<code>struct sockaddr_in</code>呢？因为我们不想要让自己绑在 IPv4 或 IPv6， 所以我们使用通用的泛型<code>struct sockaddr_storage</code>，我们知道这样有足够的空间可以用在 IPv4 与 IPv6。</p>
<p>记住，如果你<code>connect()</code>到一个 datagram socket，你可以在你全部的交易中只使用<code>send()</code>与<code>recv()</code>。socket本身仍然是 datagram socket，数据包仍然使用 UDP，但是 socket interface 会自动帮你增加目的与来源资料。</p>
<h2 id="close-与-shutdown-－-从我面前消失吧"><a href="#close-与-shutdown-－-从我面前消失吧" class="headerlink" title="close() 与 shutdown() － 从我面前消失吧"></a>close() 与 shutdown() － 从我面前消失吧</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//函数原型</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">close</span>(sockfd);</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">shutdown</span><span class="params">(<span class="keyword">int</span> sockfd, <span class="keyword">int</span> how)</span></span>;</span><br></pre></td></tr></table></figure>

<p>如果你想要能多点控制 socket 如何关闭，可以使用 <code>shutdown()</code>函数。它让你可以切断单向的通信，或者双向（就像是<code>close()</code>所做的）。0 不允许再接收数据；1 不允许再传送数据；2 不允许再传送与接收数据。</p>
<p>重要的是<code>shutdown()</code>实际上没有关闭 file descriptor，它只是改变了它的可用性。如果要释放 socket descriptor， 你还是需要使用<code>close()</code>。</p>
<p>若你在 unconnected datagram socket 上使用<code>shutdown()</code>，它只会单纯的让 socket 无法再进行<code>send()</code>与<code>recv()</code>调用；要记住你只能在有<code>connect()</code>到 datagram socket 的时候使用。</p>
<h2 id="getpeername-－-你是谁"><a href="#getpeername-－-你是谁" class="headerlink" title="getpeername() － 你是谁"></a>getpeername() － 你是谁</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 函数原型</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">getpeername</span><span class="params">(<span class="keyword">int</span> sockfd, struct sockaddr *addr, <span class="keyword">int</span> *addrlen)</span></span>;</span><br></pre></td></tr></table></figure>

<p><code>getpeername()</code>函数会告诉你另一端连接的 stream socket 是谁。一旦你取得了它们的地址，你就可以用 <code>inet_ntop()</code>、<code>getnameinfo()</code>或<code>gethostbyaddr()</code>取得更多的资料。</p>
<h2 id="gethostname-－-我是谁"><a href="#gethostname-－-我是谁" class="headerlink" title="gethostname() － 我是谁"></a>gethostname() － 我是谁</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 函数原型</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">gethostname</span><span class="params">(<span class="keyword">char</span> *hostname, <span class="keyword">size_t</span> <span class="built_in">size</span>)</span></span>;</span><br></pre></td></tr></table></figure>

<p>比<code>getpeername()</code>更简单的函数是<code>gethostname()</code>，它会返回你运行程序的电脑名，这个名称之后可以用在<code>gethostbyname()</code>，用来定义你本地端电脑的 IP 地址。</p>
<p>IPv4的老方法使用<code>gethostbyname()</code>来得到包含域名和IP地址信息的<code>struct hostent</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// !!! 这 是 老 方 法 !!!</span></span><br><span class="line"></span><br><span class="line"><span class="function">struct hostent *<span class="title">gethostbyname</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *name)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">hostent</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">char</span> *h_name;        <span class="comment">/* 主机的官方域名 */</span></span><br><span class="line">    <span class="keyword">char</span> **h_aliases;    <span class="comment">/* 一个以NULL结尾的主机别名数组 */</span></span><br><span class="line">    <span class="keyword">int</span> h_addrtype;      <span class="comment">/* 返回的地址类型，在Internet环境下为AF-INET */</span></span><br><span class="line">    <span class="keyword">int</span> h_length;        <span class="comment">/*地址的字节长度 */</span></span><br><span class="line">    <span class="keyword">char</span> **h_addr_list;  <span class="comment">/* 一个以0结尾的数组，包含该主机的所有地址*/</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> h_addr h_addr_list[0] <span class="comment">/*在h-addr-list中的第一个地址*/</span></span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>网络编程</category>
      </categories>
      <tags>
        <tag>网络编程</tag>
      </tags>
  </entry>
  <entry>
    <title>DynaMix - Dynamic Mobile Device Integration for Efficient Cross-device Resource Sharing 论文笔记</title>
    <url>/2020/04/14/ATC18-DynaMix/</url>
    <content><![CDATA[<h1 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h1><p>跨设备资源共享提供了利用远程设备资源的能力。这样就能通过访问大量异构设备（如智能手机、可穿戴设备、智能电视），扩展移动设备的能力，提供多设备服务。但是现有的资源共享机制面临三个问题：</p>
<ul>
<li><p>共享的资源覆盖范围有限</p>
</li>
<li><p>涉及使用多个设备的复杂编程工作</p>
</li>
<li><p>现有方案无法优化任务的放置，因此在低速无线网络上运行时会遭受损失</p>
</li>
</ul>
<p>文章提出DynaMix，这是一种新颖的框架，可实现有效的跨设备资源共享。</p>
<ul>
<li><p>首先，DynaMix使用分布式共享内存（DSM）和IO请求转发来完全集成各种资源。DSM集成了CPU和内存，而IO请求转发集成了IO资源。</p>
</li>
<li><p>其次，由于DSM和IO请求转发支持在应用程序级别以下的低级资源共享，因此DynaMix不需要应用程序知道多个设备，从而实现了良好的可编程性。</p>
</li>
<li><p>第三，DynaMix通过监视每台设备的资源使用情况和设备间的网络使用情况来最大程度地减少设备间的通信，从而动态地在设备之间重新分配任务； 此外，DynaMix通过监视远程设备的连通性并获取应用程序状态的检查点来支持远程设备的无缝即插即用。</p>
</li>
</ul>
<h1 id="现有方案的局限性"><a href="#现有方案的局限性" class="headerlink" title="现有方案的局限性"></a>现有方案的局限性</h1><h2 id="IO请求转发"><a href="#IO请求转发" class="headerlink" title="IO请求转发"></a>IO请求转发</h2><p>IO转发是一种利用远程IO资源的方式，通过转发IO请求到目的设备，然后目的设备代表请求设备访问请求的资源。请求转发方案可以在不同层（例如，内核，平台，用户）中转发IO请求。</p>
<p>但是，现有请求转发方案的适用性受到如下限制。首先，它们仅支持用于IO资源的共享。接下来，他们需要精心设计的抽象层来支持单设备应用程序。此外，除非他们以优化的任务分配访问资源，否则它们可能会承受严重的网络开销。（将视频解码的工作放到播放设备可以减少网络数据的传输）。</p>
<h1 id="Dynamix-Framework"><a href="#Dynamix-Framework" class="headerlink" title="Dynamix Framework"></a>Dynamix Framework</h1><ul>
<li><p>首先，为了减轻程序员的负担，DynaMix不需要任何特殊的编程概念，也不需要特殊的API，只有基础内存一致性模型除外。程序员可以在具有多线程库的单个设备上编写普通的多线程程序，而无需担心远程资源。</p>
</li>
<li><p>其次，用户可以选择所需的资源以在运行时执行DynaMix应用程序。 DynaMix框架可动态集成所选资源，并构建一个称为“DynaMix设备”的虚拟设备。</p>
</li>
<li><p>第三，DynaMix检测网络流量并自动在设备之间重新分配任务，以最大程度地减少网络开销。在DynaMix设备中，DynaMix应用程序的任务（即线程）可以自由访问远程资源，也可以迁移以实现最佳任务重新分配。</p>
</li>
</ul>
<p>Dynamix Framework 有两个基础操作模型，并需要为每个设备安装两个软件，资源集成器和线程迁移器：</p>
<h2 id="远程资源集成"><a href="#远程资源集成" class="headerlink" title="远程资源集成"></a>远程资源集成</h2><p>安装在每台设备上的资源集成器将共同应用DSM模型和内核级IO请求转发，以集成计算（例如CPU，内存）和IO（例如显示，存储）资源。这种机制使DynaMix能够满足理想的资源共享的两个设计目标：单设备编程模型和高资源覆盖率。</p>
<p>资源集成器分三个步骤执行集成。首先，资源集成器收集由远程资源集成器广播的远程资源的信息（例如，CPU频率，存储器大小，IO类型），并使资源可用于用户应用程序。其次，如果应用程序尝试使用远程资源，则资源集成器会将请求转发给目标资源集成器。第三，目标资源集成器通过共享内存将计算结果交付给应用程序，或者通过转发将IO结果交付给应用程序。</p>
<h2 id="资源感知的任务重新分配"><a href="#资源感知的任务重新分配" class="headerlink" title="资源感知的任务重新分配"></a>资源感知的任务重新分配</h2><p>仅通过IO请求转发，DynaMix会引起严重的设备间通信。 因此，DynaMix通过将线程自适应地迁移到最佳设备来应用资源感知的任务重新分配机制，以最大程度地减少整个设备间的通信量。此机制满足资源感知任务重新分配的设计目标。</p>
<p>资源集成器和线程迁移器共同协作以启用任务重新分配。首先，资源集成器监视每个线程的资源使用情况（例如CPU，网络）以检测可能的资源竞争。 其次，在检测到争用时，线程迁移器权衡各种线程分配方案，并找到最佳方案。 第三，线程迁移器通过将线程的执行上下文传递到目标设备来迁移线程。</p>
<h1 id="Implementation"><a href="#Implementation" class="headerlink" title="Implementation"></a>Implementation</h1><h2 id="资源集成器"><a href="#资源集成器" class="headerlink" title="资源集成器"></a>资源集成器</h2><p>资源集成器由三个组件组成，DSM引擎、IO引擎、设备状态监视器</p>
<h3 id="DSM引擎"><a href="#DSM引擎" class="headerlink" title="DSM引擎"></a>DSM引擎</h3><p>DSM引擎通过DSM的方式，将多个设备的内存区域集成到单个内存空间中。DSM引擎在收到内存访问请求后，要么传递其本地内存数据，要么将请求转发到拥有该数据的目标DSM引擎。它还与主守护程序一起协调这些通信，以实现全局一致的内存管理。</p>
<p>DSM引擎的三种性能优化。首先，它采用延迟释放一致性模型来安全地延迟获取释放块中的内存同步。其次，它在检测到顺序的内存访问模式时主动执行内存预取。第三，它使用页面级一致性块来减少一致性开销。</p>
<h3 id="IO引擎"><a href="#IO引擎" class="headerlink" title="IO引擎"></a>IO引擎</h3><p>IO引擎通过内核级请求转发来管理对本地和远程IO资源的访问。为了将来自主机的请求转发到远程目标设备，I O引擎拦截与IO相关的系统调用（例如，open，read，write，ioctl），并将其与输入参数一起传递给远程设备。然后，远程设备执行转发的请求，并将结果返回给主机。</p>
<p>为了访问远程存储上的文件，IO引擎与主守护程序一起使用，该守护程序保留包含文件元数据的文件目录。因此，加入DynaMix设备的设备应将其文件元数据信息上传到共享文件目录。在收到文件访问请求后，IO引擎首先检查本地文件目录。如果该文件不存在，则IO引擎会要求主守护程序在共享文件目录中找到该位置，并将请求转发给所有者的设备。</p>
<h3 id="设备状态监视器"><a href="#设备状态监视器" class="headerlink" title="设备状态监视器"></a>设备状态监视器</h3><p>设备状态监视器会定期收集各种系统信息（例如，每个线程的CPU利用率，网络阻塞时间）以检测CPU和网络的占用量。当CPU被充分利用但每个线程的CPU利用率较低而又不存在其他瓶颈时（例如，没有IO等待），它可以检测到CPU争用。另一方面，当由于远程IO访问或内存同步而导致的停顿时间超过预定义的阈值（例如，在我们的环境中为30％）时，它会检测到网络争用。在检测到此类更改后，设备状态监视器会立即通知主守护程序启动线程重新分配。</p>
<h2 id="线程迁移器"><a href="#线程迁移器" class="headerlink" title="线程迁移器"></a>线程迁移器</h2><p>线程迁移器由四个组件组成，线程管理器、迁移选择器、迁移引擎、心跳通讯器</p>
<h3 id="线程管理器"><a href="#线程管理器" class="headerlink" title="线程管理器"></a>线程管理器</h3><p>线程管理器维护正在运行的线程的各种信息，例如执行状态，资源使用情况和锁定。在资源争用中，线程管理器计算线程的数据通信（即线程到线程和线程到资源），并将结果发送到迁移选择器，该选择器确定进行迁移的最佳对象及其目标设备。线程管理器还实现了内核级的锁，以使不同设备之间的线程同步。</p>
<h3 id="迁移选择器"><a href="#迁移选择器" class="headerlink" title="迁移选择器"></a>迁移选择器</h3><p>利用线程管理器提供的信息，迁移选择器可以确定迁移的最佳线程及其目标设备，并将信息作为迁移建议通知给主守护程序。</p>
<p>迁移选择器首先将紧密耦合的线程分组为一个线程组，这是最小迁移单元。这种分组简化了选择过程，并防止了不必要的迁移启动。如果线程之间的线程间通信量大于预定义的阈值，则该算法会将线程设置为线程组。接下来，它以使网络开销减少，网络增益最大化的方式找到最佳的线程组及其目标设备。请注意，在特定时间段内，选定的线程组被暂时排除在下一个目标选择中，以避免对同一组进行频繁的迁移调用。使用指数补偿延长时间。</p>
<h3 id="迁移引擎"><a href="#迁移引擎" class="headerlink" title="迁移引擎"></a>迁移引擎</h3><p>在迁移选择器确定了要迁移的线程（即线程组）及其目标设备之后，迁移引擎最终将执行线程迁移。</p>
<p>DynaMix通过采用线程克隆和实时迁移来支持低开销的迁移。通过在短时间内传输基本页面，可以最大程度地减少线程在迁移期间的暂停时间。</p>
<h3 id="心跳通讯器"><a href="#心跳通讯器" class="headerlink" title="心跳通讯器"></a>心跳通讯器</h3><p>心跳通信器定期交换心跳消息，以检查设备连接并共享其资源状，可以从心跳消息的往返延迟估计设备间网络延迟。然后，资源状态信息将传递到迁移选择器。</p>
<p>心跳通讯器可以检测到哪个远程设备加入或离开了DynaMix设备。对于新加入的设备，其心跳通信器会广播心跳消息。接收到消息后，主守护程序会在DynaMix设备中注册新设备。心跳通信器还通过监视连接性（例如，丢包数）来检测不稳定的设备。如果设备变得不稳定，则心跳通信器会通知主守护程序开始将设备中的线程迁移到更稳定的设备，以避免可能导致整体进度损失的线程恢复。如果意外断开连接，主心跳通信器会通知线程管理器从最新检查点（第4.2.1节）中恢复。</p>
<h2 id="Master-Daemon"><a href="#Master-Daemon" class="headerlink" title="Master Daemon"></a>Master Daemon</h2><p>DynaMix设备具有单个主守护程序，该守护程序管理各种系统状态（例如线程，锁，内存页面，文件）以协调DynaMix操作和组件。主守护程序在无故障的主设备（用户发起应用的设备）上运行，由三个组件组成：线程目录，页面目录和文件目录。</p>
<h3 id="线程目录"><a href="#线程目录" class="headerlink" title="线程目录"></a>线程目录</h3><p>线程目录管理线程的全局状态（例如线程位置），并仲裁线程迁移过程。它从设备状态监视器收集资源争用信号，并从迁移选择器收集迁移建议。在收到建议后，线程目录选择最佳的迁移线程组及其目的地，以实现最高的网络收益，然后管理迁移引擎以执行设计的迁移。</p>
<p>线程目录还维护锁的信息（例如，当前所有者，状态）。要获取锁，每个设备都应查询主设备的线程目录。为了减少锁获取的开销，线程目录可以推测性地将锁授予频繁持有锁设备；当另一台设备尝试获取该锁时，线程目录将收回指定的锁。</p>
<h3 id="页面目录"><a href="#页面目录" class="headerlink" title="页面目录"></a>页面目录</h3><p>页面目录管理内存页面的共享状态，以协调内存同步操作。当设备由于缺页而发送远程读取请求时，页面目录将查询一个共享器表，该表保留每个页面的共享器设备列表。 然后，它将请求中继到共享设备之一，共享设备会将页面传递到请求设备。</p>
<h3 id="文件目录"><a href="#文件目录" class="headerlink" title="文件目录"></a>文件目录</h3><p>文件目录为全局一致的文件视图管理文件元数据和共享文件的物理位置。 每当新设备加入DynaMix设备或设备更新元数据时，主守护程序都会更新其文件目录，然后将更新后的信息通知其他设备。</p>
]]></content>
      <categories>
        <category>Paper</category>
      </categories>
      <tags>
        <tag>云计算</tag>
        <tag>调度</tag>
      </tags>
  </entry>
  <entry>
    <title>IX - A Protected Dataplane Operating System for High Throughput and Low Latency 论文笔记</title>
    <url>/2020/04/14/OSDI14-IX/</url>
    <content><![CDATA[<h1 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h1><h2 id="用户态网络栈"><a href="#用户态网络栈" class="headerlink" title="用户态网络栈"></a>用户态网络栈</h2><p>传统观点认为，积极的网络要求，例如小消息的高数据包速率和微秒级的尾端延迟，最好在用户态网络栈中进行处理。用户态网络栈消除了上下文切换的开销，但是并没有消除高数据包速率和低延迟之间的艰难权衡；同时也会缺乏保护，用户程序的bugs会污染网络栈。</p>
<h2 id="IX简介"><a href="#IX简介" class="headerlink" title="IX简介"></a>IX简介</h2><p>IX，一个<code>data plane</code>操作系统，旨在达到高吞吐量，低延迟，强大的保护和资源效率这四个权衡。IX使用硬件虚拟化来分离<code>control plane</code>（内核的管理和调度功能）和<code>data plane</code>（网络处理）。</p>
<p>IX利用Dune和虚拟化硬件以不同的保护级别运行<code>data plane</code>内核和应用程序，并将<code>control plane</code>与<code>data plane</code>隔离。 在我们的实现中，<code>control plane</code>是完整的Linux内核，<code>data plane</code>在专用的硬件线程上作为受保护的基于库的操作系统运行。</p>
<p>这个<code>data plane</code>架构建立在本地的零拷贝API之上，通过将硬件线程和软件队列专用于<code>data plane</code>实例、处理有限的批量数据包至完成（run-to-completion）、消除了一致性流量和多核同步，从而优化了带宽和延迟。</p>
<p>IX API不同于POSIX API，其设计遵循可交换性规则。但是，libix用户级库包含类似于流行的libevent库的基于事件的API，可与多种现有应用程序兼容。</p>
<p>IX演示了通过重新访问网络API并利用现代NIC和多核芯片的优势，我们可以设计出实现高吞吐量和低延迟以及强大的保护和资源效率的系统。它还表明，通过将性能关键型IO功能的较小子集与内核的其余部分分开，我们可以构建与众不同的IO系统，并获得较大的性能提升，同时保持与由OS提供的庞大的API和服务的兼容性。</p>
<p>我们证明了IX在吞吐量和端到端延迟方面均明显优于Linux和最新的用户态网络栈。</p>
<p>IX的两点贡献：通过虚拟化实现保护和直接硬件访问；低延迟和高吞吐量的执行模型。</p>
<h1 id="Implementation"><a href="#Implementation" class="headerlink" title="Implementation"></a>Implementation</h1><p><img src="/images/osdi14-IX-1.png" alt="pic"></p>
<h2 id="Overview"><a href="#Overview" class="headerlink" title="Overview"></a>Overview</h2><p>图1(a)显示了IX架构，重点是<code>control plane</code>和多个<code>data plane</code>之间的分离。</p>
<p>IX的<code>control plane</code>由完整的Linux内核和IXCP（用户级程序）组成。 Linux内核初始化PCIe设备（例如NIC），并提供用于向<code>data plane</code>分配资源的基本机制，包括core，内存和网络队列。同样重要的是，Linux提供了与多种应用程序兼容所必需的系统调用和服务，例如文件系统和信号支持。IXCP监视资源使用情况和<code>data plane</code>性能，并实施资源分配策略。</p>
<p>我们在VMX root ring 0中运行Linux内核，该模式通常用于在虚拟化系统中允许<code>hypervisor</code>。我们在Linux中使用Dune模块，使<code>data plane</code>能够在VMX not-root ring 0中作为特定于应用程序的OS运行，该模式通常用于在虚拟化系统中运行guest内核。像往常一样，应用程序在VMX ring 3中运行。 这种方法为<code>data plane</code>提供了对硬件功能（如页表和异常）的直接访问，以及对NIC的直通访问。此外，它在<code>control plane</code>，<code>data plane</code>和不受信任的应用程序代码之间提供了全面的三向保护。</p>
<p>每个IX的<code>data plane</code>都支持一个多线程应用程序。例如，图1a显示了用于多线程Memcached服务器的一个<code>data plane</code>和用于多线程httpd服务器的另一<code>data plane</code>。<code>control plane</code>以粗粒度方式将资源分配给每个<code>data plane</code>。核心分配是通过实时优先级和cpusets控制的；内存以大量页分配；每个NIC硬件队列都分配给单个<code>data plane</code>。这种方法避免了在要求苛刻的应用程序之间进行细粒度的时间复用的开销和不可预测性。</p>
<h2 id="IX-Dataplane"><a href="#IX-Dataplane" class="headerlink" title="IX Dataplane"></a>IX Dataplane</h2><p>IX的<code>data plane</code>目前由39K SLOC组成，并利用了一些现有的代码库：41％源自Intel NIC设备驱动程序的DPDK变体，26％源自lwIP TCP/IP堆栈，15％来自Dune库。我们没有使用DPDK框架的其余部分，并且为IX高度修改了所有三个代码库。其余的大约是新代码的7K SLOC。 我们选择lwIP作为TCP / IP处理的起点，因为它的模块化和作为RFC兼容，功能丰富的网络堆栈的成熟性。我们为UDP，ARP和ICMP实现了自己的RFC兼容支持。</p>
<p>我们构建了一个名为libix的用户级库，该库抽象了底层API的复杂性。它为旧版应用程序提供了兼容的编程模型，并大大简化了新应用程序的开发。libix当前包括一个与libevent和非阻塞POSIX套接字操作非常类似的接口。它还包括用于零拷贝读取和写入操作的新接口，这些接口效率更高，但需要更改现有应用程序。</p>
<h2 id="run-to-completion-operation"><a href="#run-to-completion-operation" class="headerlink" title="run-to-completion operation"></a>run-to-completion operation</h2><p>每个IX的<code>data plane</code>都作为单个地址空间OS运行，并在共享的用户级地址空间内支持两种线程类型：与IX的<code>data plane</code>交互以启动和使用网络IO的弹性线程；以及背景线程。</p>
<p>图1(b)说明了IX的<code>data plane</code>中弹性线程的run-to-completion操作。 NIC接收映射到服务器的主内存中的缓冲区，并且NIC的接收描述符环中填充了一组缓冲区描述符，允许它使用DMA传输传入的数据包。弹性线程（1）轮询接收描述符环，并可能将新的缓冲区描述符传到NIC，以用于将来的传入数据包。然后，弹性线程（2）通过TCP/IP网络栈处理一定数量的数据包，从而生成事件条件。接下来，线程（3）切换到用户空间应用程序，该应用程序消费所有事件条件。假设传入的数据包包括远程请求，则应用程序将处理这些请求并以一批系统调用作为响应。从用户空间返回控制后，线程（4）处理所有的批量系统调用，尤其是那些直接控制传出TCP/IP通信的调用。线程还（5）运行所有内核计时器，以确保符合TCP行为。最后（6），它将传出的以太网帧放置在NIC的发送描述符环中进行传输，并通知NIC通过更新发送环的尾部寄存器来启动这些帧的DMA传输。在单独的传递中，它还会根据传输环的头部位置释放所有已完成传输的缓冲区，从而有可能生成已发送的事件条件。循环重复此过程，直到没有网络活动为止。在这种情况下，线程将进入沉默状态，该状态涉及超线程友好的轮询，或者可选地进入省电的状态，这是以额外的延迟为代价的。</p>
]]></content>
      <categories>
        <category>Paper</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>网络</tag>
      </tags>
  </entry>
  <entry>
    <title>Pocket - Elastic Ephemeral Storage for Serverless Analytics 论文笔记</title>
    <url>/2020/04/13/OSDI18-Pocket/</url>
    <content><![CDATA[<h1 id="What-is-Serverless"><a href="#What-is-Serverless" class="headerlink" title="What is Serverless?"></a>What is Serverless?</h1><p>Serverless Computing（无服务器计算）是云计算的一种类型。</p>
<h2 id="云计算"><a href="#云计算" class="headerlink" title="云计算"></a>云计算</h2><p>云计算，简单来说就是，通过互联网（云）分发计算服务，用户为使用的服务而付费，有以下四种主要的类型：</p>
<ul>
<li>Infrastructure as a Service (IaaS)</li>
<li>Platform as a Service (PaaS)</li>
<li>Software as a Service (SaaS)</li>
<li>Serverless</li>
</ul>
<h2 id="无服务器计算"><a href="#无服务器计算" class="headerlink" title="无服务器计算"></a>无服务器计算</h2><p>Serverless Computing有以下四个特点：</p>
<ul>
<li><p>免除用户自己管理服务器的麻烦，与服务器有关的概念都由Serverless云架构处理（Serverless，即服务器从你的概念里消失了）</p>
</li>
<li><p>根据需要提供计算资源，自动扩展站点</p>
</li>
<li><p>资源被更加精确地分配，而传统的云计算需要提前分配更多资源以便在需要的时候可用</p>
</li>
<li><p>Serverless会自动适应资源使用的波动（即Elastic），用户只需要为实时使用的资源付费，但是这些的波动会使计费无法预测</p>
</li>
</ul>
<p>关于Serverless更详细的描述可以参考 <a href="https://northstack.com/cloud-computing-vs-serverless/" target="_blank" rel="noopener">What’s the Difference Between Cloud Computing and Serverless?</a></p>
<h1 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h1><h2 id="Serverless与分析负载"><a href="#Serverless与分析负载" class="headerlink" title="Serverless与分析负载"></a>Serverless与分析负载</h2><p>Serverless的弹性和计费优势使其适用于数据密集型应用，比如交互式分析。一些架构在Serverless平台上发起大量细粒度任务，在分析负载中利用所有可用的并行性，并获得接近实时的性能。</p>
<h2 id="在Serverless下的数据共享"><a href="#在Serverless下的数据共享" class="headerlink" title="在Serverless下的数据共享"></a>在Serverless下的数据共享</h2><p>分析负载一般包含多阶段，需要在不同阶段的任务之间共享状态和数据（比如shuffling）。大多数分析框架通过在每个节点上长期运行的架构代理将中间数据缓存在本地，使得不同阶段的任务可以通过网络交换中间数据。</p>
<p>但是，在Serverless部署下，没有架构代理将中间数据缓存在本地，甚至无法控制任务的调度和放置，使得任务之间的通信很困难。所以Serverless应用通过使用远程存储服务（对象存储、数据库、分布式缓存）来进行数据共享。</p>
<p>但是现有的存储服务不适合在Serverless应用中共享短暂中间数据（Ephemeral data）。文件系统、对象存储和NoSQL数据库优先提供可持久的、长期的、高可用的存储，而不是对性能和代价的优化。分布式KV存储提供高性能，但是给了用户管理存储集群规模和配置的负担（涉及选择提供合适的计算、存储和网络资源）。</p>
<p>不同存储技术（如DRAM，NVM，Flash和HDD）增加了找到关于性能和代价的最佳集群配置的复杂度。但是存储技术的选择很关键，因为作业可能表现出不同的存储延迟，带宽和容量要求，并且不同的存储技术在性能特征和成本方面存在很大差异。每种资源配置都会导致不同的性能和成本，为一个工作找到高效的存储分配并非易事，而且对于多个工作来说变得更加复杂。</p>
<h2 id="提出Pocket"><a href="#提出Pocket" class="headerlink" title="提出Pocket"></a>提出Pocket</h2><p>文章提出了Pocket，一个为在Serverless分析负载中高效共享数据而设计的分布式数据存储。Pocket为任意大小数据集、自动资源扩展和跨多存储层的智能数据放置，提供了高吞吐和低延迟。</p>
<p>Pocket的独特属性来自于通过三个平面严格划分责任：</p>
<ul>
<li>control plane —— 为负载决定数据放置策略</li>
<li>metadata plane —— 为管理分布式数据放置</li>
<li>‘dumb’ data plane —— ‘dumb’即忽略元数据，负责存储数据</li>
</ul>
<p>Pocket根据当前负载以细资源和时间粒度独立地缩放所有三个平面。Pocket使用启发式算法，在考虑负载特征的情况下，分配正确的存储介质、容量、带宽和CPU资源，获得高效的代价和性能。存储API公开了简单的I/O操作，以实现亚秒级的访问延迟。我们倾向于Pocket被云提供者管理，提供给使用者按使用计费的代价模型。</p>
]]></content>
      <categories>
        <category>Paper</category>
      </categories>
      <tags>
        <tag>存储</tag>
        <tag>云计算</tag>
        <tag>分布式</tag>
      </tags>
  </entry>
  <entry>
    <title>C++多线程并发系列（三）</title>
    <url>/2020/03/29/Cpp-thread-concurrency-3rd/</url>
    <content><![CDATA[<h1 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h1><p>上一篇中，我们看到各种在线程间保护共享数据的方法。有时候我们不仅想要保护数据，还想在独立的线程间进行同步操作。例如，在第一个线程完成前，可能需要等待另一个线程执行完成。通常情况下，线程会等待一个特定事件发生，或者等待某一条件达成。</p>
<p>尽管可以通过定期检查共享数据中存储的“任务完成”标志或类似内容来执行此操作，但这不是很理想。像这样的线程间同步操作的需求非常普遍，C++标准库以<code>condition variables</code>和<code>future</code>的形式提供了处理它的工具。</p>
<h1 id="等待一个事件或者其他条件"><a href="#等待一个事件或者其他条件" class="headerlink" title="等待一个事件或者其他条件"></a>等待一个事件或者其他条件</h1><p>当一个线程等待另一个线程完成任务时，它可以用很多种选择。第一，它可以持续地检查共享数据标志，直到另一线程完成工作时对这个标志进行重设；这样线程会消耗宝贵的 CPU 时间来持续地检查对应标志，并且当互斥量被等待线程上锁后，其他线程就没有办法获取锁，这样线程就会持续等待。</p>
<p>第二个选择线程使用<code>std::this_thread::sleep_for()</code>在检查之间进行周期性的休眠。这个实现就进步很多，因为当线程休眠时，线程没有浪费执行时间，但是很难确定正确的休眠时间。太短的休眠和没有休眠一样，都会浪费执行时间；太长的休眠时间，可能会让任务等待线程醒来。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">bool</span> flag;</span><br><span class="line"><span class="built_in">std</span>::mutex m;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">wait_for_flag</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="function"><span class="built_in">std</span>::unique_lock&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">lk</span><span class="params">(m)</span></span>;</span><br><span class="line">  <span class="keyword">while</span>(!flag)</span><br><span class="line">  &#123;</span><br><span class="line">    lk.unlock();</span><br><span class="line">    <span class="built_in">std</span>::this_thread::sleep_for(<span class="built_in">std</span>::chrono::milliseconds(<span class="number">100</span>));</span><br><span class="line">    lk.lock();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>第三个选择，也是优先的选择，使用C++标准库提供的工具去等待事件的发生。等待由另一个线程触发的事件的最基础机制就是<code>condition variables</code>（条件变量）。</p>
<h2 id="condition-variables"><a href="#condition-variables" class="headerlink" title="condition variables"></a>condition variables</h2><p>C++标准库对条件变量有两套实现：<code>std::condition_variable</code>和<code>std::condition_variable_any</code>。这两个实现都包含在<code>&lt;condition_variable&gt;</code>头文件的声明中。</p>
<p>两者都需要与一个互斥量一起才能工作（互斥量是为了同步）；前者仅限于与<code>std::mutex</code>一起工作，而后者可以和任何满足最低标准的互斥量一起工作，从而加上了_any的后缀。因为<code>std::condition_variable_any</code>更加通用，这就可能从体积、性能，以及系统资源的使用方面产生额外的开销，所以<code>std::condition_variable</code>一般作为首选的类型，当对灵活性有要求时，我们才会去考虑<code>std::condition_variable_any</code>。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 使用条件变量处理数据等待</span></span><br><span class="line"><span class="built_in">std</span>::mutex mut;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">queue</span>&lt;data_chunk&gt; data_queue;</span><br><span class="line"><span class="built_in">std</span>::condition_variable data_cond;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">data_preparation_thread</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">while</span>(more_data_to_prepare())</span><br><span class="line">  &#123;</span><br><span class="line">    data_chunk <span class="keyword">const</span> data=prepare_data();</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">lk</span><span class="params">(mut)</span></span>;</span><br><span class="line">    data_queue.push(data);</span><br><span class="line">    data_cond.notify_one();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">data_processing_thread</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">while</span>(<span class="literal">true</span>)</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::unique_lock&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">lk</span><span class="params">(mut)</span></span>;</span><br><span class="line">    data_cond.wait(lk, []&#123;<span class="keyword">return</span> !data_queue.empty();&#125;); <span class="comment">// 第二个参数为 false 时，解锁 mutex 并阻塞线程</span></span><br><span class="line">    <span class="comment">// 当收到其他线程 notify_one 时，wait 会被唤醒，重新检查条件</span></span><br><span class="line">    data_chunk data=data_queue.front();</span><br><span class="line">    data_queue.pop();</span><br><span class="line">    lk.unlock();</span><br><span class="line">    <span class="built_in">process</span>(data);</span><br><span class="line">    <span class="keyword">if</span>(is_last_chunk(data))</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对于正在处理数据的线程，这个线程首先对互斥量上锁，这里<code>std::unique_lock</code>要比<code>std::lock_guard</code>更加合适。线程之后会调用<code>std::condition_variable</code>的成员函数<code>wait()</code>，传递一个锁和一个lambda函数表达式(作为等待的条件)。Lambda函数是C++ 11添加的新特性，它可以让一个匿名函数作为其他表达式的一部分，本质是一个函数对象，并且非常合适作为标准函数的谓词。本质上来说，<code>std::condition_variable::wait</code>是对“busy-wait”的一种优化。</p>
<p>很多线程可能等待同一事件，对于通知，他们都需要做出回应。在这种情况下，线程准备好数据时，就会通过条件变量调用<code>notify_all()</code>成员函数，而非直接调用<code>notify_one()</code>函数。</p>
<h1 id="用条件变量实现线程安全的queue"><a href="#用条件变量实现线程安全的queue" class="headerlink" title="用条件变量实现线程安全的queue"></a>用条件变量实现线程安全的queue</h1><p><code>std::queue</code>的接口如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>, <span class="title">class</span> <span class="title">Container</span> = <span class="title">std</span>:</span>:<span class="built_in">deque</span>&lt;T&gt;&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">queue</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="keyword">explicit</span> <span class="title">queue</span><span class="params">(<span class="keyword">const</span> Container&amp;)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">explicit</span> <span class="title">queue</span><span class="params">(Container&amp;&amp;)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">template</span>&lt;class Alloc&gt; <span class="keyword">explicit</span> <span class="title">queue</span><span class="params">(<span class="keyword">const</span> Alloc&amp;)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">template</span>&lt;class Alloc&gt; <span class="keyword">explicit</span> <span class="title">queue</span><span class="params">(<span class="keyword">const</span>  Container&amp;, <span class="keyword">const</span> Alloc&amp;)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">template</span>&lt;class Alloc&gt; <span class="keyword">explicit</span> <span class="title">queue</span><span class="params">(Container&amp;&amp;, <span class="keyword">const</span> Alloc&amp;)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">template</span>&lt;class Alloc&gt; <span class="keyword">explicit</span> <span class="title">queue</span><span class="params">(<span class="keyword">const</span> <span class="built_in">queue</span>&amp;, <span class="keyword">const</span> Alloc&amp;)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">template</span>&lt;class Alloc&gt; <span class="keyword">explicit</span> <span class="title">queue</span><span class="params">(<span class="built_in">queue</span>&amp;&amp;, <span class="keyword">const</span> Alloc&amp;)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="function">T&amp; <span class="title">front</span><span class="params">()</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">const</span> T&amp; <span class="title">front</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">  <span class="function">T&amp; <span class="title">back</span><span class="params">()</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">const</span> T&amp; <span class="title">back</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">bool</span> <span class="title">empty</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">  <span class="function">size_type <span class="title">size</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">swap</span><span class="params">(<span class="built_in">queue</span>&amp;)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">push</span><span class="params">(<span class="keyword">const</span> T&amp;)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">push</span><span class="params">(T&amp;&amp;)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">pop</span><span class="params">()</span></span>;</span><br><span class="line">  <span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span>... <span class="title">Args</span>&gt; <span class="title">void</span> <span class="title">emplace</span>(<span class="title">Args</span>&amp;&amp;... <span class="title">args</span>);</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>和<code>std::stack</code>一样，<code>std::queue</code>的接口设计存在固有的竞争，因此需要将<code>front()</code>和<code>pop()</code>合并成一个函数（就像合并<code>std::stack</code>的<code>top()</code>和<code>pop()</code>）。这里提供了<code>pop()</code>的两个变种，<code>try_pop()</code>总会直接返回（即使没有可弹出的值），<code>wait_and_pop()</code>等待有值可检索才返回，即非阻塞函数和阻塞函数。用之前实现stack的方式实现queue，接口就会像下面这样</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">threadsafe_queue</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  threadsafe_queue();</span><br><span class="line">  threadsafe_queue(<span class="keyword">const</span> threadsafe_queue&amp;);</span><br><span class="line">  threadsafe_queue&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> threadsafe_queue&amp;) = <span class="keyword">delete</span>;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">push</span><span class="params">(T)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">bool</span> <span class="title">try_pop</span><span class="params">(T&amp;)</span></span>; <span class="comment">// 没有可检索的值则返回false</span></span><br><span class="line">  <span class="function"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;T&gt; <span class="title">try_pop</span><span class="params">()</span></span>; <span class="comment">// 直接返回检索值，没有则返回空指针</span></span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">wait_and_pop</span><span class="params">(T&amp;)</span></span>;</span><br><span class="line">  <span class="function"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;T&gt; <span class="title">wait_and_pop</span><span class="params">()</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">bool</span> <span class="title">empty</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>使用条件变量完整实现线程安全的queue</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;memory&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;mutex&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;condition_variable&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;queue&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">threadsafe_queue</span> &#123;</span></span><br><span class="line">  <span class="keyword">mutable</span> <span class="built_in">std</span>::mutex m; <span class="comment">// 必须可变</span></span><br><span class="line">  <span class="built_in">std</span>::<span class="built_in">queue</span>&lt;T&gt; data_queue;</span><br><span class="line">  <span class="built_in">std</span>::condition_variable cv;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  threadsafe_queue() &#123;&#125;</span><br><span class="line">  threadsafe_queue(<span class="keyword">const</span> threadsafe_queue&amp; rhs)</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">l</span><span class="params">(rhs.m)</span></span>;</span><br><span class="line">    data_queue = rhs.data_queue;</span><br><span class="line">  &#125;</span><br><span class="line">  threadsafe_queue&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> threadsafe_queue&amp;) = <span class="keyword">delete</span>;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">push</span><span class="params">(T x)</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">l</span><span class="params">(m)</span></span>;</span><br><span class="line">    data_queue.push(<span class="built_in">std</span>::<span class="built_in">move</span>(x));</span><br><span class="line">    cv.notify_one();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">wait_and_pop</span><span class="params">(T&amp; x)</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::unique_lock&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">l</span><span class="params">(m)</span></span>;</span><br><span class="line">    cv.wait(l, [<span class="keyword">this</span>] &#123; <span class="keyword">return</span> !data_queue.empty(); &#125;);</span><br><span class="line">    x = data_queue.front();</span><br><span class="line">    data_queue.pop();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;T&gt; <span class="title">wait_and_pop</span><span class="params">()</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::unique_lock&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">l</span><span class="params">(m)</span></span>;</span><br><span class="line">    cv.wait(l, [<span class="keyword">this</span>] &#123; <span class="keyword">return</span> !data_queue.empty(); &#125;);</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;T&gt; <span class="title">res</span><span class="params">(<span class="built_in">std</span>::make_shared&lt;T&gt;(data_queue.front()))</span></span>;</span><br><span class="line">    data_queue.pop();</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">bool</span> <span class="title">try_pop</span><span class="params">(T&amp; x)</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">l</span><span class="params">(m)</span></span>;</span><br><span class="line">    <span class="keyword">if</span> (data_queue.empty()) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    x = data_queue.front();</span><br><span class="line">    data_queue.pop();</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;T&gt; <span class="title">try_pop</span><span class="params">()</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">l</span><span class="params">(m)</span></span>;</span><br><span class="line">    <span class="keyword">if</span> (data_queue.empty()) <span class="keyword">return</span> <span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;T&gt;();</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;T&gt; <span class="title">res</span><span class="params">(<span class="built_in">std</span>::make_shared&lt;T&gt;(data_queue.front()))</span></span>;</span><br><span class="line">    data_queue.pop();</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">bool</span> <span class="title">empty</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    <span class="comment">// 其他线程可能有此对象（拷贝构造）所以要上锁</span></span><br><span class="line">    <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">l</span><span class="params">(m)</span></span>;</span><br><span class="line">    <span class="keyword">return</span> data_queue.empty();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h1 id="使用future等待一次性事件"><a href="#使用future等待一次性事件" class="headerlink" title="使用future等待一次性事件"></a>使用future等待一次性事件</h1><p>C++标准库模型将一次性事件称为<code>future</code>(期望值)。当线程需要等待特定的一次性事件时，某种程度上来说就需要知道这个事件在未来的期望结果。之后，这个线程会周期性的等待或检查，事件是否触发；检查期间也会执行其他任务。另外，等待任务期间它可以先执行另外一些任务，直到对应的任务触发，而后等待期望值的状态会变为就绪。一个期望值可能是数据相关的，也可能不是。当事件发生时(并且期望状态为就绪)，并且这个期望值就不能被重置。</p>
<p>C++标准库中，有两种期望值，使用两种类型模板实现，声明在<code>&lt;future&gt;</code>头文件中：<code>unique futures</code>(唯一期望值)(<code>std::future&lt;&gt;</code>)和<code>shared futures</code>(共享期望值)(<code>std::shared_future&lt;&gt;</code>)。<code>std::future</code>的实例只能与一个指定事件相关联，而<code>std::shared_future</code>的实例就能关联多个事件。后者的实现中，所有实例会在同时变为就绪状态，并且他们可以访问与事件相关的任何数据。与数据无关处，可以使用<code>std::future&lt;void&gt;</code>与<code>std::shared_future&lt;void&gt;</code>的特化模板。<strong>期望值对象本身并不提供同步访问</strong>，当多个线程需要访问一个独立期望值对象时，必须使用互斥量或类似同步机制对访问进行保护</p>
<h2 id="后台返回任务值"><a href="#后台返回任务值" class="headerlink" title="后台返回任务值"></a>后台返回任务值</h2><p>最简单的一次性事件就是运行在后台的计算结果，而<code>std::thread</code>不能获取返回值。</p>
<p>当不着急要任务结果时，可以使用<code>std::async</code>启动一个异步任务。与<code>std::thread</code>对象等待的方式（不提供直接接收返回值的机制）不同，<code>std::async</code>会返回一个<code>std::future</code>对象，这个对象持有最终计算出来的结果。当需要这个值时，只需要调用这个对象的get()成员函数；并且会阻塞线程直到期望值状态为就绪为止；之后，返回计算结果。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 使用std::future从异步任务中获取返回值</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;future&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">find_the_answer_to_ltuae</span><span class="params">()</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">do_other_stuff</span><span class="params">()</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="built_in">std</span>::<span class="built_in">future</span>&lt;<span class="keyword">int</span>&gt; the_answer=<span class="built_in">std</span>::async(find_the_answer_to_ltuae);</span><br><span class="line">  do_other_stuff();</span><br><span class="line">  <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;<span class="string">"The answer is "</span>&lt;&lt;the_answer.<span class="built_in">get</span>()&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>与<code>std::thread</code>做的方式一样，<code>std::async</code>允许你通过添加额外的调用参数，向函数传递额外的参数。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 使用std::async向函数传递参数</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;future&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 函数</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">X</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">(<span class="keyword">int</span>,<span class="built_in">std</span>::<span class="built_in">string</span> <span class="keyword">const</span>&amp;)</span></span>;</span><br><span class="line">  <span class="function"><span class="built_in">std</span>::<span class="built_in">string</span> <span class="title">bar</span><span class="params">(<span class="built_in">std</span>::<span class="built_in">string</span> <span class="keyword">const</span>&amp;)</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line">X x;</span><br><span class="line"><span class="keyword">auto</span> f1=<span class="built_in">std</span>::async(&amp;X::foo,&amp;x,<span class="number">42</span>,<span class="string">"hello"</span>);  <span class="comment">// 调用p-&gt;foo(42, "hello")，p是指向x的指针</span></span><br><span class="line"><span class="keyword">auto</span> f2=<span class="built_in">std</span>::async(&amp;X::bar,x,<span class="string">"goodbye"</span>);  <span class="comment">// 调用tmpx.bar("goodbye")， tmpx是x的拷贝副本</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 成员函数</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Y</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">  <span class="function"><span class="keyword">double</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="keyword">double</span>)</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line">Y y;</span><br><span class="line"><span class="keyword">auto</span> f3=<span class="built_in">std</span>::async(Y(),<span class="number">3.141</span>);  <span class="comment">// 调用tmpy(3.141)，tmpy通过Y的移动构造函数得到</span></span><br><span class="line"><span class="keyword">auto</span> f4=<span class="built_in">std</span>::async(<span class="built_in">std</span>::ref(y),<span class="number">2.718</span>);  <span class="comment">// 调用y(2.718)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// std::ref 引用作为参数</span></span><br><span class="line"><span class="function">X <span class="title">baz</span><span class="params">(X&amp;)</span></span>;</span><br><span class="line"><span class="built_in">std</span>::async(baz,<span class="built_in">std</span>::ref(x));  <span class="comment">// 调用baz(x)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 只支持移动的类型</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">move_only</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  move_only();</span><br><span class="line">  move_only(move_only&amp;&amp;)</span><br><span class="line">  move_only(move_only <span class="keyword">const</span>&amp;) = <span class="keyword">delete</span>;</span><br><span class="line">  move_only&amp; <span class="keyword">operator</span>=(move_only&amp;&amp;);</span><br><span class="line">  move_only&amp; <span class="keyword">operator</span>=(move_only <span class="keyword">const</span>&amp;) = <span class="keyword">delete</span>;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">auto</span> f5=<span class="built_in">std</span>::async(move_only());  <span class="comment">// 调用tmp()，tmp是通过std::move(move_only())构造得到</span></span><br></pre></td></tr></table></figure>

<p>默认情况下，<code>std::async</code>启动新线程，还是当future等待时才同步运行任务（不启动新线程），取决于实现。大多数情况下，就是你想要的结果，但是你也可以在函数调用之前向<code>std::async</code>传递一个额外参数。这个参数的类型是<code>std::launch</code>，既可以是<code>std::launch::defered</code>，表明函数调用被延迟到<code>wait()</code>或<code>get()</code>函数调用时才执行；也可以是<code>std::launch::async</code>，表明函数必须在其所在的独立线程上执行；还可以是 <code>std::launch::deferred | std::launch::async</code>，表明实现可以选择这两种方式的一种，这是是默认的选项。如下所示：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> f6=<span class="built_in">std</span>::async(<span class="built_in">std</span>::launch::async,Y(),<span class="number">1.2</span>);  <span class="comment">// 在新线程上执行</span></span><br><span class="line"><span class="keyword">auto</span> f7=<span class="built_in">std</span>::async(<span class="built_in">std</span>::launch::deferred,baz,<span class="built_in">std</span>::ref(x));  <span class="comment">// 在future调用wait()或get()时执行</span></span><br><span class="line"><span class="keyword">auto</span> f8=<span class="built_in">std</span>::async(</span><br><span class="line">              <span class="built_in">std</span>::launch::deferred | <span class="built_in">std</span>::launch::async,</span><br><span class="line">              baz,<span class="built_in">std</span>::ref(x));  <span class="comment">// 实现选择执行方式</span></span><br><span class="line"><span class="keyword">auto</span> f9=<span class="built_in">std</span>::async(baz,<span class="built_in">std</span>::ref(x));</span><br><span class="line">f7.wait();  <span class="comment">//  调用延迟函数</span></span><br></pre></td></tr></table></figure>

<h3 id="创造future的三种方式"><a href="#创造future的三种方式" class="headerlink" title="创造future的三种方式"></a>创造future的三种方式</h3><p><code>std::async</code>不是让<code>std::future</code>与任务实例相关联的唯一方式；你也可以将任务包装入<code>std::packaged_task&lt;&gt;</code>实例中，或通过编写代码的方式，使用<code>std::promise&lt;&gt;</code>类型模板显式设置值。与<code>std::promise&lt;&gt;</code>对比，<code>std::packaged_task&lt;&gt;</code>具有更高层的抽象，所以我们从“高抽象”的模板说起。</p>
<h2 id="任务与future关联"><a href="#任务与future关联" class="headerlink" title="任务与future关联"></a>任务与future关联</h2><p><code>std::packaged_task&lt;&gt;</code>对一个函数或可调用对象，绑定一个期望值。当调用<code>std::packaged_task&lt;&gt;</code>对象时，它就会调用相关函数或可调用对象，将期望状态置为就绪，返回值也会被存储。</p>
<p><code>std::packaged_task&lt;&gt;</code>的模板参数是一个函数签名。函数签名的返回类型可以用来标识从<code>get_future()</code>返回的<code>std::future&lt;&gt;</code>的类型，而函数签名的参数列表，可用来指定packaged_task的函数调用操作符。</p>
<p>由于<code>std::packaged_task</code>对象是一个可调用对象，所以可以封装在<code>std::function</code>对象中，从而作为线程函数传递到<code>std::thread</code>对象中，或作为可调用对象传递另一个函数中，或可以直接进行调用。当<code>std::packaged_task</code>作为一个函数被调用时，实参将由函数调用操作符传递到底层函数，并且返回值作为异步结果存储在<code>std::future</code>，可通过<code>get_future()</code>获取。</p>
<p>因此你可以把用<code>std::packaged_task</code>打包任务，并在它被传到别处之前的适当时机取回期望值。当需要异步任务的返回值时，你可以等待期望的状态变为“就绪”。</p>
<p>很多GUI架构要求用指定线程更新GUI，如果另一个线程要更新GUI，就需要发送信消息给指定线程。使用<code>std::packaged_task</code>即可实现此功能。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;deque&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;mutex&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;future&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;utility&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::mutex m;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">deque</span>&lt;<span class="built_in">std</span>::packaged_task&lt;<span class="keyword">void</span>()&gt; &gt; tasks;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">gui_shutdown_message_received</span><span class="params">()</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">get_and_process_gui_message</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">gui_thread</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">while</span>(!gui_shutdown_message_received())</span><br><span class="line">  &#123;</span><br><span class="line">    get_and_process_gui_message();</span><br><span class="line">    <span class="built_in">std</span>::packaged_task&lt;<span class="keyword">void</span>()&gt; task;</span><br><span class="line">    <span class="comment">// 将lock_guard的作用域限制在这个花括号之间</span></span><br><span class="line">    &#123;</span><br><span class="line">      <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">lk</span><span class="params">(m)</span></span>;</span><br><span class="line">      <span class="keyword">if</span>(tasks.empty())</span><br><span class="line">        <span class="keyword">continue</span>;</span><br><span class="line">      task=<span class="built_in">std</span>::<span class="built_in">move</span>(tasks.front());</span><br><span class="line">      tasks.pop_front();</span><br><span class="line">    &#125;</span><br><span class="line">    task();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="built_in">std</span>::thread <span class="title">gui_bg_thread</span><span class="params">(gui_thread)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> Func&gt;</span><br><span class="line"><span class="function"><span class="built_in">std</span>::<span class="built_in">future</span>&lt;<span class="keyword">void</span>&gt; <span class="title">post_task_for_gui_thread</span><span class="params">(Func f)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="function"><span class="built_in">std</span>::packaged_task&lt;<span class="title">void</span><span class="params">()</span>&gt; <span class="title">task</span><span class="params">(f)</span></span>;</span><br><span class="line">  <span class="built_in">std</span>::<span class="built_in">future</span>&lt;<span class="keyword">void</span>&gt; res=task.get_future();</span><br><span class="line">  <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">lk</span><span class="params">(m)</span></span>;</span><br><span class="line">  tasks.push_back(<span class="built_in">std</span>::<span class="built_in">move</span>(task));</span><br><span class="line">  <span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="使用promises"><a href="#使用promises" class="headerlink" title="使用promises"></a>使用promises</h2><p><code>std::promise&lt;T&gt;</code>提供设定值的方式，这个类型会和后面看到的<code>std::future&lt;T&gt;</code>对象相关联。一对<code>std::promise</code>/<code>std::future</code>提供了一个可行的机制：<code>future</code>可以阻塞等待线程，同时，提供数据的线程可以使用组合中的<code>promise</code>来对相关值进行设置，并将期望值的状态置为“就绪”。</p>
<p>可以通过一个给定的<code>std::promise</code>的<code>get_future()</code>成员函数来获取与之相关的<code>std::future</code>对象，跟<code>std::packaged_task</code>的用法类似。当承诺值已经通过调用<code>set_value()</code>成员函数设置完毕，对应期望值的状态变为“就绪”，并且可用于检索已存储的值。当在设置值之前销毁<code>std::promise</code>，将会存储一个异常。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::promise&lt;<span class="keyword">int</span>&gt; ps;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">future</span>&lt;<span class="keyword">int</span>&gt; ft = ps.get_future();</span><br><span class="line">ps.set_value(<span class="number">42</span>); <span class="comment">// set_value还会将状态设置为就绪</span></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; ft.<span class="built_in">get</span>(); <span class="comment">// 42</span></span><br></pre></td></tr></table></figure>

<p>使用promise解决单线程多连接问题</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;future&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">process_connections</span><span class="params">(connection_set&amp; connections)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">while</span>(!done(connections))</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="keyword">for</span>(connection_iterator</span><br><span class="line">            connection=connections.<span class="built_in">begin</span>(),<span class="built_in">end</span>=connections.<span class="built_in">end</span>();</span><br><span class="line">          connection!=<span class="built_in">end</span>;</span><br><span class="line">          ++connection)</span><br><span class="line">    &#123;</span><br><span class="line">      <span class="keyword">if</span>(connection-&gt;has_incoming_data())</span><br><span class="line">      &#123;</span><br><span class="line">        data_packet data=connection-&gt;incoming();</span><br><span class="line">        <span class="built_in">std</span>::promise&lt;payload_type&gt;&amp; p=</span><br><span class="line">            connection-&gt;get_promise(data.id);</span><br><span class="line">        p.set_value(data.payload);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span>(connection-&gt;has_outgoing_data())</span><br><span class="line">      &#123;</span><br><span class="line">        outgoing_packet data=</span><br><span class="line">            connection-&gt;top_of_outgoing_queue();</span><br><span class="line">        connection-&gt;send(data.payload);</span><br><span class="line">        data.promise.set_value(<span class="literal">true</span>);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="将异常存在future中"><a href="#将异常存在future中" class="headerlink" title="将异常存在future中"></a>将异常存在future中</h2><p>函数作为<code>std::async</code>的一部分时，当调用抛出一个异常时，这个异常就会存储到期望值中，之后期望值的状态被置为“就绪”，之后调用<code>get()</code>会抛出这个已存储异常。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">f</span><span class="params">(<span class="keyword">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (x &lt; <span class="number">0</span>)</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="built_in">std</span>::out_of_range(<span class="string">"x &lt; 0"</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">auto</span> ft = <span class="built_in">std</span>::async(f, <span class="number">-1</span>); <span class="comment">// ft将存储异常</span></span><br><span class="line">  <span class="keyword">int</span> x = ft.<span class="built_in">get</span>(); <span class="comment">// 抛出已存储的异常</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>将函数打包入<code>std::packaged_task</code>任务包中后，到任务被调用时，同样的事情也会发生；打包函数抛出一个异常，这个异常将被存储在期望值中，准备在<code>get()</code>调用时再次抛出。</p>
<p>当然，通过函数的显式调用，<code>std::promise</code>也能提供同样的功能。当存入的是一个异常而非一个数值时，就需要调用<code>set_exception()</code>成员函数，而非<code>set_value()</code>。这通常是用在一个catch块中，并作为算法的一部分，为了捕获异常，使用异常填充承诺值。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="built_in">std</span>::promise&lt;<span class="keyword">double</span>&gt; some_promise;</span><br><span class="line"><span class="keyword">try</span></span><br><span class="line">&#123;</span><br><span class="line">  some_promise.set_value(calculate_value());</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">catch</span>(...)</span><br><span class="line">&#123;</span><br><span class="line">  some_promise.set_exception(<span class="built_in">std</span>::current_exception());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="多个线程等待"><a href="#多个线程等待" class="headerlink" title="多个线程等待"></a>多个线程等待</h2><p>尽管<code>std::future</code>处理了将数据从一个线程传输到另一个线程所需的所有同步，但是对特定<code>std::future</code>实例的成员函数的调用不会彼此同步。如果从多个线程访问单个<code>std::future</code>对象而不进行额外的同步，则将发生数据争用和未定义的行为。这是因为<code>std::future</code>模型独享同步结果的所有权，并且通过调用<code>get()</code>函数，一次性的获取数据，这就让并发访问变的毫无意义，只有一个线程可以获取结果值，因为在第一次调用<code>get()</code>后，就没有值可以再获取了。</p>
<p><code>std::future</code>是只移动的，所以其所有权可以在不同的实例中互相传递，但是只有一个实例可以获得特定的同步结果，而<code>std::shared_future</code>实例是可拷贝的，所以多个对象可以引用同一关联期望值的结果。</p>
<p>每一个<code>std::shared_future</code>的独立对象上，成员函数调用返回的结果还是不同步的，所以为了在多个线程访问一个独立对象时避免数据竞争，必须使用锁来对访问进行保护。优先使用的办法：为了替代只有一个拷贝对象的情况，可以让每个线程都拥有自己对应的拷贝对象；这样，当每个线程都通过自己拥有的<code>std::shared_future</code>对象获取结果，那么多个线程访问共享同步结果就是安全的。</p>
<p><code>std::shared_future</code>的实例同步<code>std::future</code>实例的状态。当<code>std::future</code>对象没有与其他对象共享同步状态所有权，那么所有权必须使用<code>std::move</code>将所有权传递到<code>std::shared_future</code>，其默认构造函数如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 使用 std::move</span></span><br><span class="line"><span class="built_in">std</span>::promise&lt;<span class="keyword">int</span>&gt; p;</span><br><span class="line"><span class="function"><span class="built_in">std</span>::<span class="built_in">future</span>&lt;<span class="keyword">int</span>&gt; <span class="title">f</span><span class="params">(p.get_future())</span></span>;</span><br><span class="line">assert(f.valid());  <span class="comment">// 期望值 f 是合法的</span></span><br><span class="line"><span class="function"><span class="built_in">std</span>::shared_future&lt;<span class="keyword">int</span>&gt; <span class="title">sf</span><span class="params">(<span class="built_in">std</span>::<span class="built_in">move</span>(f))</span></span>;</span><br><span class="line">assert(!f.valid());  <span class="comment">// 期望值 f 现在是不合法的</span></span><br><span class="line">assert(sf.valid());  <span class="comment">// sf 现在是合法的</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 直接构造</span></span><br><span class="line"><span class="built_in">std</span>::promise&lt;<span class="built_in">std</span>::<span class="built_in">string</span>&gt; p;</span><br><span class="line"><span class="function"><span class="built_in">std</span>::shared_future&lt;<span class="built_in">std</span>::<span class="built_in">string</span>&gt; <span class="title">sf</span><span class="params">(p.get_future())</span></span>;  <span class="comment">// 隐式转移所有权</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用 share()</span></span><br><span class="line"><span class="built_in">std</span>::promise&lt;<span class="built_in">std</span>::<span class="built_in">map</span>&lt; SomeIndexType, SomeDataType, SomeComparator,</span><br><span class="line">     SomeAllocator&gt;::iterator&gt; p;</span><br><span class="line"><span class="keyword">auto</span> sf=p.get_future().share(); <span class="comment">// 自动类型推导，使得代码容易修改</span></span><br></pre></td></tr></table></figure>

<h1 id="限定等待时间（timeout）"><a href="#限定等待时间（timeout）" class="headerlink" title="限定等待时间（timeout）"></a>限定等待时间（timeout）</h1><p>由于阻塞调用的时间不确定，在一些情况下需要限制等待时间。指定超时的方式有两种：一是指定一段延迟的时间（duration），另一种是指定一个时间点。</p>
<h2 id="clock（时钟）"><a href="#clock（时钟）" class="headerlink" title="clock（时钟）"></a>clock（时钟）</h2><p>对于标准库来说，时钟就是时间信息源。具体来说，时钟是提供了四种信息的类：</p>
<ul>
<li><p>当前时间<code>std::chrono::system_clock::now()</code></p>
</li>
<li><p>表示时间值的类型<code>std::chrono::time_point</code></p>
</li>
<li><p>时钟的滴答周期，指定为秒的分数，每秒滴答25次的时钟的周期为<code>std::ratio&lt;1,25&gt;</code></p>
</li>
<li><p>如果时钟以统一的速率滴答（无论该速率是否与周期匹配）且无法调整，则称该时钟为稳定时钟。如果时钟稳定，则时钟类的is_steady静态数据成员为true，否则为false。通常，<code>std::chrono::system_clock</code>不稳定，因为可以调整时钟，即使这种调整是自动进行的，也可以考虑本地时钟漂移。由于稳定时钟对于timeout计算很重要，因此 C++ 标准库提供了<code>std::chrono::steady_clock</code>形式的时钟。C++ 标准库提供的其他时钟为<code>std::chrono::system_clock</code>（如上所述），它表示系统的“实时”时钟，并提供用于将其时间点与time_t值进行相互转换的功能，以及<code>std::chrono::high_resolution_clock</code>，它提供了所有库提供的时钟中最小的滴答周期（因此也可能是最大精度）。</p>
</li>
</ul>
<h2 id="std-chrono-duration"><a href="#std-chrono-duration" class="headerlink" title="std::chrono::duration"></a>std::chrono::duration</h2><p>标准库提供了表示时间间隔类型的<code>std::chrono::duration</code>。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 比如将表示秒的类型定义为</span></span><br><span class="line"><span class="built_in">std</span>::duration&lt;<span class="keyword">int</span>&gt; <span class="comment">// 即std::chrono::seconds</span></span><br><span class="line"><span class="comment">// 则表示分的类型可定义为</span></span><br><span class="line"><span class="built_in">std</span>::duration&lt;<span class="keyword">int</span>, <span class="built_in">std</span>::ratio&lt;<span class="number">60</span>&gt;&gt; <span class="comment">// 即std::chrono::minutes</span></span><br><span class="line"><span class="comment">// 表示毫秒的类型可定义为</span></span><br><span class="line"><span class="built_in">std</span>::duration&lt;<span class="keyword">int</span>, <span class="built_in">std</span>::ratio&lt;<span class="number">1</span>, <span class="number">1000</span>&gt;&gt; <span class="comment">// 即std::chrono::milliseconds</span></span><br></pre></td></tr></table></figure>

<p>C++ 14的<code>std::chrono_literals</code>提供了表示时间的后缀名。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>::chrono_literals;</span><br><span class="line"><span class="keyword">auto</span> x = <span class="number">45</span><span class="built_in">min</span>; <span class="comment">// 等价于std::chrono::minutes(45)</span></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; x.count(); <span class="comment">// 45</span></span><br><span class="line"><span class="keyword">auto</span> y = <span class="built_in">std</span>::chrono::duration_cast&lt;<span class="built_in">std</span>::chrono::seconds&gt;(x);</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; y.count(); <span class="comment">// 2700</span></span><br><span class="line"><span class="keyword">auto</span> z = <span class="built_in">std</span>::chrono::duration_cast&lt;<span class="built_in">std</span>::chrono::hours&gt;(x);</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; z.count(); <span class="comment">// 0（转换会截断）</span></span><br></pre></td></tr></table></figure>

<p>标准库通过字面值运算符模板实现此后缀功能。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">constexpr</span> <span class="built_in">std</span>::chrono::minutes <span class="keyword">operator</span> <span class="string">""</span><span class="built_in">min</span>(<span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">long</span> m)</span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">std</span>::chrono::minutes(m);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>duration支持四则运算。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>::chrono_literals;</span><br><span class="line"><span class="keyword">auto</span> x = <span class="number">1</span>h;</span><br><span class="line"><span class="keyword">auto</span> y = <span class="number">15</span><span class="built_in">min</span>;</span><br><span class="line"><span class="keyword">auto</span> z = x - <span class="number">2</span> * y;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; z.count(); <span class="comment">// 30</span></span><br></pre></td></tr></table></figure>

<p>使用duration即可设置等待时间 —— <code>wait_for()</code>。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">f</span><span class="params">()</span></span>;</span><br><span class="line"><span class="keyword">auto</span> ft = <span class="built_in">std</span>::async(f);</span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>::chrono_literals;</span><br><span class="line"><span class="keyword">if</span> (ft.wait_for(<span class="number">1</span>s) == <span class="built_in">std</span>::future_status::<span class="built_in">ready</span>)</span><br><span class="line">&#123;</span><br><span class="line">  <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; ft.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="std-chrono-time-point"><a href="#std-chrono-time-point" class="headerlink" title="std::chrono::time_point"></a>std::chrono::time_point</h2><p><code>std::chrono::time_point</code>是表示时间的类型，值为从某个时间点（比如unix时间戳：1970年1月1日0时0分）开始计时的时间长度。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 第一个模板参数为开始时间点的时钟类型，第二个为时间单位</span></span><br><span class="line"><span class="built_in">std</span>::chrono::time_point&lt;<span class="built_in">std</span>::chrono::system_clock, <span class="built_in">std</span>::chrono::seconds&gt;</span><br></pre></td></tr></table></figure>

<p>time_point可以加减dutation；两个time_point也能相减。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>::chrono_literals;</span><br><span class="line"><span class="keyword">auto</span> x = <span class="built_in">std</span>::chrono::high_resolution_clock::now();</span><br><span class="line"><span class="keyword">auto</span> y = x + <span class="number">1</span>s;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="built_in">std</span>::chrono::duration_cast&lt;<span class="built_in">std</span>::chrono::milliseconds&gt;(y - x).count();</span><br><span class="line"></span><br><span class="line"><span class="keyword">auto</span> start = <span class="built_in">std</span>::chrono::high_resolution_clock::now();</span><br><span class="line">doSomething();</span><br><span class="line"><span class="keyword">auto</span> <span class="built_in">stop</span> = <span class="built_in">std</span>::chrono::high_resolution_clock::now();</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="built_in">std</span>::chrono::duration_cast&lt;<span class="built_in">std</span>::chrono::milliseconds&gt;(<span class="built_in">stop</span> - start).count();</span><br></pre></td></tr></table></figure>

<p>使用绝对的时间点来设置等待时间 —— <code>wait_until()</code>。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::condition_variable cv;</span><br><span class="line"><span class="keyword">bool</span> done;</span><br><span class="line"><span class="built_in">std</span>::mutex m;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">wait_loop</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">const</span> <span class="keyword">auto</span> timeout = <span class="built_in">std</span>::chrono::steady_clock::now() + <span class="built_in">std</span>::chrono::milliseconds(<span class="number">500</span>);</span><br><span class="line">  <span class="function"><span class="built_in">std</span>::unique_lock&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">l</span><span class="params">(m)</span></span>;</span><br><span class="line">  <span class="keyword">while</span> (!done)</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="keyword">if</span> (cv.wait_until(l, timeout) == <span class="built_in">std</span>::cv_status::timeout) <span class="keyword">break</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> done;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="接受timeout的函数"><a href="#接受timeout的函数" class="headerlink" title="接受timeout的函数"></a>接受timeout的函数</h2><p>timeout可以用于休眠，比如<code>std::this_thread::sleep_for</code>和<code>std::this_thread::sleep_until</code>，此外timeout还能配合条件变量、future甚至mutex使用。<code>std::mutex</code>和<code>std::recursive_mutex</code>不支持timeout，而<code>std::timed_mutex</code>和<code>std::recursive_timed_mutex</code>支持，它们提供了<code>try_lock_for()</code>和<code>try_lock_until()</code></p>
<p>支持timeout的函数有：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::this_thread::sleep_for</span><br><span class="line"><span class="built_in">std</span>::this_thread::sleep_until</span><br><span class="line"><span class="built_in">std</span>::condition_variable::wait_for</span><br><span class="line"><span class="built_in">std</span>::condition_variable::wait_until</span><br><span class="line"><span class="built_in">std</span>::condition_variable_any::wait_for</span><br><span class="line"><span class="built_in">std</span>::condition_variable_any::wait_until</span><br><span class="line"><span class="built_in">std</span>::timed_mutex::try_lock_for</span><br><span class="line"><span class="built_in">std</span>::timed_mutex::try_lock_until</span><br><span class="line"><span class="built_in">std</span>::recursive_timed_mutex::try_lock_for</span><br><span class="line"><span class="built_in">std</span>::recursive_timed_mutex::try_lock_until</span><br><span class="line"><span class="built_in">std</span>::unique_lock::try_lock_for</span><br><span class="line"><span class="built_in">std</span>::unique_lock::try_lock_until</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">future</span>::wait_for</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">future</span>::wait_until</span><br><span class="line"><span class="built_in">std</span>::shared_future::wait_for</span><br><span class="line"><span class="built_in">std</span>::shared_future::wait_until</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>并发控制</tag>
      </tags>
  </entry>
  <entry>
    <title>C++多线程并发系列（二）</title>
    <url>/2020/03/22/Cpp-thread-concurrency-2nd/</url>
    <content><![CDATA[<h1 id="同步对象的类型"><a href="#同步对象的类型" class="headerlink" title="同步对象的类型"></a>同步对象的类型</h1><h2 id="Locks"><a href="#Locks" class="headerlink" title="Locks"></a>Locks</h2><p>Lock（锁）是一个抽象概念，锁保护了对某种共享资源的访问，只有拥有锁，才能访问受保护的共享资源。要拥有锁，首先需要某个 lockable 的对象，然后从该对象中获得锁。</p>
<p>锁的概念还有着某种 exclusion（排斥），有时可能无法获得锁的所有权，这个操作会失败（返回一些错误代码或异常，以表明获取锁的尝试失败）或阻塞（直到拥有锁才能返回）。</p>
<h2 id="Mutexes"><a href="#Mutexes" class="headerlink" title="Mutexes"></a>Mutexes</h2><p>Mutex（互斥量，Mutual Exclusion 的缩写），如果不加术语限定，它就是一种 lockable 对象类型，一次只能由一个线程拥有它的锁，只有获得锁的线程才能释放 mutex 上的锁。当 mutex 被锁时，任何获取锁的尝试都将失败或阻塞，即使该尝试是由同一线程发出的。</p>
<h3 id="Recursive-Mutexes"><a href="#Recursive-Mutexes" class="headerlink" title="Recursive Mutexes"></a>Recursive Mutexes</h3><p>Recursive mutex（嵌套互斥量）类似于 mutex，但是一个线程可以同时拥有多个它的锁。线程A可以在不释放已持有的锁的情况下，获取 recursive mutex 上更多的锁；但是线程B不能获得 recursive mutex 上的任何锁，直到线程A持有的所有锁都已释放。</p>
<p>在大多数情况下，recursive mutex 是不可取的，因为它使得代码难以理解。</p>
<h3 id="Reader-Writer-Mutexes"><a href="#Reader-Writer-Mutexes" class="headerlink" title="Reader/Writer Mutexes"></a>Reader/Writer Mutexes</h3><p>Reader/Writer Mutex（读写互斥量），它们提供两种不同的所有权类型：</p>
<ul>
<li><p>exclusive ownership（独占所有权 /写所有权），工作方式类似于普通 mutex 的所有权</p>
</li>
<li><p>shared ownerhip（共享所有权 / 读所有权），共享所有权更宽松，任意数量的线程可以同时获得 mutex 的共享所有权。当任何线程持有共享锁时，任何线程都不能在 mutex 上获得排他锁。</p>
</li>
</ul>
<p>读写互斥量通常用于保护很少更新的共享数据。读取线程在读取数据时将获得共享所有权；当需要修改数据时，修改线程首先获取 mutex 的独占所有权，从而确保没有其他线程正在读取数据，然后在修改完成后释放互斥量的锁。</p>
<h3 id="Spinlocks"><a href="#Spinlocks" class="headerlink" title="Spinlocks"></a>Spinlocks</h3><p>Spinlock（自旋锁）是一种特殊的 mutex，它在必须等待锁时，并不调用操作系统的同步函数，而是会不断循环尝试去获得 mutex 的锁。</p>
<p>如果锁不是经常被持有，或者仅持有很短的时间，那么自旋锁的做法比调用重量级线程同步函数更加有效。</p>
<h2 id="Semaphores"><a href="#Semaphores" class="headerlink" title="Semaphores"></a>Semaphores</h2><p>Semaphore（信号量）是一个非常宽松的 lockable 对象。给定的信号量具有预定义的最大计数和当前计数，通过P操作（减少信号量）来获得信号量的所有权；通过V操作（增加信号量）来释放信号量的所有权。</p>
<p>P操作会减少当前计数，如果计数已经为零，无法减少时，将等线程阻塞，直到另一个线程通过V操作来增加计数。</p>
<p>V操作会增加当前计数，如果在调用之前计数为零，并且有一个线程在等待中被阻塞，则该线程将被唤醒；如果有多个线程在等待，则只会唤醒一个；如果该计数已经达到最大值，则通常会忽略该信号，尽管某些信号量可能会报告错误。</p>
<p>mutex 的所有权和一个线程紧密相关，并且只有获得了 mutex 锁的线程才能释放它；但 semaphore 所有权却更为宽松和短暂，任何线程都可以随时向该semaphore发出信号，无论该线程先前是否在等待 semaphore。</p>
<p><strong>C++ 标准将在 C++ 20 引入 semaphore 类型</strong></p>
<h2 id="Critial-Section"><a href="#Critial-Section" class="headerlink" title="Critial Section"></a>Critial Section</h2><p>Critical Section（临界区）是拥有锁的那段代码，它从获取锁的点开始，到释放锁的点结束。</p>
<h1 id="使用-mutexes-保护共享数据"><a href="#使用-mutexes-保护共享数据" class="headerlink" title="使用 mutexes 保护共享数据"></a>使用 mutexes 保护共享数据</h1><h2 id="条件竞争"><a href="#条件竞争" class="headerlink" title="条件竞争"></a>条件竞争</h2><p><code>invariant</code>（不变量）是对于特定数据结构而言始终正确的陈述。修改线程间共享的数据最简单的潜在问题是不变量被破坏，这些不变量通常在更新期间被破坏。</p>
<p><code>race conditon</code>（竞争条件）是指并发的结果取决于在两个或多个线程上执行操作的相对顺序，错误的执行顺序可能会导致不变量的破坏等问题。<code>data race</code>（数据竞争）是竞争条件的一个具体类型，即由于同时修改单个对象而产生的竞争条件。数据竞争会导致可怕的未定义行为。</p>
<h2 id="C-mutexes-amp-amp-lock-对象"><a href="#C-mutexes-amp-amp-lock-对象" class="headerlink" title="C++ mutexes &amp;&amp; lock 对象"></a>C++ mutexes &amp;&amp; lock 对象</h2><p>C++中通过实例化<code>std::mutex</code>创建互斥量实例，通过成员函数<code>lock()</code>对互斥量上锁，<code>unlock()</code>进行解锁；但是<strong>不推荐</strong>直接去调用成员函数，调用成员函数就意味着，必须在每个函数出口都要去调用unlock()，也包括异常的情况。</p>
<p>C++标准库为 mutex 提供了一个RAII语法的类模板<code>std::lock_guard</code>，在构造时就能锁住互斥量，并在析构的时候进行解锁，从而保证了一个已锁的互斥量能被正确解锁。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 使用 mutex 保护 list</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;list&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;mutex&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">list</span>&lt;<span class="keyword">int</span>&gt; some_list;</span><br><span class="line"><span class="built_in">std</span>::mutex some_mutex;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">add_to_list</span><span class="params">(<span class="keyword">int</span> new_value)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">guard</span><span class="params">(some_mutex)</span></span>;</span><br><span class="line">  some_list.push_back(new_value);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">list_contains</span><span class="params">(<span class="keyword">int</span> value_to_find)</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">guard</span><span class="params">(some_mutex)</span></span>;</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">std</span>::<span class="built_in">find</span>(some_list.<span class="built_in">begin</span>(),some_list.<span class="built_in">end</span>(),value_to_find) != some_list.<span class="built_in">end</span>(); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>C++ 17中添加了一个新特性，模板类型参数推导，类似<code>std::lock_guard</code>这样简单的模板类型的模板参数列表可以省略。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="built_in">std</span>::lock_guard <span class="title">guard</span><span class="params">(some_mutex1)</span></span>;</span><br></pre></td></tr></table></figure>

<p>C++ 17共有六种 mutex 类型：</p>
<ul>
<li><code>std::mutex</code></li>
<li><code>std::timed_mutex</code></li>
<li><code>std::recursive_mutex</code></li>
<li><code>std::recursive_timed_mutex</code></li>
<li><code>std::shared_mutex</code></li>
<li><code>std::shared_timed_mutex</code></li>
</ul>
<p>名称中带有”timed”的变量与没有带的变量基本相同，除了lock操作可以指定超时以限制最大等待时间，且带有”timed”的变量会损失一些性能。其他的 mutex 会在之后介绍。</p>
<p>C++ 17共有三种 lock 的RAII类模版：</p>
<ul>
<li><code>std::scoped_lock</code>（替代了<code>std::lock_guard</code>）</li>
<li><code>std::unique_lock</code></li>
<li><code>std::shared_lock</code></li>
</ul>
<p>为了与各种互斥量类型配合使用，C++标准定义了三个RAII类模板来提供可以持有互斥量锁的对象。它们都可以在构造函数中获取锁，然后在析构函数中释放该锁，但是如果有需要可以通过更复杂的方式使用它们。</p>
<h2 id="用互斥量实现线程安全的stack"><a href="#用互斥量实现线程安全的stack" class="headerlink" title="用互斥量实现线程安全的stack"></a>用互斥量实现线程安全的stack</h2><p><code>std::stack</code>的接口如下</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T,<span class="keyword">typename</span> Container=<span class="built_in">std</span>::<span class="built_in">deque</span>&lt;T&gt;&gt; </span><br><span class="line">class <span class="built_in">stack</span> &#123; </span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="keyword">explicit</span> <span class="built_in">stack</span>(<span class="keyword">const</span> Container&amp;); </span><br><span class="line">  <span class="function"><span class="keyword">explicit</span> <span class="title">stack</span><span class="params">(Container&amp;&amp; = Container())</span></span>; </span><br><span class="line">  <span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">Alloc</span>&gt; <span class="title">explicit</span> <span class="title">stack</span>(<span class="title">const</span> <span class="title">Alloc</span>&amp;);</span> </span><br><span class="line">  <span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">Alloc</span>&gt; <span class="title">stack</span>(<span class="title">const</span> <span class="title">Container</span>&amp;, <span class="title">const</span> <span class="title">Alloc</span>&amp;);</span> </span><br><span class="line">  <span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">Alloc</span>&gt; <span class="title">stack</span>(<span class="title">Container</span>&amp;&amp;, <span class="title">const</span> <span class="title">Alloc</span>&amp;);</span> </span><br><span class="line">  <span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">Alloc</span>&gt; <span class="title">stack</span>(<span class="title">stack</span>&amp;&amp;, <span class="title">const</span> <span class="title">Alloc</span>&amp;);</span> </span><br><span class="line">  <span class="function"><span class="keyword">bool</span> <span class="title">empty</span><span class="params">()</span> <span class="keyword">const</span></span>; </span><br><span class="line">  <span class="function"><span class="keyword">size_t</span> <span class="title">size</span><span class="params">()</span> <span class="keyword">const</span></span>; </span><br><span class="line">  <span class="function">T&amp; <span class="title">top</span><span class="params">()</span></span>; </span><br><span class="line">  <span class="function">T <span class="keyword">const</span>&amp; <span class="title">top</span><span class="params">()</span> <span class="keyword">const</span></span>; </span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">push</span><span class="params">(T <span class="keyword">const</span>&amp;)</span></span>; </span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">push</span><span class="params">(T&amp;&amp;)</span></span>; </span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">pop</span><span class="params">()</span></span>; </span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">swap</span><span class="params">(<span class="built_in">stack</span>&amp;&amp;)</span></span>; </span><br><span class="line">  <span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span>... <span class="title">Args</span>&gt; <span class="title">void</span> <span class="title">emplace</span>(<span class="title">Args</span>&amp;&amp;... <span class="title">args</span>);</span> <span class="comment">// New in C++14</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>即便在很简单的接口中，也可能遇到 race condition</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">stack</span>&lt;<span class="keyword">int</span>&gt; s;</span><br><span class="line"><span class="keyword">if</span> (!s.empty())</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> n = s.top();</span><br><span class="line">    s.pop();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>思考两种把<code>top()</code>和<code>pop()</code>合为一步的方法：</p>
<ol>
<li><p>在调用<code>pop()</code>时传入引用获取结果值；这种方式的明显缺点是，需要重新构造一个栈的元素类型的实例，需要一定开销。</p>
</li>
<li><p><code>pop()</code>返回指向弹出元素的指针，指针可以自由拷贝且不会抛异常（这需要管理对象的内存分配，使用<code>std::shared_ptr</code>是个不错的选择）；但这个方案对于内置类型来说开销太大。</p>
</li>
</ol>
<p>当栈的元素类型为内置类型时，使用方法一，否则使用方法二。下面定义了一个线程安全的栈。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// An class definition for a thread-safe stack</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;exception&gt; </span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;memory&gt; // For std::shared_ptr&lt;&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;mutex&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stack&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">empty_stack</span>:</span> <span class="built_in">std</span>::exception </span><br><span class="line">&#123;</span><br><span class="line">  <span class="function"><span class="keyword">const</span> <span class="keyword">char</span>* <span class="title">what</span><span class="params">()</span> <span class="keyword">const</span> <span class="keyword">noexcept</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"empty stack!"</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;; </span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt; </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">threadsafe_stack</span> </span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">  <span class="built_in">std</span>::<span class="built_in">stack</span>&lt;T&gt; data;</span><br><span class="line">  <span class="keyword">mutable</span> <span class="built_in">std</span>::mutex m;</span><br><span class="line"><span class="keyword">public</span>: </span><br><span class="line">  threadsafe_stack(): data(<span class="built_in">std</span>::<span class="built_in">stack</span>&lt;T&gt;()) &#123;&#125;</span><br><span class="line">  threadsafe_stack(<span class="keyword">const</span> threadsafe_stack&amp; other)</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">lock</span><span class="params">(other.m)</span></span>;</span><br><span class="line">    data = other.data;</span><br><span class="line">  &#125;</span><br><span class="line">  threadsafe_stack&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> threadsafe_stack&amp;) = <span class="keyword">delete</span>; <span class="comment">// Assignment operator is deleted</span></span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">push</span><span class="params">(T new_value)</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">lock</span><span class="params">(m)</span></span>;</span><br><span class="line">    data.push(<span class="built_in">std</span>::<span class="built_in">move</span>(new_value));</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;T&gt; <span class="title">pop</span><span class="params">()</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">lock</span><span class="params">(m)</span></span>;</span><br><span class="line">    <span class="keyword">if</span>(data.empty()) <span class="keyword">throw</span> empty_stack();</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;T&gt; <span class="keyword">const</span> <span class="title">res</span><span class="params">(<span class="built_in">std</span>::make_shared&lt;T&gt;(data.top()))</span></span>; </span><br><span class="line">    data.pop(); </span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">pop</span><span class="params">(T&amp; value)</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">lock</span><span class="params">(m)</span></span>; </span><br><span class="line">    <span class="keyword">if</span>(data.empty()) <span class="keyword">throw</span> empty_stack();</span><br><span class="line">    value=data.top(); </span><br><span class="line">    data.pop();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">bool</span> <span class="title">empty</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">lock</span><span class="params">(m)</span></span>;</span><br><span class="line">    <span class="keyword">return</span> data.empty();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="避免死锁"><a href="#避免死锁" class="headerlink" title="避免死锁"></a>避免死锁</h2><h3 id="std-scoped-lock-或者-std-lock-guard-std-lock"><a href="#std-scoped-lock-或者-std-lock-guard-std-lock" class="headerlink" title="std::scoped_lock 或者 std::lock_guard + std::lock"></a>std::scoped_lock 或者 std::lock_guard + std::lock</h3><p><code>std::lock</code>可以一次性锁住多个互斥量，并且没有副作用(使用避免死锁算法来避免死锁)。（<code>std::scoped_lock</code>提供此函数的 RAII 包装，下面会介绍）</p>
<p><code>std::lock</code>对象通过一系列隐式调用来进行<code>lock()</code>、<code>try_lock()</code>和<code>unlock()</code>操作；如果调用<code>lock()</code>或<code>unlock()</code>导致异常，则在重新抛出之前，对所有被锁的对象调用<code>unlock()</code>。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;mutex&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">some_big_object</span>;</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">swap</span><span class="params">(some_big_object&amp; lhs,some_big_object&amp; rhs)</span></span>;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">X</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">  some_big_object some_detail;</span><br><span class="line">  <span class="built_in">std</span>::mutex m;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  X(some_big_object <span class="keyword">const</span>&amp; sd):some_detail(sd)&#123;&#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 类X的友元函数，定义在类的外部，但有权访问类的所有成员</span></span><br><span class="line">  <span class="function"><span class="keyword">friend</span> <span class="keyword">void</span> <span class="title">swap</span><span class="params">(X&amp; lhs, X&amp; rhs)</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(&amp;lhs==&amp;rhs)</span><br><span class="line">      <span class="keyword">return</span>;</span><br><span class="line">    <span class="built_in">std</span>::lock(lhs.m,rhs.m);</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">lock_a</span><span class="params">(lhs.m,<span class="built_in">std</span>::adopt_lock)</span></span>;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">lock_b</span><span class="params">(rhs.m,<span class="built_in">std</span>::adopt_lock)</span></span>;</span><br><span class="line">    swap(lhs.some_detail,rhs.some_detail);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><code>std::lock_guard</code>的第二个参数<code>std::adopt_lock</code>表示mutex已经被锁住，以及应该使用 mutex 现有锁的 ownership，而不需要在构造器里锁住 mutex。</p>
<p>C++ 17对这种情况提供了支持，<code>std::scoped_lock</code>是一种新的RAII类型模板，与<code>std::lock_guard</code>的功能等价，这个新类型能接受不定数量的互斥量类型作为模板参数，以及相应的互斥量(数量和类型)作为构造参数。互斥量支持构造即上锁，与<code>std::lock</code>的用法相同，其解锁阶段是在析构中进行。<code>std::scoped_lock</code>的好处在于，可以将所有<code>std::lock</code>替换掉，从而减少潜在错误的发生。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// std::scoped_lock 替代 std::lock + std::lock_guard</span></span><br><span class="line"><span class="function"><span class="keyword">friend</span> <span class="keyword">void</span> <span class="title">swap</span><span class="params">(X&amp; lhs, X&amp; rhs)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">if</span>(&amp;lhs==&amp;rhs)</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">  <span class="built_in">std</span>::scoped_lock(lhs.m,rhs.m);</span><br><span class="line">  swap(lhs.some_detail,rhs.some_detail);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="层次锁"><a href="#层次锁" class="headerlink" title="层次锁"></a>层次锁</h3><p>建议是使用层次锁，如果一个锁被低层持有，就不允许再上锁。使用层次锁的实例：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 设定值来表示层级</span></span><br><span class="line"><span class="function">hierarchical_mutex <span class="title">high</span><span class="params">(<span class="number">10000</span>)</span></span>;</span><br><span class="line"><span class="function">hierarchical_mutex <span class="title">mid</span><span class="params">(<span class="number">6000</span>)</span></span>;</span><br><span class="line"><span class="function">hierarchical_mutex <span class="title">low</span><span class="params">(<span class="number">5000</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">lf</span><span class="params">()</span> <span class="comment">// 最低层函数</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="function"><span class="built_in">std</span>::scoped_lock <span class="title">l</span><span class="params">(low)</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">hf</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="function"><span class="built_in">std</span>::scoped_lock <span class="title">l</span><span class="params">(high)</span></span>;</span><br><span class="line">  lf(); <span class="comment">// 可以调用低层函数</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">mf</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="function"><span class="built_in">std</span>::scoped_lock <span class="title">l</span><span class="params">(mid)</span></span>;</span><br><span class="line">  hf(); <span class="comment">// 中层调用了高层函数，违反了层次结构</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//  hierarchical_mutex 的简单实现</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">hierarchical_mutex</span> &#123;</span></span><br><span class="line">  <span class="built_in">std</span>::mutex internal_mutex;</span><br><span class="line">  <span class="keyword">const</span> <span class="keyword">unsigned</span> <span class="keyword">long</span> hierarchy_value; <span class="comment">// 当前层级值</span></span><br><span class="line">  <span class="keyword">unsigned</span> <span class="keyword">long</span> previous_hierarchy_value; <span class="comment">// 前一线程的层级值</span></span><br><span class="line">  <span class="keyword">static</span> <span class="keyword">thread_local</span> <span class="keyword">unsigned</span> <span class="keyword">long</span> this_thread_hierarchy_value; <span class="comment">// 所在线程的层级值，thread_local表示值存在于线程存储期</span></span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">check_for_hierarchy_violation</span><span class="params">()</span> <span class="comment">// 检查是否违反层级结构</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (this_thread_hierarchy_value &lt;= hierarchy_value)</span><br><span class="line">    &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="built_in">std</span>::logic_error(<span class="string">"mutex hierarchy violated"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">update_hierarchy_value</span><span class="params">()</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    <span class="comment">// 先存储当前线程的层级值（用于解锁时恢复）</span></span><br><span class="line">    previous_hierarchy_value = this_thread_hierarchy_value;</span><br><span class="line">    <span class="comment">// 再把其设为锁的层级值</span></span><br><span class="line">    this_thread_hierarchy_value = hierarchy_value;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  explicit hierarchical_mutex(unsigned long value): hierarchy_value(value),  previous_hierarchy_value(0) &#123;&#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    check_for_hierarchy_violation(); <span class="comment">// 要求线程层级值大于锁的层级值</span></span><br><span class="line">    internal_mutex.lock(); <span class="comment">// 内部锁被锁住</span></span><br><span class="line">    update_hierarchy_value(); <span class="comment">// 更新层级值</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">unlock</span><span class="params">()</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (this_thread_hierarchy_value != hierarchy_value)</span><br><span class="line">    &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="built_in">std</span>::logic_error(<span class="string">"mutex hierarchy violated"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 恢复前一线程的层级值</span></span><br><span class="line">    this_thread_hierarchy_value = previous_hierarchy_value;</span><br><span class="line">    internal_mutex.unlock();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">bool</span> <span class="title">try_lock</span><span class="params">()</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    check_for_hierarchy_violation();</span><br><span class="line">    <span class="keyword">if</span> (!internal_mutex.try_lock()) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    update_hierarchy_value();</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 初始化为ULONG_MAX以使构造锁时能通过检查</span></span><br><span class="line"><span class="function"><span class="keyword">thread_local</span> <span class="keyword">unsigned</span> <span class="keyword">long</span>  <span class="title">hierarchical_mutex::this_thread_hierarchy_value</span><span class="params">(ULONG_MAX)</span></span>;</span><br></pre></td></tr></table></figure>

<h2 id="更灵活的锁-std-unique-lock"><a href="#更灵活的锁-std-unique-lock" class="headerlink" title="更灵活的锁 std::unique_lock"></a>更灵活的锁 std::unique_lock</h2><p><code>std::unique_lock</code>比<code>std::lock_guard</code>更灵活，它不必总是拥有与其相关联的互斥量。</p>
<p>可将<code>std::adopt_lock</code>作为第二个参数传入构造函数，对互斥量进行管理；也可以将<code>std::defer_lock</code>作为第二个参数传递进去，表明互斥量应保持<strong>解锁状态</strong>，这样互斥量的锁就可以被<code>std::unique_lock</code>对象的<code>lock()</code>函数所获取，或传递<code>std::unique_lock</code>对象到std::lock()中。</p>
<p>允许<code>std::unique_lock</code>实例不总是拥有互斥量的代价就是是否拥有互斥量的标志被存储，且会被更新。所以<code>std::unique_lock</code>会占用比较多的空间，并且比<code>std::lock_guard</code>稍慢一些。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// std::unique_lock + std::lock 替代 std::lock + std::lock_guard</span></span><br><span class="line"><span class="function"><span class="keyword">friend</span> <span class="keyword">void</span> <span class="title">swap</span><span class="params">(X&amp; lhs, X&amp; rhs)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">if</span>(&amp;lhs==&amp;rhs)</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">  <span class="function"><span class="built_in">std</span>::unique_lock&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">lock_a</span><span class="params">(lhs.m,<span class="built_in">std</span>::defer_lock)</span></span>; </span><br><span class="line">  <span class="function"><span class="built_in">std</span>::unique_lock&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">lock_b</span><span class="params">(rhs.m,<span class="built_in">std</span>::defer_lock)</span></span>;</span><br><span class="line">  <span class="built_in">std</span>::lock(lock_a,lock_b);</span><br><span class="line">  swap(lhs.some_detail,rhs.some_detail);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>由于<code>std::unique_lock</code>支持<code>lock()</code>，<code>try_lock()</code>和<code>unlock()</code>成员函数，所以能将<code>std::unique_lock</code>对象传递到<code>std::lock()</code>。</p>
<p>除非你想将<code>std::unique_lock</code>的所有权进行转让，或是对其做一些其他的事情外，你最好使用C++ 17中提供的<code>std::scoped_lock</code>。</p>
<h3 id="不同域中互斥量所有权的传递"><a href="#不同域中互斥量所有权的传递" class="headerlink" title="不同域中互斥量所有权的传递"></a>不同域中互斥量所有权的传递</h3><p>因为<code>std::unique_lock</code>实例不必与自身相关的互斥量，一个互斥量的所有权可以通过移动操作，在不同的实例中进行传递。<code>std::unique_lock</code>是可移动，但不可赋值的类型。</p>
<p>某些情况下，这种转移是自动发生的，例如：当函数返回一个实例（右值）；另一些情况下，需要显式的调用<code>std::move()</code>来执行移动操作（左值）。从本质上来说，需要依赖于源值是否是左值（一个实际的值或是引用，有变量名）或一个右值（一个临时类型，无变量名）。</p>
<p>一种使用可能是允许一个函数去锁住一个互斥量，并且将所有权移到调用者上，所以调用者可以在这个锁保护的范围内执行额外的动作。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="built_in">std</span>::unique_lock&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">get_lock</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">extern</span> <span class="built_in">std</span>::mutex some_mutex;</span><br><span class="line">  <span class="function"><span class="built_in">std</span>::unique_lock&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">lk</span><span class="params">(some_mutex)</span></span>;</span><br><span class="line">  prepare_data();</span><br><span class="line">  <span class="keyword">return</span> lk;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">process_data</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="function"><span class="built_in">std</span>::unique_lock&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">lk</span><span class="params">(get_lock())</span></span>;</span><br><span class="line">  do_something();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="锁的合理粒度"><a href="#锁的合理粒度" class="headerlink" title="锁的合理粒度"></a>锁的合理粒度</h3><p>锁的粒度是用来描述单个锁保护的数据量大小；选择足够粗的加锁粒度以确保保护所需的数据很重要，而确保仅针对需要加锁的操作才持有锁也很重要。</p>
<p>特别要注意的是，请勿在持有锁时执行任何耗时的活动，例如文件IO。除非该锁旨在保护对文件的访问，否则在持有该锁的同时执行IO会不必要地延迟其他线程（因为它们将在等待获取锁时阻塞），从而可能消除使用多个线程带来的任何性能提升。</p>
<p><code>std::unique_lock</code>在这种情况下效果很好，因为您可以在代码不再需要访问共享数据时调用<code>unlock()</code>，如果以后需要访问代码，则可以再次调用<code>lock()</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">get_and_process_data</span><span class="params">()</span> </span>&#123; </span><br><span class="line">  <span class="function"><span class="built_in">std</span>::unique_lock&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">my_lock</span><span class="params">(the_mutex)</span></span>; </span><br><span class="line">  some_class data_to_process=get_next_data_chunk(); </span><br><span class="line">  my_lock.unlock(); </span><br><span class="line">  result_type result=<span class="built_in">process</span>(data_to_process); </span><br><span class="line">  my_lock.lock(); </span><br><span class="line">  write_result(data_to_process,result); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>假设您正在尝试比较一个简单的数据成员，该成员是一个普通的int。由于int的复制成本很低，因此您可以轻松地复制每个要比较的对象的数据，而只需持有该对象的锁，然后比较复制的值。这意味着您将每个互斥量上的锁保持最短的时间，并且您没有在锁定另一个互斥量的同时持有该锁。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在比较运算符中，一次只锁住一个互斥量</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Y</span> &#123;</span> </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">  <span class="keyword">int</span> some_detail; </span><br><span class="line">  <span class="keyword">mutable</span> <span class="built_in">std</span>::mutex m; </span><br><span class="line">  <span class="function"><span class="keyword">int</span> <span class="title">get_detail</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">lock_a</span><span class="params">(m)</span></span>;</span><br><span class="line">    <span class="keyword">return</span> some_detail; </span><br><span class="line">  &#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  Y(<span class="keyword">int</span> sd): some_detail(sd) &#123;&#125; </span><br><span class="line">  <span class="keyword">friend</span> <span class="keyword">bool</span> <span class="keyword">operator</span>==(Y <span class="keyword">const</span>&amp; lhs, Y <span class="keyword">const</span>&amp; rhs) &#123; </span><br><span class="line">    <span class="keyword">if</span>(&amp;lhs==&amp;rhs) </span><br><span class="line">      <span class="keyword">return</span> <span class="literal">true</span>; </span><br><span class="line">    <span class="keyword">int</span> <span class="keyword">const</span> lhs_value=lhs.get_detail(); </span><br><span class="line">    <span class="keyword">int</span> <span class="keyword">const</span> rhs_value=rhs.get_detail(); </span><br><span class="line">    <span class="keyword">return</span> lhs_value==rhs_value; </span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="保护共享数据的初始化过程"><a href="#保护共享数据的初始化过程" class="headerlink" title="保护共享数据的初始化过程"></a>保护共享数据的初始化过程</h2><p>假设你有一个共享源，构建代价很昂贵（它可能会打开一个数据库连接或分配出很多的内存）。在多线程的情况下，我们要保证共享源只被初始化一次，C++ 标准库提供了<code>std::once_flag</code>和<code>std::call_once</code>来处理这种情况。</p>
<p>比起锁住互斥量并显式的检查共享源，每个线程只需要使用<code>std::call_once</code>就可以，在<code>std::call_once</code>的结束时，就能安全的知道指针已经被其他的线程初始化了。使用<code>std::call_once</code>比显式使用互斥量消耗的资源更少，特别是当初始化完成后。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;some_resource&gt; resource_ptr;</span><br><span class="line"><span class="built_in">std</span>::once_flag resource_flag;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">init_resource</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  resource_ptr.reset(<span class="keyword">new</span> some_resource);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="built_in">std</span>::call_once(resource_flag, init_resource);  <span class="comment">// 可以完整的进行一次初始化</span></span><br><span class="line">  resource_ptr-&gt;do_something();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在类中使用<code>std::call_once</code>，注意<code>std::call_once(flag, &amp;X::func, this);</code>这样的写法。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">X</span> &#123;</span> </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">  connection_info connection_details; </span><br><span class="line">  connection_handle connection; </span><br><span class="line">  <span class="built_in">std</span>::once_flag connection_init_flag; </span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">open_connection</span><span class="params">()</span> </span>&#123; </span><br><span class="line">    connection=connection_manager.<span class="built_in">open</span>(connection_details); </span><br><span class="line">  &#125; </span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  X(connection_info <span class="keyword">const</span>&amp; connection_details_): connection_details(connection_details_) &#123;&#125; </span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">send_data</span><span class="params">(data_packet <span class="keyword">const</span>&amp; data)</span> </span>&#123; </span><br><span class="line">    <span class="built_in">std</span>::call_once(connection_init_flag,&amp;X::open_connection,<span class="keyword">this</span>); </span><br><span class="line">    connection.send_data(data); </span><br><span class="line">  &#125; </span><br><span class="line">  <span class="function">data_packet <span class="title">receive_data</span><span class="params">()</span> </span>&#123; </span><br><span class="line">    <span class="built_in">std</span>::call_once(connection_init_flag,&amp;X::open_connection,<span class="keyword">this</span>); </span><br><span class="line">    <span class="keyword">return</span> connection.receive_data(); </span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>在C++ 11中，<code>static变量的初始化是线程安全的</code>，所以当只有一个全局实例时（单例模式），可以不使用<code>std::call_once</code>而直接用static变量。（C++ 11规定static变量的初始化只发生在一个线程中，直到初始化完成前其他线程都不会做处理，从而避免了条件竞争。）</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">my_class</span>;</span> </span><br><span class="line"></span><br><span class="line"><span class="function">my_class&amp; <span class="title">get_my_class_instance</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">static</span> my_class instance;</span><br><span class="line">  <span class="keyword">return</span> instance; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="std-shared-mutex"><a href="#std-shared-mutex" class="headerlink" title="std::shared_mutex"></a>std::shared_mutex</h2><p>对于不常更新的数据结构，使用<code>std::mutex</code>会影响并发地读取数据，这里需要另一种不同的互斥量，这种互斥量常被称为 reader-writer mutex，因为其允许两种不同的使用方式：一个”writer”线程独占访问，让多个”reader”线程共享并发访问。C++17标准库提供了两种非常好的互斥量，<code>std::shared_mutex</code>和<code>std::shared_timed_mutex</code>，后者比前者提供了更多操作，但前者性能更高。</p>
<p>对于更新操作，可以使用<code>std::lock_guard&lt;std::shared_mutex&gt;</code>和<code>std::unique_lock&lt;std::shared_mutex&gt;</code>上锁，与<code>std::mutex</code>所做的一样，这就能保证更新线程的独占访问。</p>
<p>对于其他不需要去修改数据结构的线程，其可以使用<code>std::shared_lock&lt;std::shared_mutex&gt;</code>获取共享访问。这种RAII类型模板<code>std::shared_lock</code>是在C++ 14中的新特性，这与使用<code>std::unique_lock</code>一样，除了多线程可以同时获取同一个<code>std::shared_mutex</code>上的共享锁之外。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 实现了简单的DNS缓存类</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;map&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;mutex&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;shared_mutex&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">dns_entry</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">dns_cache</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">  <span class="built_in">std</span>::<span class="built_in">map</span>&lt;<span class="built_in">std</span>::<span class="built_in">string</span>,dns_entry&gt; entries;</span><br><span class="line">  <span class="keyword">mutable</span> <span class="built_in">std</span>::shared_mutex entry_mutex;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="function">dns_entry <span class="title">find_entry</span><span class="params">(<span class="built_in">std</span>::<span class="built_in">string</span> <span class="keyword">const</span>&amp; domain)</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::shared_lock&lt;<span class="built_in">std</span>::shared_mutex&gt; <span class="title">lk</span><span class="params">(entry_mutex)</span></span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">map</span>&lt;<span class="built_in">std</span>::<span class="built_in">string</span>,dns_entry&gt;::const_iterator <span class="keyword">const</span> it=</span><br><span class="line">       entries.<span class="built_in">find</span>(domain);</span><br><span class="line">    <span class="keyword">return</span> (it==entries.<span class="built_in">end</span>())?dns_entry():it-&gt;second;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">update_or_add_entry</span><span class="params">(<span class="built_in">std</span>::<span class="built_in">string</span> <span class="keyword">const</span>&amp; domain,</span></span></span><br><span class="line"><span class="function"><span class="params">                           dns_entry <span class="keyword">const</span>&amp; dns_details)</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::shared_mutex&gt; <span class="title">lk</span><span class="params">(entry_mutex)</span></span>;</span><br><span class="line">    entries[domain]=dns_details;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>在类中将成员函数修饰为const表明在该函数体内，不能修改对象的数据成员而且不能调用非const函数。<code>mutable</code>修饰词是为了突破const函数的限制而设置的。</p>
<h2 id="std-recursive-mutex"><a href="#std-recursive-mutex" class="headerlink" title="std::recursive_mutex"></a>std::recursive_mutex</h2><p>C++标准库提供了<code>std::recursive_mutex</code>类以支持嵌套互斥量。除了可以对同一线程的单个实例上获取多个锁，其他功能与<code>std::mutex</code>相同。互斥量锁住其他线程前，必须释放拥有的所有锁，所以当调用<code>lock()</code>三次后，也必须调用<code>unlock()</code>三次。</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>并发控制</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 78. 子集</title>
    <url>/2020/03/18/LeetCode-78/</url>
    <content><![CDATA[<h2 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h2><p><a href="https://leetcode-cn.com/problems/subsets/" target="_blank" rel="noopener">子集</a></p>
<p>给定一组不含重复元素的整数数组 nums，返回该数组所有可能的子集。(解集不能包含重复的子集。)</p>
<p>示例：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">输入: nums &#x3D; [1,2,3]</span><br><span class="line">输出:</span><br><span class="line">[</span><br><span class="line">  [3],</span><br><span class="line">  [1],</span><br><span class="line">  [2],</span><br><span class="line">  [1,2,3],</span><br><span class="line">  [1,3],</span><br><span class="line">  [2,3],</span><br><span class="line">  [1,2],</span><br><span class="line">  []</span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<h2 id="解法一（递归）"><a href="#解法一（递归）" class="headerlink" title="解法一（递归）"></a>解法一（递归）</h2><p>观察到输出集合从空集开始，依次遍历nums内的数值，每遍历一个数值，就往输出集合中加入原来的所有子集中添加当前遍历到的数值。例如，遍历到nums内的数值2，输出集合原来的子集有[]、[1]，需要往集合中添加[2]、[1,2]。使用递归实现这一过程。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> &#123;</span></span><br><span class="line"> <span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="built_in">vector</span>&lt;<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&gt; <span class="title">subsets</span><span class="params">(<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&amp; nums)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">vector</span>&lt;<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&gt; ans;</span><br><span class="line">    ans.push_back(<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; &#123;&#125;);</span><br><span class="line">    subsetsCore(nums, ans, <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">return</span> ans;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">subsetsCore</span><span class="params">(<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&amp; nums, <span class="built_in">vector</span>&lt;<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&gt;&amp; ans, <span class="keyword">int</span> level)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (level &gt; nums.<span class="built_in">size</span>())</span><br><span class="line">      <span class="keyword">return</span>;</span><br><span class="line">    <span class="keyword">int</span> <span class="built_in">size</span> = ans.<span class="built_in">size</span>();</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="built_in">size</span>; i++) &#123;</span><br><span class="line">      <span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; tmp = ans[i];</span><br><span class="line">      tmp.push_back(nums[level - <span class="number">1</span>]);</span><br><span class="line">      ans.push_back(tmp);</span><br><span class="line">    &#125;</span><br><span class="line">    subsetsCore(nums, ans, level + <span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="解法二（回溯）"><a href="#解法二（回溯）" class="headerlink" title="解法二（回溯）"></a>解法二（回溯）</h2><p>输出集合从空集开始，回溯先从nums的第一个数值1开始，依次得到[1]、[1,2]、[1,2,3]、[1,3]，然后再从nums的第二个、第三个数值开始，直到回溯结束。</p>
<p><strong>回溯算法模版</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">result = []</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">backtrack</span><span class="params">(路径, 选择列表)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> 满足结束条件:</span><br><span class="line">        result.add(路径)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    <span class="keyword">for</span> 选择 <span class="keyword">in</span> 选择列表:</span><br><span class="line">        做选择</span><br><span class="line">        backtrack(路径, 选择列表)</span><br><span class="line">        撤销选择</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> &#123;</span></span><br><span class="line"> <span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="built_in">vector</span>&lt;<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&gt; <span class="title">subsets</span><span class="params">(<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&amp; nums)</span> </span>&#123;</span><br><span class="line">    backtrace(nums, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">return</span> ans;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">backtrace</span><span class="params">(<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&amp; nums, <span class="keyword">int</span> pos)</span> </span>&#123;</span><br><span class="line">    ans.push_back(path);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = pos; i &lt; nums.<span class="built_in">size</span>(); i++) &#123;</span><br><span class="line">      path.push_back(nums[i]);</span><br><span class="line">      backtrace(nums, i + <span class="number">1</span>);</span><br><span class="line">      path.pop_back();</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"> <span class="keyword">private</span>:</span><br><span class="line">  <span class="built_in">vector</span>&lt;<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&gt; ans;</span><br><span class="line">  <span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; path &#123;&#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="解法三（位运算）"><a href="#解法三（位运算）" class="headerlink" title="解法三（位运算）"></a>解法三（位运算）</h2><p>子集的所有情况其实可以用n位的二进制（n 为 <code>nums.size()</code>），例如上面的例子可以用3位二进制表示所有情况，000、001、010、011、100、101、110、111。不过如果nums的size大于一定数值时会溢出（例如int类型不能超过32位），可以使用bitset。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> &#123;</span></span><br><span class="line"> <span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="built_in">vector</span>&lt;<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&gt; <span class="title">subsets</span><span class="params">(<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&amp; nums)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">vector</span>&lt;<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&gt; ans;</span><br><span class="line">    <span class="keyword">int</span> N = <span class="number">1</span> &lt;&lt; nums.<span class="built_in">size</span>();</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; N; i++) &#123;</span><br><span class="line">      <span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; tmp;</span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; nums.<span class="built_in">size</span>(); j++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (i &amp; (<span class="number">1</span> &lt;&lt; j)) &#123;</span><br><span class="line">          tmp.push_back(nums[j]);</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      ans.push_back(tmp);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> ans;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>算法</category>
      </categories>
      <tags>
        <tag>LeetCode</tag>
        <tag>ALT子集类型</tag>
        <tag>AL递归</tag>
        <tag>AL回溯</tag>
        <tag>AL位运算</tag>
      </tags>
  </entry>
  <entry>
    <title>C++多线程并发系列（一）</title>
    <url>/2020/03/16/Cpp-thread-concurrency-1st/</url>
    <content><![CDATA[<h1 id="一些概念"><a href="#一些概念" class="headerlink" title="一些概念"></a>一些概念</h1><p>C++标准本身不支持IPC，所以应用需要依赖具体平台的API来实现IPC。本文不涉及IPC，想要了解IPC这方面内容可以参考特定操作系统的实现，比如<a href="https://book.douban.com/subject/4118577/" target="_blank" rel="noopener">《UNIX》网络编程 卷二</a>。</p>
<h2 id="并行-amp-并发："><a href="#并行-amp-并发：" class="headerlink" title="并行 &amp; 并发："></a>并行 &amp; 并发：</h2><p>一般解释都是，并发是指宏观上在一段时间内能同时运行多个程序，而并行则指同一时刻能运行多个指令。下面给出一个从目的角度出发的解释：</p>
<p>并行的主要目的是在硬件支持的基础上，来提升处理大量数据的性能，用来解决一个单一的任务。</p>
<p>并发的主要目的是 1. 将无关的代码分离，使程序更容易理解和测试 2. 提高任务响应速度。</p>
<h2 id="任务并行-amp-数据并行"><a href="#任务并行-amp-数据并行" class="headerlink" title="任务并行 &amp; 数据并行"></a>任务并行 &amp; 数据并行</h2><p>任务并行：将单个任务划分成多个任务，同时运行，减少整体运行时间，任务之间可能存在许多依赖关系。</p>
<p>数据并行：将数据划分成多个不同的部分，每个线程同时对不同的数据部分执行相同的操作。</p>
<h1 id="线程管理"><a href="#线程管理" class="headerlink" title="线程管理"></a>线程管理</h1><p>线程使得程序能在数个处理器核心同时执行，定义于头文件<code>&lt;thread&gt;</code></p>
<p><code>&lt;thread&gt;</code>主要内容：定义了一个类<code>std::thread</code>；定义了命名空间<code>std::this_thread</code>及其内部的辅助函数。更多内容详见 <a href="https://zh.cppreference.com/w/cpp/header/thread" target="_blank" rel="noopener">cppreference</a>。</p>
<h2 id="线程基础"><a href="#线程基础" class="headerlink" title="线程基础"></a>线程基础</h2><h3 id="启动线程"><a href="#启动线程" class="headerlink" title="启动线程"></a>启动线程</h3><p><code>std::thread</code> 构造函数</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="built_in">std</span>::thread <span class="title">my_thread1</span><span class="params">(func_name)</span></span>; <span class="comment">// 将函数名作为参数</span></span><br><span class="line"><span class="function"><span class="built_in">std</span>::thread <span class="title">my_thread2</span><span class="params">(func_obj)</span></span>; <span class="comment">// 将函数对象作为参数</span></span><br></pre></td></tr></table></figure>

<p>注意：当将<code>函数对象</code>作为参数传递时，如果传递了一个临时变量，而不是一个命名的变量，那么C++编译器会将其解析为函数声明，而不是类型对象的定义。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="built_in">std</span>::thread <span class="title">my_thread</span><span class="params">(class_name())</span></span>; <span class="comment">// 将类的临时变量作为参数，会被错误解析成函数声明</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 解决方法</span></span><br><span class="line"><span class="function"><span class="built_in">std</span>::thread <span class="title">my_thread1</span><span class="params">((class_name()))</span></span>; <span class="comment">//多加一层括号</span></span><br><span class="line"><span class="built_in">std</span>::thread my_thread2 &#123;class_name()&#125;; <span class="comment">//使用新统一的初始化语法</span></span><br></pre></td></tr></table></figure>

<p>非常推荐使用上面的第二种解决方法，C++11 的<code>花括号初始化</code>语法（花括号初始化不会允许 narrowing conversions，即会丢失信息的转换，例如int转char），下面的例子都将用 {} 初始化。</p>
<p>使用<code>lambda表达式</code>也能避免这个问题。lambda表达式是 C++11 的一个新特性，它使用一个可以捕获局部变量的局部函数来生成一个函数对象。</p>
<p><strong>注意：<code>std::thread</code>的对象是线程句柄，可能处于不表示任何线程的状态（默认构造、被移动、join 或 detach 后），并且执行线程可能与任何 thread 对象无关（detach 后）</strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::thread my_thread1; <span class="comment">// 默认构造，不表示任何线程</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::thread my_thread2 &#123;func_name&#125;;</span><br><span class="line"><span class="built_in">std</span>::thread my_thread3 &#123;my_thread2&#125;; <span class="comment">// my_thread2 被移动给 my_thread3，不再表示任何线程</span></span><br></pre></td></tr></table></figure>

<p>没有两个<code>std::thread</code>对象会表示同一执行线程,所以<code>std::thread</code>的对象是可移动的 (movable)，但不是可复制的 (copyable)。</p>
<h3 id="join-amp-detach"><a href="#join-amp-detach" class="headerlink" title="join &amp; detach"></a>join &amp; detach</h3><p>在启动线程之后，需要明确是 <code>join</code>（等待线程结束）还是 <code>detach</code>（分离线程与<code>std::thread</code>对象）；如果在<code>std::thread</code>对象被销毁之前还没有做出决定，程序会被终止（<code>std::thread</code>的析构函数会调用<code>std::terminate()</code>）。因此，要保证即使有异常存在，也需要确保线程能够正确的 join 或 detech。</p>
<h4 id="join"><a href="#join" class="headerlink" title="join"></a>join</h4><p>调用join()的行为，清理了线程相关的存储部分，这样std::thread对象将不再与已经完成的线程有任何关联。这意味着，只能对一个线程使用一次join()；一旦已经使用过join()，std::thread对象就不能再次join了，当对其使用joinable()时，将返回false。</p>
<p>考虑到对线程使用join()之前，调用的函数可能会产生异常而终止，有两种方式可以保证join()在调用函数退出前被执行：</p>
<ul>
<li><p><code>try / catch</code>语句块捕获异常；</p>
</li>
<li><p>使用<code>RAII</code>(在构造函数内获取资源，在析构函数内释放资源)，提供一个类，在析构函数中使用join()。</p>
</li>
</ul>
<h4 id="detach"><a href="#detach" class="headerlink" title="detach"></a>detach</h4><p>使用detach()会让线程在后台运行，这就意味着主线程不能与之产生直接交互。C++运行库保证，当线程退出时，相关资源的能够正确回收，后台线程的归属和控制由C++运行库来处理。通常称 detach 后的线程为<code>守护线程</code>(daemon threads)。</p>
<p>如果要 detach 线程，就必须保证线程结束之前，可访问的数据的有效性，避免产生<code>未定义行为</code>。这种情况很可能发生在线程还没结束，函数已经退出的时候，这时线程函数还持有函数局部变量的指针或引用。</p>
<h2 id="向线程函数传参"><a href="#向线程函数传参" class="headerlink" title="向线程函数传参"></a>向线程函数传参</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">func</span><span class="params">(<span class="keyword">int</span> i, <span class="built_in">std</span>::<span class="built_in">string</span> <span class="keyword">const</span>&amp; s)</span></span>; </span><br><span class="line"><span class="built_in">std</span>::thread t &#123;func, <span class="number">3</span>, <span class="string">"hello"</span>&#125;;</span><br></pre></td></tr></table></figure>

<p>使用带有数据成员的函数对象类似于给函数传递参数。<code>std::string const&amp;</code>和<code>const std::string&amp;</code>没有区别。</p>
<p>但请务必记住，默认情况下，参数会复制到内部存储中，新创建的执行线程可以在其中访问它们，然后将它们作为右值传递给可调用对象或函数，就像它们是临时的一样。 即使函数中的相应参数需要引用，也会执行此操作。</p>
<h4 id="案例一"><a href="#案例一" class="headerlink" title="案例一"></a>案例一</h4><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">func</span><span class="params">(<span class="keyword">int</span> i, <span class="built_in">std</span>::<span class="built_in">string</span> <span class="keyword">const</span>&amp; s)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">oops</span><span class="params">(<span class="keyword">int</span> some_param)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">char</span> <span class="built_in">buffer</span>[<span class="number">1024</span>];</span><br><span class="line">  <span class="built_in">sprintf</span>(<span class="built_in">buffer</span>, <span class="string">"%i"</span>, some_param);</span><br><span class="line">  <span class="built_in">std</span>::thread t &#123;func, <span class="number">3</span>, <span class="built_in">buffer</span>&#125;; <span class="comment">// 可能导致未定义行为</span></span><br><span class="line">  t.<span class="built_in">detach</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上述程序在传递参数时，将字符串的字面值（即buffer，char const * 类型）传给线程函数的第二个参数，之后，在线程的上下文中需要完成字面值向 std::string 对象的转化。如果函数 oops 在 buffer 转换成 std::string 对象之前结束，则会导致一些未定义行为。</p>
<p>解决方案是在传递到std::thread构造函数之前就将字面值转化为std::string对象。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::thread t &#123;func, <span class="number">3</span>, <span class="built_in">std</span>::<span class="built_in">string</span>(<span class="built_in">buffer</span>)&#125;;</span><br></pre></td></tr></table></figure>

<h4 id="案例二"><a href="#案例二" class="headerlink" title="案例二"></a>案例二</h4><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">update_data</span><span class="params">(<span class="keyword">int</span> id, <span class="built_in">std</span>::<span class="built_in">string</span>&amp; data)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">oops</span><span class="params">(<span class="keyword">int</span> id)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">string</span> data;</span><br><span class="line">    <span class="built_in">std</span>:thread t &#123;update_data, id, data&#125;; <span class="comment">// 会导致编译失败</span></span><br><span class="line">    t.join();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>std::thread</code>的构造函数无视线程函数期待的<code>非常量引用</code>参数类型，并盲目地拷贝已提供的变量，会导致编译失败。</p>
<p>解决方案是使用<code>std::ref</code>将参数转换成引用的形式。这和<code>std::bind</code>（预先把指定可调用对象的某些参数绑定到已有的变量，产生一个新的可调用对象，有点类似默认参数）一样，<code>std::thread</code>构造函数和<code>std::bind</code>的操作在标准库中以相同的机制进行定义.</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::thread t &#123;update_data, id, <span class="built_in">std</span>::ref(data)&#125;;</span><br></pre></td></tr></table></figure>

<h4 id="案例三"><a href="#案例三" class="headerlink" title="案例三"></a>案例三</h4><p>可以传递一个成员函数指针作为线程函数，并提供一个合适的对象指针作为成员函数的第一个参数</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">X</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">func</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line">X my_x;</span><br><span class="line"><span class="built_in">std</span>::thread t &#123;&amp;X::do_lengthy_work, &amp;my_x&#125;;</span><br></pre></td></tr></table></figure>

<h4 id="案例四"><a href="#案例四" class="headerlink" title="案例四"></a>案例四</h4><p>线程函数的参数只能支持移动，不支持拷贝的情况，使用<code>std::move</code>将对象的所有权转移到线程函数中。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">process_data</span><span class="params">(<span class="built_in">std</span>::<span class="built_in">unique_ptr</span>&lt;data&gt;)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">unique_ptr</span>&lt;data&gt; p &#123;<span class="keyword">new</span> data&#125;;</span><br><span class="line"><span class="built_in">std</span>::thread t &#123;process_data, <span class="built_in">std</span>::<span class="built_in">move</span>(p)&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="运行时决定线程数量"><a href="#运行时决定线程数量" class="headerlink" title="运行时决定线程数量"></a>运行时决定线程数量</h2><p><code>std::thread::hardware_concurrency()</code>这个函数返回能并发的线程数量，多核系统中，返回值可以是CPU核芯的数量。返回值也仅仅是一个提示，当系统信息无法获取时，函数也会返回0。</p>
<p>下面是一个并行版的求和算法</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> Iterator,<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">accumulate_block</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(Iterator first,Iterator last,T&amp; result)</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">    result=<span class="built_in">std</span>::accumulate(first,last,result);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> Iterator,<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function">T <span class="title">parallel_accumulate</span><span class="params">(Iterator first,Iterator last,T init)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">const</span> length=<span class="built_in">std</span>::distance(first,last);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span>(!length)</span><br><span class="line">    <span class="keyword">return</span> init;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">const</span> min_per_thread=<span class="number">25</span>;</span><br><span class="line">  <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">const</span> max_threads=</span><br><span class="line">      (length+min_per_thread<span class="number">-1</span>)/min_per_thread;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">const</span> hardware_threads=</span><br><span class="line">      <span class="built_in">std</span>::thread::hardware_concurrency();</span><br><span class="line"></span><br><span class="line">  <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">const</span> num_threads=</span><br><span class="line">      <span class="built_in">std</span>::<span class="built_in">min</span>(hardware_threads != <span class="number">0</span> ? hardware_threads : <span class="number">2</span>, max_threads);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">const</span> block_size=length/num_threads;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="built_in">std</span>::<span class="built_in">vector</span>&lt;T&gt; <span class="title">results</span><span class="params">(num_threads)</span></span>;</span><br><span class="line">  <span class="function"><span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="built_in">std</span>::thread&gt; <span class="title">threads</span><span class="params">(num_threads<span class="number">-1</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">  Iterator block_start=first;</span><br><span class="line">  <span class="keyword">for</span>(<span class="keyword">unsigned</span> <span class="keyword">long</span> i=<span class="number">0</span>; i &lt; (num_threads<span class="number">-1</span>); ++i)</span><br><span class="line">  &#123;</span><br><span class="line">    Iterator block_end=block_start;</span><br><span class="line">    <span class="built_in">std</span>::advance(block_end,block_size);</span><br><span class="line">    threads[i]=<span class="built_in">std</span>::thread(</span><br><span class="line">        accumulate_block&lt;Iterator,T&gt;(),</span><br><span class="line">        block_start,block_end,<span class="built_in">std</span>::ref(results[i]));</span><br><span class="line">    block_start=block_end;</span><br><span class="line">  &#125;</span><br><span class="line">  accumulate_block&lt;Iterator,T&gt;()(</span><br><span class="line">      block_start,last,results[num_threads<span class="number">-1</span>]);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">auto</span>&amp; entry : threads)</span><br><span class="line">    entry.join();</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">std</span>::accumulate(results.<span class="built_in">begin</span>(),results.<span class="built_in">end</span>(),init);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其中决定线程数量的代码如下</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">const</span> min_per_thread = <span class="number">25</span>;</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">const</span> max_threads = (length+min_per_thread<span class="number">-1</span>) / min_per_thread;</span><br><span class="line"></span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">const</span> hardware_threads = <span class="built_in">std</span>::thread::hardware_concurrency();</span><br><span class="line"></span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">const</span> num_threads = <span class="built_in">std</span>::<span class="built_in">min</span>(hardware_threads != <span class="number">0</span> ? hardware_threads : <span class="number">2</span>, max_threads);</span><br></pre></td></tr></table></figure>

<h2 id="标识线程"><a href="#标识线程" class="headerlink" title="标识线程"></a>标识线程</h2><p>线程标识类型为<code>std::thread::id</code>，并可以通过两种方式进行检索：</p>
<ul>
<li><p>可以通过调用<code>std::thread</code>对象的成员函数<code>get_id()</code>来直接获取（如果<code>std::thread</code>对象没有与任何执行线程相关联，<code>get_id()</code>将返回<code>std::thread::id</code>默认构造值，这个值表示“无线程”）</p>
</li>
<li><p>当前线程中调用<code>std::this_thread::get_id()</code>也可以获得线程标识</p>
</li>
</ul>
<p><code>std::thread::id</code>类型对象提供相当丰富的对比操作，这允许程序员将其当做为容器的键值，做排序的比较。标准库也提供<code>std::hash&lt;std::thread::id&gt;</code>容器，所以<code>std::thread::id</code>也可以作为无序容器的键值。</p>
<p><code>std::thread::id</code>实例常用作检测线程是否需要进行一些操作</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::thread::id master_thread;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">some_core_part_of_algorithm</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">if</span>(<span class="built_in">std</span>::this_thread::get_id()==master_thread)</span><br><span class="line">  &#123;</span><br><span class="line">    do_master_thread_work();</span><br><span class="line">  &#125;</span><br><span class="line">  do_common_work();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>并发控制</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 234. 回文链表</title>
    <url>/2020/03/15/LeetCode-234/</url>
    <content><![CDATA[<h2 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h2><p><a href="https://leetcode-cn.com/problems/palindrome-linked-list/" target="_blank" rel="noopener">回文链表</a></p>
<p>请判断一个链表是否为回文链表。</p>
<p>示例 1 :</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">输入: 1-&gt;2</span><br><span class="line">输出: false</span><br></pre></td></tr></table></figure>

<p>示例 2 :</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">输入: 1-&gt;2-&gt;2-&gt;1</span><br><span class="line">输出: true</span><br></pre></td></tr></table></figure>

<p>进阶 :</p>
<p>你能否用 O(n) 时间复杂度和 O(1) 空间复杂度解决此题？</p>
<h2 id="解法一"><a href="#解法一" class="headerlink" title="解法一"></a>解法一</h2><p>最直观的想法是将链表中的所有值都读到数组中，然后判断是否为回文。时间复杂度为O(n)，空间复杂度O(n)。</p>
<h2 id="解法二（快慢指针）"><a href="#解法二（快慢指针）" class="headerlink" title="解法二（快慢指针）"></a>解法二（快慢指针）</h2><p>慢指针指向链表的开始位置，快指针先走到链表结尾处，比较值是否相等；然后慢指针走一步，快指针走n - 1步（走到结尾再回到开头），再比较值，直到快慢指针相遇（即慢指针走到链表的一半处）。时间复杂度为O(n<sup>2</sup>)，空间复杂度为O(1)。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="keyword">bool</span> <span class="title">isPalindrome</span><span class="params">(ListNode* head)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (head == <span class="literal">nullptr</span>)</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    ListNode *slow = head, *fast = head;</span><br><span class="line">    <span class="keyword">int</span> <span class="built_in">size</span> = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span> (fast-&gt;next) &#123;</span><br><span class="line">      fast = fast-&gt;next;</span><br><span class="line">      <span class="built_in">size</span>++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="built_in">size</span> / <span class="number">2</span>; i++) &#123;</span><br><span class="line">      <span class="keyword">if</span> (slow-&gt;val != fast-&gt;val)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">      slow = slow-&gt;next;</span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; <span class="built_in">size</span> - <span class="number">1</span>; j++) &#123;</span><br><span class="line">        fast = fast-&gt;next;</span><br><span class="line">        <span class="keyword">if</span> (!fast)</span><br><span class="line">          fast = head;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="解法三（翻转链表）"><a href="#解法三（翻转链表）" class="headerlink" title="解法三（翻转链表）"></a>解法三（翻转链表）</h2><p>先走到链表中点，再将之后的链表翻转，用双指针分别从链表的开头和结尾开始向中点移动并比较。例如 1-&gt;2-&gt;2-&gt;1 翻转成 1-&gt;2&lt;-2&lt;-1；1-&gt;2-&gt;3-&gt;2-&gt;1 翻转成 1-&gt;2-&gt;3&lt;-2&lt;-1。时间复杂度为O(n)，空间复杂度为O(1)。</p>
<p>用快慢指针的方式可快速找到链表的中点（慢指针走一步，快指针走两步）。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="keyword">bool</span> <span class="title">isPalindrome</span><span class="params">(ListNode* head)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (head == <span class="literal">nullptr</span>)</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    ListNode *slow = head, *fast = head;</span><br><span class="line">    <span class="keyword">while</span> (fast-&gt;next &amp;&amp; fast-&gt;next-&gt;next) &#123;</span><br><span class="line">      slow = slow-&gt;next;</span><br><span class="line">      fast = fast-&gt;next-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    fast = slow-&gt;next;</span><br><span class="line">    slow-&gt;next = <span class="literal">nullptr</span>;</span><br><span class="line">    <span class="keyword">while</span> (fast) &#123;</span><br><span class="line">      ListNode *tmp = fast;</span><br><span class="line">      fast = fast-&gt;next;</span><br><span class="line">      tmp-&gt;next = slow;</span><br><span class="line">      slow = tmp;</span><br><span class="line">    &#125;</span><br><span class="line">    fast = head;</span><br><span class="line">    <span class="keyword">while</span> (fast &amp;&amp; slow) &#123;</span><br><span class="line">      <span class="keyword">if</span> (fast-&gt;val != slow-&gt;val) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">      &#125;</span><br><span class="line">      fast = fast-&gt;next;</span><br><span class="line">      slow = slow-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>算法</category>
      </categories>
      <tags>
        <tag>LeetCode</tag>
        <tag>ALT快慢指针</tag>
        <tag>ALT链表翻转</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 974. 和可被 K 整除的子数组</title>
    <url>/2020/03/09/LeetCode-974/</url>
    <content><![CDATA[<h2 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h2><p><a href="https://leetcode-cn.com/problems/subarray-sums-divisible-by-k/" target="_blank" rel="noopener">和可被 K 整除的子数组</a></p>
<p>给定一个整数数组 A，返回其中元素之和可被 K 整除的（连续、非空）子数组的数目。</p>
<p>示例 1 :</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">输入：A = [<span class="number">4</span>,<span class="number">5</span>,<span class="number">0</span>,<span class="number">-2</span>,<span class="number">-3</span>,<span class="number">1</span>], K = <span class="number">5</span></span><br><span class="line">输出：<span class="number">7</span></span><br><span class="line">解释：</span><br><span class="line">有 <span class="number">7</span> 个子数组满足其元素之和可被 K = <span class="number">5</span> 整除：</span><br><span class="line">[<span class="number">4</span>, <span class="number">5</span>, <span class="number">0</span>, <span class="number">-2</span>, <span class="number">-3</span>, <span class="number">1</span>], [<span class="number">5</span>], [<span class="number">5</span>, <span class="number">0</span>], [<span class="number">5</span>, <span class="number">0</span>, <span class="number">-2</span>, <span class="number">-3</span>], [<span class="number">0</span>], [<span class="number">0</span>, <span class="number">-2</span>, <span class="number">-3</span>], [<span class="number">-2</span>, <span class="number">-3</span>]</span><br></pre></td></tr></table></figure>

<h2 id="解法一（哈希表）"><a href="#解法一（哈希表）" class="headerlink" title="解法一（哈希表）"></a>解法一（哈希表）</h2><p>看着题目与 <a href="https://leetcode-cn.com/problems/subarray-sum-equals-k/" target="_blank" rel="noopener">LeetCode 560. 和为K的子数组</a> 很像，就依葫芦画瓢地用了hash表的方法，时间复杂度为O(n<sup>2</sup>)，想法很好，不幸的是超时了。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="keyword">int</span> <span class="title">subarraysDivByK</span><span class="params">(<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&amp; A, <span class="keyword">int</span> K)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">unordered_map</span>&lt;<span class="keyword">int</span>, <span class="keyword">int</span>&gt; table;</span><br><span class="line">    table.insert(&#123;<span class="number">0</span>, <span class="number">1</span>&#125;);</span><br><span class="line">    <span class="keyword">int</span> sum = <span class="number">0</span>, ans = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; A.<span class="built_in">size</span>(); i++) &#123;</span><br><span class="line">      sum += A[i];</span><br><span class="line">      <span class="keyword">auto</span> iter = table.<span class="built_in">begin</span>();</span><br><span class="line">      <span class="keyword">while</span> (iter != table.<span class="built_in">end</span>()) &#123;</span><br><span class="line">        <span class="keyword">if</span> ((sum - iter-&gt;first) % K == <span class="number">0</span>)</span><br><span class="line">          ans += iter-&gt;second;</span><br><span class="line">        iter++;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span> (table.<span class="built_in">find</span>(sum) != table.<span class="built_in">end</span>())</span><br><span class="line">        table.<span class="built_in">find</span>(sum)-&gt;second++;</span><br><span class="line">      <span class="keyword">else</span></span><br><span class="line">        table.insert(&#123;sum, <span class="number">1</span>&#125;);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> ans;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="解法二（取余）"><a href="#解法二（取余）" class="headerlink" title="解法二（取余）"></a>解法二（取余）</h2><p>其实两道题确实有关联，都是前缀和的题目，不过用hash表来记录前缀和的方法不适合这道题。</p>
<p>考虑到如果两个前缀和对 K 进行取余的结果相等，则这两个前缀和之间的元素和可以被 K 整除，由于我们可以用大小为K的数组去记录所有前缀和的余数分布，然后累加起来得到最终结果。</p>
<p><strong>取余公式：(x % K + K) % K</strong></p>
<p>还要记得额外处理前缀和对 K 取余为0的情况。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> &#123;</span></span><br><span class="line"> <span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="keyword">int</span> <span class="title">subarraysDivByK</span><span class="params">(<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&amp; A, <span class="keyword">int</span> K)</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; <span class="title">count</span><span class="params">(K)</span></span>;</span><br><span class="line">    <span class="keyword">int</span> sum = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; A.<span class="built_in">size</span>(); i++) &#123;</span><br><span class="line">      sum += A[i];</span><br><span class="line">      count[(sum % K + K) % K]++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> ans = count[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; K; i++) &#123;</span><br><span class="line">      ans += count[i] * (count[i] - <span class="number">1</span>) / <span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> ans;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>算法</category>
      </categories>
      <tags>
        <tag>LeetCode</tag>
        <tag>ALT前缀和</tag>
        <tag>AL取余</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 713. 乘积小于K的子数组</title>
    <url>/2020/03/09/LeetCode-713/</url>
    <content><![CDATA[<h2 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h2><p><a href="https://leetcode-cn.com/problems/subarray-product-less-than-k/" target="_blank" rel="noopener">乘积小于K的子数组</a></p>
<p>给定一个正整数数组 nums ，找出该数组内乘积小于 k 的连续的子数组的个数。</p>
<p>示例 1:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">输入: nums &#x3D; [10,5,2,6], k &#x3D; 100</span><br><span class="line">输出: 8</span><br><span class="line">解释: 8个乘积小于100的子数组分别为: [10], [5], [2], [6], [10,5], [5,2], [2,6], [5,2,6]。</span><br><span class="line">需要注意的是 [10,5,2] 并不是乘积小于100的子数组。</span><br></pre></td></tr></table></figure>

<h2 id="解题思路（滑动窗口）"><a href="#解题思路（滑动窗口）" class="headerlink" title="解题思路（滑动窗口）"></a>解题思路（滑动窗口）</h2><p>采用滑动窗口的方法，先设置left初始值为0，然后循环增加right的值，乘积（滑动窗口）乘以 nums[right]，单调不减，直到乘积 &gt;= k；再将left向右移动，乘积除以 nums[right]，单调不增，直到乘积 &lt; k。算法时间复杂度为O(n)。</p>
<p>题目难点在于，当right增长时如何计数，计数代码为：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">ans += right - left + <span class="number">1</span></span><br></pre></td></tr></table></figure>

<p>具体为什么这样计算，参照<strong>累加和公式</strong>：</p>
<p>$\sum_1^n x = n + \sum_1^{n-1} x$</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="keyword">int</span> <span class="title">numSubarrayProductLessThanK</span><span class="params">(<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&amp; nums, <span class="keyword">int</span> k)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (k &lt;= <span class="number">1</span>)</span><br><span class="line">      <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> product = <span class="number">1</span>, ans = <span class="number">0</span>, left = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> right = <span class="number">0</span>; right &lt; nums.<span class="built_in">size</span>(); right++) &#123;</span><br><span class="line">      product *= nums[right];</span><br><span class="line">      <span class="keyword">while</span> (product &gt;= k) &#123;</span><br><span class="line">        product /= nums[left];</span><br><span class="line">        left++;</span><br><span class="line">      &#125;</span><br><span class="line">      ans += right - left + <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> ans;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>算法</category>
      </categories>
      <tags>
        <tag>LeetCode</tag>
        <tag>ALT滑动窗口</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 560. 和为K的子数组</title>
    <url>/2020/03/08/LeetCode-560/</url>
    <content><![CDATA[<h2 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h2><p><a href="https://leetcode-cn.com/problems/subarray-sum-equals-k/" target="_blank" rel="noopener">和为K的子数组</a></p>
<p>给定一个整数数组和一个整数 k，你需要找到该数组中和为 k 的连续的子数组的个数。</p>
<p>示例 1 :</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">输入:nums &#x3D; [1,1,1], k &#x3D; 2</span><br><span class="line">输出: 2 , [1,1] 与 [1,1] 为两种不同的情况。</span><br></pre></td></tr></table></figure>

<h2 id="解法一（滑动窗口）"><a href="#解法一（滑动窗口）" class="headerlink" title="解法一（滑动窗口）"></a>解法一（滑动窗口）</h2><p>我的思路是采用滑动窗口的方式，将窗口的大小（即sub_size，子数组大小）从1一直增长到数组大小，测试所有窗口大小下的情况，时间复杂度为 O(n<sup>2</sup>)。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="keyword">int</span> <span class="title">subarraySum</span><span class="params">(<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&amp; nums, <span class="keyword">int</span> k)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> <span class="built_in">size</span> = nums.<span class="built_in">size</span>(), count = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> sub_size = <span class="number">1</span>; sub_size &lt;= <span class="built_in">size</span>; sub_size++) &#123;</span><br><span class="line">      <span class="keyword">int</span> windows_sum = <span class="number">0</span>;</span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; sub_size; i++) &#123;</span><br><span class="line">        windows_sum += nums[i];</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span> (windows_sum == k)</span><br><span class="line">        count++;</span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">int</span> j = sub_size; j &lt; <span class="built_in">size</span>; j++) &#123;</span><br><span class="line">        windows_sum += nums[j] - nums[j - sub_size];</span><br><span class="line">        <span class="keyword">if</span> (windows_sum == k)</span><br><span class="line">          count++;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> count;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="解法二（哈希表）"><a href="#解法二（哈希表）" class="headerlink" title="解法二（哈希表）"></a>解法二（哈希表）</h2><p>解法二是LeetCode官方给出的解法——<a href="https://leetcode-cn.com/problems/subarray-sum-equals-k/solution/he-wei-kde-zi-shu-zu-by-leetcode/" target="_blank" rel="noopener">采用哈希表</a>，算法的时间复杂度为O(n)。</p>
<p>思想是：如果 sum[j] - sum[i] = k，则索引i和j之间的元素和为k；而 sum[j] - k = sum[i]也是一样的意思。</p>
<p>我们用哈希表去依次记录从0一直到size - 1的累加和，以及累加和出现的次数。每次算得一个位置的累加和sum，就判断 sum - k 的结果是否在hash表中，以及出现的次数，这就表示现在求得的累加和与之前某个位置的累加和之间的元素和为K，用count记录得到最终结果。</p>
<p><strong>注意</strong>哈希表一开始要 <code>map.insert({0, 1});</code>，这步不能忘了。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> &#123;</span></span><br><span class="line"> <span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="keyword">int</span> <span class="title">subarraySum</span><span class="params">(<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&amp; nums, <span class="keyword">int</span> k)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">unordered_map</span>&lt;<span class="keyword">int</span>, <span class="keyword">int</span>&gt; <span class="built_in">map</span>;</span><br><span class="line">    <span class="built_in">map</span>.insert(&#123;<span class="number">0</span>, <span class="number">1</span>&#125;);</span><br><span class="line">    <span class="keyword">int</span> <span class="built_in">size</span> = nums.<span class="built_in">size</span>(), sum = <span class="number">0</span>, count = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="built_in">size</span>; i++) &#123;</span><br><span class="line">      sum += nums[i];</span><br><span class="line">      <span class="keyword">if</span> (<span class="built_in">map</span>.<span class="built_in">find</span>(sum - k) != <span class="built_in">map</span>.<span class="built_in">end</span>())</span><br><span class="line">        count += <span class="built_in">map</span>.<span class="built_in">find</span>(sum - k)-&gt;second;</span><br><span class="line">      <span class="keyword">if</span> (<span class="built_in">map</span>.<span class="built_in">find</span>(sum) != <span class="built_in">map</span>.<span class="built_in">end</span>())</span><br><span class="line">        <span class="built_in">map</span>.<span class="built_in">find</span>(sum)-&gt;second++;</span><br><span class="line">      <span class="keyword">else</span></span><br><span class="line">        <span class="built_in">map</span>.insert(&#123;sum, <span class="number">1</span>&#125;);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> count;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>算法</category>
      </categories>
      <tags>
        <tag>LeetCode</tag>
        <tag>ALT滑动窗口</tag>
        <tag>ALT前缀和</tag>
        <tag>AL哈希表</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2020/02/26/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/" target="_blank" rel="noopener">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/" target="_blank" rel="noopener">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html" target="_blank" rel="noopener">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues" target="_blank" rel="noopener">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">"My New Post"</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html" target="_blank" rel="noopener">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html" target="_blank" rel="noopener">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html" target="_blank" rel="noopener">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html" target="_blank" rel="noopener">Deployment</a></p>
]]></content>
  </entry>
</search>
